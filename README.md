# SEWCP — Semiconductor Electrostatic Wafer Chuck Platform

**A 300 mm bipolar electrostatic chuck pedestal for RF-biased plasma process equipment —
specified, modelled, drawn and verified end to end by an AI engineering agent operating under a
repository-driven governance framework, with every claim computed rather than asserted.**

| | |
|---|---|
| **Repository release** | `v0.11.0` — CAD and software complete |
| **Engineering baseline** | SEWCP Rev A, Volumes 00–09 — **FROZEN** |
| **Governing framework** | AIEF 1.0.0 — **FROZEN**, fifteen amendments, all six compiler stages emitted |
| **Physical qualification** | **NOT STARTED.** 0 of 91 hardware-verifiable requirements verified — no article has been built |
| **Hardware build** | **BLOCKED** by [`ECR-D-016`](.ai/project/ecr/ECR-D-016_Support_Ring_Isolation_Joint_Does_Not_Close.md) pending a Rev B baseline revision |
| **Licence** | MIT (code) AND CC-BY-4.0 (documents and design data) — boundary by path, see [`LICENSE`](LICENSE) |

---

## Read this part first

This repository is a **design and process artifact, not a qualified product.** Two things are
true at once and both matter:

- Every CAD-verifiable property is verified, computed from observed model state, and
  reproducible from a clean clone — 821 tests pass there, 873 with the tokenizer artifacts.
  Every standing check exits 0 **except two, which exit 1 and are meant to**:
  `aief_analysis`, because `ECR-D-016` does not close, and `aief_exec check`, which reports
  three open exec-layer conditions of its own.
- **Nothing physical has been built or measured.** 91 of the 137 numbered component
  requirements need hardware and none is verified. Four mass figures are `MODEL-PREDICTED` and
  labelled so. See [`PVR-001`](.ai/project/verification/PVR-001_Physical_Verification_Record_And_Test_Matrix.md).

And one defect is open against the design itself. Filing the creepage/clearance drawing trace
that an open item recorded as owed found that **the Support Ring isolation joint does not
close**: `SR-02`, `SR-03` and `SR-04` all fail on frozen dimensions, for one reason — two
sections of `spec/03` compute a flange gap as empty while a third puts a 6.00 mm grounded ring
inside it. It is ruled, its remedy is computed, and it is **deliberately not implemented here**
because implementing it is a specification re-baseline. Do not build to this baseline.

---

## What this is

| Layer | What it holds |
|---|---|
| **Engineering specification** | `spec/` — Volumes 00–09, 142 requirements, frozen and hash-registered. The Base Plate `SEWCP-100` is frozen and out of scope; nine components are specified |
| **CAD** | Ten Fusion 360 designs, a 19-occurrence master assembly, 11 drawing documents over 14 sheets, an indentured BOM — all generated by agent-driven automation and verified against observed model state |
| **Deliverables** | `cad/exports/`, `drawings/`, `cad/bom/` — 61 files, every one digest-registered and checked bi-directionally |
| **Framework** | `framework/` + `.ai/` — AIEF, a repository-driven operating system for AI-assisted engineering. Laws, roles, gates, approvals, a hash-chained ledger, and a six-stage compiler |
| **Tooling** | `src/` — eight Python packages implementing the standing checks. `tests/` — 873 tests |

---

## Architecture, in one screen

```
  spec/**  (frozen, hash-registered)
     |
     |  requirements  ->  parameter master  ->  design solution
     v
  src/aief_cad  ------- commands ------->  cad/bridge/queue/  ---.
                                                                  |
  Fusion 360 + fusion_addin/AIEF_CAD_Bridge  <---------------------'
     |                                                            
     |  OBSERVED model state (never the command's own claim)      
     v                                                            
  cad/bridge/obs/  ->  cad/runs/<run>/run.json   (tracked evidence)
     |
     v
  verification  ->  cad/runs/SYSTEM_INTERFACES.json, FINAL_SYSTEM_VERIFICATION.json
     |
     v
  drawings/generate.py  ->  SVG + PDF + provenance sidecar per dimension
  src/sedep/bom         ->  indentured BOM, cross-checked four ways
```

**The rule that shapes all of it: a CAD operation is not evidence that it worked.** Every
acceptance reads the model back out of Fusion — extents, volume, mass, material, feature list —
and compares against the requirement. The file queue is the transport; `cad/runs/**/run.json`
is the record, and it is tracked, including the failures.

### CAD automation

`fusion_addin/AIEF_CAD_Bridge/` is a Fusion 360 add-in that polls a file queue, executes one
operation, and writes back what it *observed*. `src/aief_cad/` drives it: requirements are
compiled to a design solution, the solution to a command sequence, the sequence executed, and
the resulting model verified. A document is **saved only on a verified PASS**; a failed attempt
is discarded or reverted, which is why the tree carries no blank orphan designs
(`cad/DOCUMENT_LIFECYCLE.md`).

### Agent architecture

Five universal roles plus four `mechanical` and three `software` roles, each with a contract
declaring responsibilities, allowed and forbidden actions, escalation rules and duty conflicts
(`.ai/core/agents/`). The constraint that does the work is **LAW-05: no role verifies its own
output.** Independent QA runs in a cold context that reconstructs every fact from the
repository — and it is not decorative: the most recent round returned **NOT CLEARED** and found
a blocking defect in a ruling that had passed 846 tests, and the round after that found the repair's own check did not catch what it claimed.

### Verification architecture

Nothing in this repository is believed because a document says it. Every property that matters
has a standing check that computes it, and the checks are attacked by tests that construct the
defect and require failure:

| Check | Property |
|---|---|
| `python -m aief_gate` | The `LC-M04-EXIT` criteria `C1`–`C7`, from the ECR records directly |
| `python -m aief_clearance` | `spec/00` §3.2 feature clearance, pair by pair |
| `python -m aief_params check` | The 105-parameter CAD master against the package |
| `python -m aief_approval verify` | Approval-chain integrity under LAW-10 |
| `python -m aief_deliverables` | The 61 deliverables against their register, both directions |
| `python -m aief_analysis` | `SR-07`/`AP-08` loads and the `SR-02/03/04` insulation trace |
| `python -m aief_register` | No state register asserts a value another artifact governs |
| `python -m aief_stage6` | The framework's own release build; boot step B2a |

Four defect classes went unnoticed for many sessions, and every one was **a declared property
with no standing check**. That is the lesson the repository is organised around.

---

## Reproducing this

```bash
git clone https://github.com/Raar1999/SEWCP_Master_Assembly.git
cd SEWCP_Master_Assembly
python -m venv .venv && . .venv/Scripts/activate     # Windows; use bin/activate on POSIX
pip install -r requirements.txt

PYTHONPATH=src python -m aief_gate            # LC-M04 CAD-READY: YES, exit 0
PYTHONPATH=src python -m aief_clearance       # CLEARANCE OK
PYTHONPATH=src python -m aief_params check    # 105 parameters
PYTHONPATH=src python -m aief_approval verify # APPROVAL CHAINS OK
PYTHONPATH=src python -m aief_deliverables    # DELIVERABLES OK - 61 registered, 61 reproduce
PYTHONPATH=src python -m pytest tests/ -q     # 821 passed, 52 skipped
PYTHONPATH=src python -m aief_analysis        # EXITS 1 BY DESIGN - see below
```

**These are the numbers a clone actually produces**, measured by cloning this repository from
GitHub and running the battery against it — not the numbers from the author's machine. Every
standing check exits 0 offline except `aief_analysis`, which exits 1 by design. The suite is **821 passed, 52 skipped, 0 failed**; with the
tokenizer artifacts provisioned it is **873 passed, 0 skipped**.

**`aief_analysis` exits 1 and is meant to.** It files the `SR-03`/`SR-04` trace, and the trace
does not close. A check reporting PASS on `ECR-D-016` would be the defect, not the finding.

### The 52 skips, and how to make them run

They measure token budgets through the two normative tokenizer families declared at
`AIEF-AMD-010` §AMD-26, whose artifacts are **third-party binaries and are not tracked here**.
Nothing estimates in their absence — `budget_measurement_record` rules that absence *blocks*
rather than estimates — so the tests skip rather than guess. To run them, place both artifacts
in `build/stage6/tokenizer_artifacts/`:

| Family | File | Raw-octet SHA-256 pinned in `core/MANIFEST.lock` |
|---|---|---|
| TF-1 | `cl100k_base.tiktoken` | `223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7` |
| TF-2 | `spiece.model` (T5 SentencePiece) | `d60acb128cf7b7f2536e8f38a5b18a05535c9e14c7a355904270e15b0945ea86` |

`AIEF_STAGE6_TOKENIZER_ARTIFACTS` overrides the directory. A sibling
`TRUST_ON_FIRST_USE.json` recording `sha256_raw_octets` per artifact is optional: with no
record the pin is computed from the artifact in hand; **with a record, a mismatch is a loud
failure and the measurement is refused** — never a silent re-pin. TF-1 is read directly from
the `.tiktoken` vocabulary file with no network and no cache.

**This gap was a defect, and it was found the only way it could be.** Until `S-2026-08-17-01`
these 35 of the 52 had no skip guard and **failed** on a clone while passing at the desk, so
`README.md` claimed a reproducibility that had never been tested from outside. Recorded as
`OI-V-16`; the `validate` workflow now runs the battery on GitHub's runners, where there is no
author's machine to be lucky on.

**What a clone cannot reproduce at all.** The Fusion designs live in Autodesk cloud
versioning, so regenerating `cad/` needs Fusion 360 with the add-in installed. Everything else
— every gate, every digest, the drawing set, the BOM and the whole test suite — runs offline.

---

## Deliverables

61 files, 4,995,097 bytes, every digest registered in
[`cad/DELIVERABLES.md`](cad/DELIVERABLES.md) and checked in both directions.

| | |
|---|---|
| Neutral geometry | 11 STEP — 10 components + the master assembly — `cad/exports/step/` |
| Tessellated | 10 STL — `cad/exports/stl/` |
| Drawings | 11 documents / 14 sheets, SVG + PDF + a provenance sidecar per dimension (79 dimensions, 0 unsourced) — `drawings/` |
| BOM | Indentured Rev A, cross-checked four ways — `cad/bom/` |

The parametric `.f3d` is deliberately **not** here: `SEDEP-PMP-002` §3.1 places the parametric
source of record in Fusion cloud versioning and git holds the neutral record.

---

## Status, honestly

| | |
|---|---|
| Gate `LC-M04-EXIT` | **PASS** — `C1`–`C7`, computed |
| Tests | **873 pass, 0 fail** locally; **821 pass, 52 skip, 0 fail** from a clean clone |
| Boot step B2a (core integrity) | **PASS** — 75/75, independently recomputed |
| Freeze registry | **31 of 31 verify** |
| Approval chains | **Clean** |
| Assembly | 19 occurrences, interfaces 12/12, final system verification 19/19 |
| **Physical verification** | **0 of 91. NOT VERIFIED — HARDWARE REQUIRED** |
| **`ECR-D-016`** | **OPEN — blocks hardware build.** Rev B baseline revision required |
| **`ECR-D-014`** | **OPEN** — dispositioned, applied, repaired; awaiting one fresh independent QA round |
| `CMP-BLOCK-004`/`-005` | Open. They gate **AIEF framework Release 1.0.0**, not this repository's release |

Everything open is at [`.ai/project/OPEN_ITEMS.md`](.ai/project/OPEN_ITEMS.md), with the full
record at [`OPEN_ITEMS_REGISTER.md`](.ai/project/OPEN_ITEMS_REGISTER.md). Nothing is hidden
there, including the failures.

---

## Navigating

| If you want to… | Go to |
|---|---|
| Understand the framework and how to work in this repo | [`ENGINEERING.md`](ENGINEERING.md) |
| Read the engineering baseline | [`spec/README.md`](spec/README.md) |
| Understand the architecture, datums, budgets | [`spec/00_…Architecture_and_Interface_Control.md`](spec/00_SEWCP-ENG-001_Architecture_and_Interface_Control.md) |
| See what is open and why | [`.ai/project/OPEN_ITEMS_REGISTER.md`](.ai/project/OPEN_ITEMS_REGISTER.md) |
| See the engineering decisions and their alternatives | [`.ai/project/decisions/`](.ai/project/decisions/) |
| See what only hardware can settle | [`PVR-001`](.ai/project/verification/PVR-001_Physical_Verification_Record_And_Test_Matrix.md) |
| Find any document | [`INDEX.md`](INDEX.md) |
| Contribute | [`CONTRIBUTING.md`](CONTRIBUTING.md) |

---

## Repository policy

Binding, in force now:

- **No AI attribution** in any commit, file, or document.
- **No `Co-authored-by`** trailers.
- **Git author information is never modified.**
- `spec/` is frozen; defects are raised as an ECR, never edited in place.
- No engineering decision is made in a commit.

Full text: [`CONTRIBUTING.md`](CONTRIBUTING.md) §1.

---

## Provenance

The engineering work in this repository — specification, CAD, drawings, analysis, the framework
and its tooling — was produced by an AI agent under human owner authority, with decision
authority delegated for specified runs and every delegated decision recorded as such in
[`.ai/project/decisions/`](.ai/project/decisions/). Human approvals and delegated decisions are
distinguished everywhere they appear, and **a delegated decision is never recorded as a human
approval.** Where a governing rule requires a genuinely independent party — LAW-05 —
independence is supplied by a cold-context session or the item stays open. `ECR-D-014` is open
for exactly that reason.
