# Session Summary — S-2026-08-17-03

> **Instance artifact**, partition `project`, per `tpl-session-summary`.
> The third round's repairs. **`ECR-D-014`'s enforcement condition is discharged**; what
> remained was record accuracy, and one defect that had recurred three sessions running and
> needed a procedural cure rather than a fourth correction.

## Session id

`S-2026-08-17-03` — matches `ledger/SEG-0000/L-0000004.session_id`.

## Role

`software.test-engineer` for the standing check added here, `repository-engineer` for the
records. **Not `qa-engineer`** — this session repairs what an independent context found and
LAW-05 bars it from certifying the repair, for the third time.

## What the third round found

**Verdict `NOT CLEARED`, and the most important line in it is a PASS.**

**Condition A — the canonical-path enforcement — is FULLY DISCHARGED**, and the round says it
should not be re-audited. It applied the four required mutations and **eleven further ones,
six of which the previous round had not tried**: deleting the post-condition, truncating
inside `boot_read_prefix` so both derivations agree on a wrong region, returning before the
post-condition, clamping instead of raising, comparing the record to itself, and an exploit of
the stub probe's leniency. **All fifteen die.** It also confirmed `_emit_once(canonical=True)`
cannot reach a canonical write: `assert_write_allowed` gates the only two sites and both sit
after the post-condition.

**Condition B failed on record accuracy alone.** Two artifacts still published `TCR-002`'s
finding count as *six* — `CHANGELOG.md`, which the repair commit never touched, and a line of
`DECISIONS_S-2026-08-17-01.md` four lines below one that *was* corrected.

**And it found the same defect for the third consecutive session.** My corrections to
`STATE_REGISTER.md` were themselves stale on the day they were written: `last_ledger_seq` `2`
against a live `3`, and `R-023`/`R-025` named as current heads by the very commit that
superseded them.

## Work performed

1. **The recurring defect cured procedurally, not corrected again.** The register no longer
   recites `last_ledger_seq`, the exec-layer head, or `HEAD`'s token cost — three values other
   artifacts govern and that a session's own close moves. `tests/test_state_register_currency.py`
   makes their absence a checked property: it fails if the register states a
   `last_ledger_seq` disagreeing with `ledger/HEAD`, or names a result id as a current head
   that `aief_exec` does not agree is current. **Both round-3 defects were reinstated into the
   live file and both were caught**, then reverted.
2. **Five record corrections**: the two *six findings* → **eight**; `LICENSE` §1a's
   `.gitkeep` → `validate.yml` and its file count withdrawn rather than re-stated; `LICENSE`
   §1's exhaustiveness claim reconciled with §1a; a *"to the digit"* left in a test docstring.
3. **`cad/DELIVERABLES.md` brought into line with `ECR-D-015` §5** — it still published the
   byte-identity claim the ECR had withdrawn, which is the round-2 finding recurring inside
   its own repair.
4. **`OI-C-10` arrived.** See below.

## `OI-C-10` arrived, in the same run, one round later

The previous session published the arithmetic: 597 tokens against a 600 cap, ~6 per
identifier, *"the next identifier raised against this repository breaches `V-09` and halts
Stage 6"*.

This session raised `OI-V-18` for the register-staleness finding. The index went to **602**
and `V-09` halted, exactly as written.

**The identifier was withdrawn and the finding folded into `OI-V-17`.** That is defensible on
the merits — both are about the register's content being governed by nothing — and it is
recorded at both rows as **forced by the budget rather than chosen**, because a register that
merges findings to stay inside a cap has stopped being a register of findings. There is no
grammar left to reduce and closing items frees nothing.

**There is no session-level action left.** The decision is A4's and the human owner's, and it
now blocks the *next* finding rather than the next dozen.

## Artifacts touched

`.ai/project/STATE_REGISTER.md` · `tests/test_state_register_currency.py` (new) ·
`.ai/project/{OPEN_ITEMS.md,OPEN_ITEMS_REGISTER.md,STATE.md}` ·
`.ai/project/decisions/DECISIONS_S-2026-08-17-01.md` · `CHANGELOG.md` · `LICENSE` ·
`cad/DELIVERABLES.md` · `tests/test_analysis_oi_c_15.py` · `results/R-028.md` · this file and
the ledger.

**Not touched:** `spec/**`, `src/aief_stage6/**`, every CAD artifact, every deliverable byte.
No Fusion document was opened.

## Open items

`OI-V-17` absorbed the register-staleness finding; `OI-C-10` records its own arrival.
**`ECR-D-014` remains open**, and what it waits on is now the narrowest it has ever been: a
fourth cold round scoped to the five corrections above. Its **enforcement** condition is
discharged and the third round says so in terms.

## Next action

A fourth cold round, scoped to this session's corrections only. Then the architecture decision
`OI-C-10` now forces, and the Rev B baseline revision `ECR-D-016` requires before any hardware.
