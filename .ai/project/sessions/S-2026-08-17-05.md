# Session Summary — S-2026-08-17-05

> **Instance artifact**, partition `project`, per `tpl-session-summary`.
> **`ECR-D-014` is closed**, on the fifth independent round's own recommendation. It took five
> rounds, four of which found a defect in the previous repair.

## Session id

`S-2026-08-17-05` — matches `ledger/SEG-0000/L-0000006.session_id`.

## Role

`software.test-engineer` for the check, `repository-engineer` for the records, with the
`chief-systems-engineer` act `STATE.md` requires. **Not `qa-engineer`** — and that is the whole
point of the closure: the act that closed `ECR-D-014` was an independent round's, in a distinct
cold session, and this session only records it.

## What the fifth round found

**Verdict `NOT CLEARED` on the round-4 corrections — and `close` on `ECR-D-014`.** It was asked
to weigh three options and chose (c): close, with named residuals.

| | |
|---|---|
| **The check was still incomplete** | Of **thirteen** sequence phrasings it constructed, **twelve got past it** — including `last_ledger_seq: 2`, the field's own name, because there is no word boundary after an underscore. Seven of nine governing sections were unscanned. The fenced YAML block that *is* the governed state in `STATE.md` was skipped entirely. And one test was **circular** — it injected a line `seq 2` that never existed in the register and passed only because of it |
| **`STATE.md`'s `next_action` was a full round stale** | The **B3 resume point**, saying "three rounds" and "a fourth round is all that remains" after the fourth had closed |
| **Two counts were asserted, not measured** | "four artifacts" where three existed; a coverage claim wider than the coverage |
| **`README.md` had two false claims** | Three standing checks exit 1, not one; and "873 passed, 0 skipped" describes a condition **nothing can produce** — one test needs a gitignored directory no code creates |

It also re-proved the substance: prefix 56/69 against 200, MI-4 5904, B2a 75/75, five ledger
entries recomputing and linking, `spec/**` byte-identical across all five release commits by
tree hash, and 0 tracked files in neither licence column and 0 in both.

## Work performed

1. **The check rebuilt again, and this time by abandoning the approach.** Matching *phrasings*
   is a losing game — a rule that catches `seq 2` is beaten by "the second entry", and one that
   catches both is beaten by "entry two". So a section named for a governed field may now
   contain **no numeral and no written number at all**. There is nothing left to phrase around.
   Scope widened to every section and into the fenced block, exempting only a governed field
   stating its own value.
2. **Re-measured against the round's own evasion list: 13 of 13 and 8 of 8 now caught**, every
   section hole closed, the YAML case closed, and both converse properties still holding — a
   blockquote recounting the defect does not fire, and the findings register stays out of scope.
3. **The circular test removed.** Every phrasing in the suite is now transcribed from a round's
   report, not written to suit a pattern. 43 tests.
4. **`ECR-D-014` CLOSED** with `closed_at`, `closed_by` and a `closure_basis` naming all five
   rounds; the register row relocated with its text carried verbatim.
5. `STATE.md`'s `next_action`, the two counts, and both `README.md` claims corrected.

## `ECR-D-014` — what the closure says, and what it does not

**Says:** the ruling is sound and independently re-proved three times; its enforcement is real
and survived fifteen mutations plus six more; LAW-02 clause 5 and LAW-05 are satisfied because
**no session that repaired it certified its own repair, across four attempts**.

**Does not say the repair sessions were clean.** They were not. Four consecutive ones each
introduced a defect of the class they were repairing. **That is a process finding, it is real,
and it belongs to `OI-V-17`** — round 5's reasoning, adopted here: holding the ECR open a sixth
time would make it the carrier for a defect class it does not own, and would not reach the
thing actually causing it.

## Open items

**Nothing new raised, for the third session running.** `OI-C-10` stands at 597 of 600 against
~5 per identifier. Round 5's Findings 2–7 are folded into `OI-V-17` and Findings 1 and 8 into
the session-close discipline. **Three findings in three sessions have now been denied their own
identifier.** That is what `OI-C-10` costs, and it is an A4 and human-owner decision that no
session can make.

`DEC-21` dispositions the local username in ten tracked CAD run records: **preserve**, because
they are tracked failure evidence and redacting them would edit the record of what was
observed. Recorded as a decision so the owner can revisit it deliberately.

## Next action

The `OI-C-10` architecture decision. Then the Rev B baseline revision `ECR-D-016` requires
before any hardware is built to this design.
