# Session Summary — S-2026-08-18-01

> **Instance artifact**, partition `project`, per `tpl-session-summary`.
> **Portfolio and governance closeout.** No engineering artifact was created, modified or
> deleted. The one governance act is the lawful supersession `R-030` → `R-031`, which the
> portfolio pass's own README rewrite made owed.

## Session id

`S-2026-08-18-01` — matches `ledger/SEG-0000/L-0000007.session_id`.

## Role

`documentation-engineer` for `docs/**`, `README.md` and the narrative corrections;
`repository-engineer` for the records, `.gitattributes` and the result supersession. Assigned at
B9 by live human instruction (`core/PRECEDENCE.md` rank 1), with delegated authority for ordinary
repository and governance decisions that do not alter verified engineering intent.

**`qa-engineer` is not among them.** Nothing here is certified by this session; `X-06` and the
suite are the instruments, and they are run, not asserted.

## What made this session necessary

The portfolio pass rewrote `README.md`. `README.md` is a **pinned deliverable of `R-030`**, so
its DC-1 moved, `R-030` went STALE, and `X-06` failed:

```
X-06 FAIL: R-030: declared CURRENT but is STALE
  deliverable README.md: DC-1 bde3fcf4…5639a != pinned be747001…f73f
```

That is the drift detector working. It was isolated before it was answered: with the original
`README.md` restored and the whole of `docs/**` present, `tests/test_exec_checks.py` runs
**107 passed** — the documentation tree stales nothing, and only the README rewrite does.

## Work performed

1. **`R-030` → `R-031`, by supersession and never by mutation** (§6.1). `T-009` declares
   `produces: R-031` and a write path to it (**X-09** mode 1); `R-030` takes its closing edit and
   nothing else; `R-031` seals `R-030` at the DC-1 it now stands at and republishes every input
   and deliverable digest against the finished tree. **`X-06` PASS.**
2. **Both digest constructions reproduced before use.** DC-3 against the two normative worked
   examples of `AIEF-AMD-008` §AMD-17 **and** against the live `L-0000006` entry hash; DC-1
   against `R-030`'s existing seal over `R-029`. A digest implementation that has not reproduced
   a known answer is not one.
3. **Six stale narrative claims corrected**, each against a named governing source, each with the
   superseded reading recorded rather than discarded — `ENGINEERING.md` §1's tag name, four cells
   of `RELEASE_READINESS_v0.11.0.md`, and this repository's `CHANGELOG.md` [0.11.0] lead. The
   **body** sections of both are as written at their rounds and are left so.
4. **`.gitattributes` extended to `portfolio/renders/**`.** Staging the two PNG renders produced
   `warning: CRLF will be replaced by LF` — the blanket `* text eol=lf` rule reaching binary
   content, which is the exact failure `ECR-D-015` recorded for the 24 binary deliverables. The
   file's own comment says *"Re-run that check before adding any"*; it was re-run, it fired, and
   the declaration is the answer. Both PNGs now round-trip byte-identically through the index,
   and all 61 existing binary deliverables were re-checked and still do.
5. **`docs/**` published** — the portfolio layer, carrying no authority, plus a findings register
   that records what was *not* repaired.
6. **Two renders generated from repository evidence**, not captured by hand:
   `scripts/render_assembly.py` composes the released STL exports with the observed occurrence
   transforms and **reconciles every placement against the bounding box Fusion observed** before
   drawing it. 18 of 19 agree to ≤ 0.072 mm; the coarsely tessellated saddle deviates 3.864 mm
   inward while its mesh volume agrees to 0.018 %. `--strict` refuses to render past tolerance.
7. **GitHub Release notes prepared and left unpublished**, with the publication gate written into
   the file.

## Two things that were got wrong first, and are recorded rather than tidied away

- **The renderer's first depth solution was a painter's algorithm.** Centroid order is not depth
  order for overlapping triangles of very different size, so the heater plate painted through the
  ESC puck's top face. Replaced with a per-pixel z-buffer. The wrong approach is described in the
  module docstring.
- **The renderer was first placed at `portfolio/render_assembly.py`**, which puts a `.py` under a
  tree `LICENSE` §1 lists in the CC-BY column while §1 also claims *"`*.py` anywhere else"* for
  MIT — **a file in both columns**, the precise property §1a asserts is empty. Moved to
  `scripts/**`, which is MIT and unambiguous. Re-measured over all 615 tracked files: **0 in
  neither column, 0 in both.**

## Artifacts touched

`README.md` · `docs/**` (new) · `portfolio/renders/*.png` (new) · `scripts/render_assembly.py`
(new) · `releases/GITHUB_RELEASE_NOTES_v0.11.0.md` (new) ·
`releases/RELEASE_READINESS_v0.11.0.md` · `CHANGELOG.md` · `ENGINEERING.md` · `.gitattributes` ·
`.ai/project/{tasks/T-009.md, results/R-030.md, results/R-031.md, STATE.md}` · this file and the
ledger.

**Not touched:** `spec/**`, `cad/**`, `drawings/**`, `analysis/**`, `params/**`,
`implementation/**`, `src/**`, `tests/**`, `framework/**`, `.ai/core/**`. **No Fusion document
was opened.** `X-06` was neither weakened nor bypassed — `src/aief_exec/**` and
`tests/test_exec_*.py` are byte-identical to `f8ff028`.

## Open items

**Nothing new raised, for the fourth session running, and `OI-C-10` is still why** — the bounded
index stands at 597 of 600 against ~5 per identifier. This session found one item that would
otherwise deserve an identifier: *narrative release documents restate values other artifacts
govern, and `aief_register` does not reach them.* Six instances were corrected; the class is not
closed, and the two lawful cures — widening the check's declared scope, or declaring those
documents to be historical snapshots — are engineering decisions. **Recorded at
`docs/DOCUMENTATION_FINDINGS.md` §3 and folded nowhere else. That is the fourth finding in four
sessions denied an identifier of its own.**

**Two findings are owner actions and are not repairable from inside the tree:** the repository is
**private** on GitHub while `CHANGELOG.md` said it becomes public, and **no GitHub Release object
exists** for any tag.

## Next action

The `PRIVATE → PUBLIC` visibility decision, which is the owner's and no session's. Then the
`OI-C-10` architecture decision. Then the Rev B baseline revision `ECR-D-016` requires before any
hardware is built to this design.
