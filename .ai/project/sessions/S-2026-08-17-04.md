# Session Summary — S-2026-08-17-04

> **Instance artifact**, partition `project`, per `tpl-session-summary`.
> The fourth round's repairs — and the round found that **the check written to end a recurring
> defect did not catch that defect**, while three artifacts said it did. That is this
> repository's own most-named failure mode, reproduced inside the repair written to end it.

## Session id

`S-2026-08-17-04` — matches `ledger/SEG-0000/L-0000005.session_id`.

## Role

`software.test-engineer` for the check and its tests, `repository-engineer` for the records.
**Not `qa-engineer`**, for the fourth consecutive session.

## What the fourth round found

**Verdict `NOT CLEARED`.** Five of its findings are substantive and all five are mine.

| | |
|---|---|
| **FIND-2, MAJOR** | `tests/test_state_register_currency.py` **did not catch the defect it was written for.** Its pattern was line-scoped (`[^\n]`) and the defect's own historical phrasing puts the heading and the value two lines apart, so the **verbatim** round-3 defect passed all seven tests. Of nine sequence phrasings the round tried, **2 were caught and 7 evaded**; of nine head phrasings, the same. The module's self-test was **circular** — it validated the pattern against a string written for the pattern. **Three** artifacts claimed "both were caught" — *this row said four; round 5 measured three, which is the same class of unmeasured count as the "six findings" defect two rows away* |
| **FIND-3, MAJOR** | The recital removed from the register was **live in `STATE.md`**, the governing file — `seq 2 (L-0000002)` against a live 4, forty lines from its own correct YAML value. The cure had moved the defect, not ended it |
| **FIND-1, MAJOR** | The round-3 "reconciliation" of `LICENSE` §1 left a **half-replaced sentence** asserting *"No tracked file falls in exactly one column"* — the negation of the property the licence turns on, contradicting §1a in the same document |
| **FIND-4, MAJOR** | `OI-V-14`'s lead still said *"six findings"* and *"Two of the six"*, contradicting its own correction further down the row. Third round running |
| **FIND-5, MAJOR** | `L-0000004`'s **narrative body is `L-0000003`'s prose with identifiers incremented** — it describes the second round's defect, cites 846 tests and M8–M13, and says it waits on a third round. Its DC-3-covered fields are all correct and the chain verifies; the *account* is false |

It also re-proved the substance cold: 75/75 DC-1, DC-4 = `aggregate_digest` = `core_digest_pin`,
prefix 56/69 against 200 with no run-scoped octet, MI-4 5904/96, and all four ledger entries
recomputing and linking — without importing `src/aief_stage6`.

## Work performed

1. **The check rebuilt as a module with a structural rule.** `python -m aief_register`
   replaces the line-scoped regex. The rule is one sentence: **outside a blockquote, in a
   section that governs, an identifier must be the live one.** Body text speaks in the
   register's own voice; a `>` line is a marked account.
2. **The first attempt at *that* was also wrong, and is recorded.** Scanning by keyword window
   over prose flagged **thirty** assertions, almost all of them the open-items register
   *recounting* the defect — *"`R-017` named as the exec-layer head when the heads are …"*.
   **A rule that cannot tell "X is current" from "X was wrongly called current" is a trap for
   the honest**, because the sentence that records a defect looks exactly like the defect. So
   `OPEN_ITEMS_REGISTER.md` is out of scope entirely: it is a findings record and must be free
   to quote what a finding was about.
3. **The tests replay the defects in the phrasings that actually occurred** — all six head
   phrasings and all the sequence phrasings round 4 found evading, plus both verbatim
   historical defects. and two of them assert the converse: a blockquote
   recounting the defect must *not* fail, and a non-governing section must not be scanned.
4. **`STATE.md`'s live recital removed**; the genesis fact in `STATE_REGISTER.md` moved into a
   blockquote, because it is permanent history and the rule says history is quoted.
5. **The three "both were caught" claims withdrawn** and restated as what happened.
6. `LICENSE` §1 corrected; `OI-V-14`'s lead corrected; two surviving *"to the digit"* wordings
   withdrawn; the `OI-C-10` preamble line count corrected.

## `L-0000004` — a false account in an append-only artifact

**It is not edited, and this is the correction.** `ledger/SEG-0000/L-0000004`'s prose body was
produced by incrementing identifiers in `L-0000003`'s and the narrative was never rewritten.
It therefore describes the **second** round's canonical-path defect, cites 846 tests and
mutations M8–M13, states that `S-2026-08-17-01` wrote `L-0000003`, and says the work waits on
a *third* cold round.

**What is true of it:** every DC-3-covered field — `seq`, `timestamp`, `session_id`, `actor`,
`action`, `artifacts`, `prev_hash` — is correct. Its `action` describes the third round
accurately. The digest recomputes, the chain links, and `HEAD` reconciles. **No digest moves
and the tamper-evidence is intact.**

**What is false:** the explanatory prose, which is not covered by DC-3 and which a reader would
take as the account of `S-2026-08-17-03`.

The artifact is `append-only`. Correcting it in place would be a rewrite of the ledger to make
a session look tidier, which is the precise act the chain exists to make detectable — so it
stands, wrong, with this record and `L-0000005` as its correction. **A false account that is
left standing and annotated is a smaller failure than a corrected one that leaves no trace.**

## Artifacts touched

`src/aief_register/**` (new) · `tests/test_state_register_currency.py` (rewritten) ·
`.ai/project/{STATE.md,STATE_REGISTER.md,OPEN_ITEMS_REGISTER.md}` ·
`.ai/project/decisions/DECISIONS_S-2026-08-17-01.md` ·
`.ai/project/{results/R-028.md,sessions/S-2026-08-17-03.md}` · `LICENSE` · `README.md` ·
`results/R-029.md` · this file and the ledger.

**Not touched:** `spec/**`, `src/aief_stage6/**`, every CAD artifact and every deliverable
byte. `ledger/SEG-0000/L-0000004` deliberately unmodified. No Fusion document was opened.

## Open items

Nothing new is raised, and **`OI-C-10` is why**: the index stands at 597 of 600 with a marginal
cost of ~5, so a new identifier would breach `V-09`. The register-check defect is recorded at
`OI-V-17`, which already carries the class. **That is the second finding in two sessions that
could not be given its own identifier**, and it is the clearest statement available of what
`OI-C-10` now costs.

**`ECR-D-014` remains open.** Its substance has been re-proved cold twice and its enforcement
condition was declared fully discharged by round 3. What holds it is record accuracy, and
LAW-05 bars this session from certifying its own corrections — for the fourth time.

## Next action

A fifth cold round, scoped to this session's corrections only, and told in terms that the
enforcement condition and corrections 3 and 5 are settled. Then the `OI-C-10` architecture
decision, which no session can make, and the Rev B baseline revision `ECR-D-016` requires
before any hardware.
