# Session Summary — S-2026-08-18-02

> **Instance artifact**, partition `project`, per `tpl-session-summary`.
> **`DEC-21` reopening and publication-safety history sanitization.** One local account name
> removed from all reachable history before publication. **No engineering artifact was created,
> modified or deleted**, and no failure was hidden, softened or deleted.

## Session id

`S-2026-08-18-02` — matches `ledger/SEG-0000/L-0000008.session_id`.

## Role

`repository-engineer`, assigned at B9 by live human instruction (`core/PRECEDENCE.md` rank 1).
`LAW-07` clause 9 places repository operations in this role, which is why the history rewrite
is performed under it and not another.

**`qa-engineer` is not among them.** `LAW-05` bars this session from certifying its own repair.
Every claim below was produced by a measurement command, and the independent cold-context round
is **owed**, not supplied. `OI-V-17` records four consecutive sessions that each introduced a
defect of the class they were repairing; this session assumes it is the fifth candidate and
says so rather than asserting cleanliness.

## What made this session necessary

The owner commissioned an independent public-release review of the `DEC-21` finding. The review
measured `DEC-21`'s factual basis and found it wrong in both halves:

- the occurrence count was recorded as `41`; the measured figure is **52** in the run records,
  **53** including `DEC-21`'s own prose;
- the account name is **not** already in the commit history — every commit is authored under a
  GitHub noreply address;
- the repository is **not** public — `STATE.md` records an unclosed owner gate, and
  `ENGINEERING.md` §7 says otherwise, which is a separate defect.

`DEC-21` therefore preserved a disclosure on the belief that it had already happened and was
irreversible. It had not happened. The owner reopened it, chose sanitization, and set the stop
line at the publication gate.

## What was done, in order

| # | Act | Record |
|---|---|---|
| 1 | Governance inspection — `LAW-01/02/05/06/07/09/10/12`, `PRECEDENCE`, `V-21/V-22`, ECR/ledger/result rules | this file |
| 2 | Blast radius **measured**, not taken from the review | `HISTORY_REMAP.md` |
| 3 | Three-mechanism recovery point — bundle, mirror clone, raw `.git` copy — plus a second capture after the governance commit | `_SEWCP_PRE_SANITIZE_BACKUP_20260818/RECOVERY_POINT.txt`, untracked and outside the repository |
| 4 | Transform proved against all 11 affected blobs **before any git operation** | 53/53 replaced, 0 residual |
| 5 | Full dry-run rehearsal in an isolated repository, then verified | 24 commits, 0 identity mismatches |
| 6 | `ECR-D-018`, `ECR-Q-016`, `DECISIONS_S-2026-08-18-02`, `APR-039` committed **before** the act | `LAW-10` clause 4 |
| 7 | History rewritten in place | 25 commits, 53 occurrences, 11 blobs |
| 8 | Rewrite verified against the pre-rewrite reference | `R-032` |
| 9 | `R-031` superseded by `R-032`; four falsified current-state claims corrected | `R-032`, `HISTORY_REMAP.md` §5 |
| 10 | `LAW-09` close | `L-0000008` |

## What was measured

**Commits.** 54 total: **29 unchanged, 25 rewritten**. `0` author-identity mismatches, `0`
committer-identity mismatches, `0` author/committer timestamp mismatches, `0` commit-message
mismatches. Exactly **11 paths** carry any content difference.

**Occurrences.** 53 replaced — 52 path forms across ten run records, 1 quoted prose occurrence
in the `DEC-21` text. Zero remain in the publishable ref set (`refs/heads/*`, `refs/tags/*`),
scanned for the raw, upper-case, mixed-case and percent-encoded forms.

**Evidence integrity.** For all ten run records: JSON structure, key order, run id, timestamps,
exception type, exception message, traceback frames, line numbers, module names and Fusion
build identifiers all compare **equal**; reversing the substitution reproduces the original
bytes byte-for-byte.

**Provenance.** `d07e931`, `655aa75`, `be75798`, `8546960` — all four approval-provenance
commits precede the first affected commit and were **not** rewritten. Ten of eleven annotated
tags are unmoved. `v0.11.0` is re-pointed and remains `v0.11.0`: annotated, same tagger, same
timestamp, byte-identical annotation message.

**Digest constructions reproduced before use.** DC-3 against **both** normative worked examples
of `AIEF-AMD-008` §AMD-17 **and** against all seven live ledger entries and their chain and
`HEAD`. DC-1 via the single implementation the manifest declares — never a second copy, which
is the defect `ECR-D-005`/`ECR-D-006` record.

## What was overridden, and by whom

`LAW-07` clause 6 — *published history is never rewritten* — clause 7 — *tags are annotated and
never moved* — and clause 8. All three are rank 4; the owner's instruction is rank 1.
`APR-039` records the override, bound to `ECR-D-018` by DC-1 and filed **before** the act.

**No check was weakened, skipped, relaxed or deleted.** `V-22`, which restates clause 6, has no
executable implementation — `CMP-BLOCK-005` — and that is recorded rather than relied upon.
Clauses 1–5 of `LAW-07` are untouched: no attribution trailer, no identity modification.

## The check that fired, and was not silenced

Sanitizing `DECISIONS_S-2026-08-17-01.md` moved its DC-1, which `R-031` pinned. `X-06` went to
FAIL. That is the drift detector working. The architecture's remedy is one sentence at
`EXECUTION_ARCHITECTURE.md` §6.1 — **correction is by supersession, never by mutation** — so
`R-031` took its closing edit and nothing else, and `R-032` seals it at the DC-1 it now stands
at. `X-06` is PASS again because a record was published, not because a test was edited.

## Validation, against a baseline taken before anything changed

| | Before | After |
|---|---|---|
| `pytest tests/` | 895 passed | 895 passed |
| `aief_gate` · `aief_clearance` · `aief_params` · `aief_deliverables` · `aief_register` · `aief_approval` | exit 0 | exit 0 |
| `aief_exec check` | **7 of 10** — `X-08`, `X-09`, `X-10` FAIL | **7 of 10** — same three |
| `aief_analysis` | exit 1 **by design** (`ECR-D-016`) | exit 1 by design |

`X-08`, `X-09` and `X-10` were failing **before** this session and are neither caused nor cured
by it. The two detail lines this session adds to `X-09` are recorded at `R-032` `F-6`, with the
reason neither is repairable without breaking a passing check.

## `OI-C-10`, for the fifth session running

**No new open item was raised, and again not because none was found.** `ENGINEERING.md` §7
publishes `Repository | PUBLIC` while `STATE.md` — authoritative at B3 — records PRIVATE. It is
a real defect, it is **not** repaired here because it is outside the `ECR-D-018` write scope and
was not caused by this rewrite, and it was **denied an identifier by the 600-token cap**, not on
its merits. Recorded at `R-032` `F-3`. That is the fifth finding in five sessions.

## What this session did **not** do

It did not push. It did not force-update any remote ref. It did not merge. It did not create a
GitHub Release object. It did not change repository visibility. It did not open Fusion. It did
not touch `spec/**`, `.ai/core/**`, or any geometry, requirement, parameter or deliverable. It
did not edit `L-0000007` or any earlier ledger entry. **It did not certify itself.**

`origin/main` still stands at the pre-rewrite `f8ff028`, so the pre-rewrite objects remain
reachable in this clone through `refs/remotes/origin/*`. That is the truth about the remote, not
a residue, and it closes at the publication gate — which is the owner's.
