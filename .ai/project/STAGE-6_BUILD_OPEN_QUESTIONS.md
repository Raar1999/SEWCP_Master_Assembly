# Stage 6 Build — Open Questions for A4 Disposition

> **Instance artifact.** Partition `project`. Filed by `project-manager` action at rank-1 direction, session `S-2026-08-08-03`, per VER-006 FIND-Q6-2 and TCR-001 F2 — a single disposition subject for the implementation's undeclared details, which previously existed only in `src/aief_stage6/**` docstrings.
>
> Raised by `software.software-engineer · S-2026-08-08-07` during the CMP-BLOCK-004 build (LAW-12: flagged, not silently decided; deterministic interim choices are in force and disclosed in code). **None may be ruled by the session that raised it.** Each interim choice defines bytes of the first canonical `MANIFEST.lock`/distributable, so all five should be ruled before those bytes become the permanent integrity baseline.

> **ALL RULED — 2026-08-08.** Disposed by `chief-systems-engineer · S-2026-08-08-10`, a cold session differing from the raiser `software.software-engineer · S-2026-08-08-07` in **both role and session** (AMD-20 identity rule; the bar this file sets is satisfied and is confirmed in writing at [`AIEF-AMD-013`](../../framework/AIEF-AMD-013_Boot_Budget_Determination_and_Stage_6_Build_Constructions.md) § *Independence declaration*). Instrument §§AMD-42…AMD-48; approvals [`APR-012`](approvals/APR-012_Amend_Framework_Manifest_AMD-013.md), [`APR-013`](approvals/APR-013_Register_AIEF-AMD-013.md). Every ruling is a manifest change — **none was resolvable as an implementation-spec note**, because each defines octets of the first canonical `MANIFEST.lock` or distributable and so belongs in the single source of truth. **The questions and their interim choices are preserved verbatim below; nothing is removed.**

| # | Question | Interim choice in force (disclosed, deterministic) | Ruling |
|---|---|---|---|
| OQ-B1 | Lock JSON layout beyond the declared member order — indentation, key style, escaping | 2-space indent, LF, one terminal LF, `ensure_ascii` false | **AMD-43 — interim adopted and completed.** `core_aggregate.lock_json_layout`: 2-space indent per level, LF, exactly one terminal LF, no trailing whitespace, no space before and exactly one after the name separator, one member/element per line, **escaping only as RFC 8259 requires** (non-ASCII emitted as itself — the clause byte-identity turns on), emission order never sorted. Manifest change |
| OQ-B2 | `build_provenance` content — nowhere declared | Minimal deterministic set: source manifest DC-1, selected profile, stage, build id, run-fixed timestamp | **AMD-44 — interim adopted, closed and made explicit.** `build_provenance_record`: exactly six members in order — `source_manifest`, `source_manifest_dc1`, `selected_profile`, `compiler_stage`, `build_id`, `timestamp`. Host, user, working directory, tool/interpreter versions, per-execution wall-clock time and any environment capture are **prohibited**: each varies across the executions and platforms byte-identity requires to agree. Manifest change |
| OQ-B3 | Lock self-measurement fixed point — `files[manifest-lock]` carries token cap 200, but the budget record lives inside the lock; the lock's own count inside its own record is undefined | Lock row marked DEFERRED in the budget record; emitted lock text cap-checked post-serialisation, breach halts | **AMD-45 — interim adopted with one substantive correction.** `budget_measurement_record.lock_self_measurement`: row emitted `DEFERRED-SELF-MEASURED`, null counts, contributing nothing to totals; serialised octets cap-checked under both families post-serialisation, breach halts. **Correction:** the deferral is keyed on the path `core/MANIFEST.lock`, **never on absence from the tree** — closing the latent fail-open TCR-001 recorded as F1. Iteration to a fixed point and placeholder digits rejected with reasons. Manifest change; implementation delta owed as OI-C-09 |
| OQ-B4 | `distributable.contents` says "nothing else" while `determinism` declares a 0755 directory mode — tension | File-only tar entries; directory-mode rule treated as vacuous | **AMD-46 — interim adopted.** `distributable.entry_types`: regular file entries only — no directory, symlink, hardlink or device entry; extractors create directories from entry paths. `contents` governs; the 0755 clause is vacuous for a conforming archive and is **retained, not deleted**, so a future entry type does not inherit an undeclared mode. Manifest change |
| OQ-B5 | BINDING pin-line layout after the Stage 6 write (alignment, inline comment retention) | Value-token-only replacement | **AMD-47 — interim adopted and totalised.** `binding_pin_write`: replace the value token on the single `core_digest_pin` line, preserve **every other octet** including indentation, the whitespace run and the inline comment. Zero matching lines, multiple matching lines, or a non-hex64 candidate value each **halt the build**. Manifest change |

Also recorded by the SE, lower-order: DC-1 of a zero-length file is undefined by the line-wise wording (no covered or registered file is empty today); budget totals scope read as capped T0∪T1 files only (cap-null T1 files excluded) — confirm intent; AMD-33 byte-identity requires the timestamp/build-id be captured once per release run and shared across executions.

| Lower-order item | Ruling |
|---|---|
| DC-1 of a zero-length file | **AMD-48.** Content normalising to empty has the empty octet string as its preimage; DC-1 is `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. The terminal-LF step applies to surviving content, not to emptiness — otherwise an empty file and a one-blank-line file collapse to one digest. **No non-empty digest changes**, and the rule defines the digest without licensing the file (zero-dead-file rule stands). Manifest change |
| Budget totals scope | **AMD-42 — the interim reading is confirmed.** The measured set is exactly the non-null-`token_cap` T0∪T1 entries and clause 2's totals are the totals over that set: it is MI-4's own domain (Σ `token_cap` over T0∪T1) and the domain of `AIEF-FRZ-001` §1.8's Sum row of 5800 and Headroom row of 200. A cap-null entry declares no bound to measure against. **Residual raised, not glossed:** `project/ledger/HEAD` is read at boot step B4 yet is cap-null and therefore unmeasured — recorded as **OI-C-08**, expressly not cured because it draws on the same MI-4 headroom OQ-15 must allocate. Manifest change, plus a `validation[V-09].verifies` binding |
| AMD-33 timestamp / build-id capture | **AMD-48.** `build_time_reproducibility.run_fixed_values`: both are captured once per **release run**, before its first execution, and supplied unchanged to every execution — inputs, never outputs. A per-execution timestamp makes byte-identity unreachable by construction and turns AMD-33's halt into a permanent one, so this is the only reading under which AMD-33 is satisfiable at all. The release run, not the execution, is therefore the unit of provenance — which is why exactly these two values are admitted to `build_provenance`. Applies equally across V-10's platforms. Manifest change |

Authority: A4, a session distinct from `S-2026-08-08-07`. Instrument path: amendment or recorded ruling per LAW-01/LAW-10 where a frozen text is touched; several may be resolvable as implementation-spec notes without manifest change.

**Disposition of that authority note:** satisfied. The ruling session is `chief-systems-engineer · S-2026-08-08-10`. All eight items proved to require manifest changes rather than implementation-spec notes, and were taken through the LAW-01/LAW-10 path accordingly. This register is closed; its Closed row is in [`OPEN_ITEMS.md`](OPEN_ITEMS.md).
