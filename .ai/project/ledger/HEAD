# Ledger HEAD

> **Instance artifact.** Emitted by aief-compile Stage 3 from `framework.manifest.json` and live repository state.
> Partition `project` — never touched by framework upgrade. Owner `repository-engineer`. Mutability mutable.

---

Read at boot step B4. Constant-time head lookup without reading the ledger body.

```yaml
seq:         9
state:       active
entry_hash:  ff77fb26af8486542c866a363254a725f3eb2f222fef45868ecd109d20b047b6   # DC-3 of L-0000009
segment:     SEG-0000
prev_hash:   8a1efb98976e947af94c3cb39e92743597cbfc014f057abe57cc924a15b030e2
```

## B4 verification

1. `SEG-0000/L-0000009` exists and recomputes under DC-3 to `HEAD.entry_hash`.
2. No file at seq 10 exists - no orphan.
3. `STATE.last_ledger_seq` equals `HEAD.seq`. **Reconciled.**

Prior head `L-0000008`; its `entry_hash` is this entry's `prev_hash`.

## Genesis semantics

**AIEF-AMD-003 §AMD-10.** `state` is `genesis` or `active`. The transition ran once, at
`S-2026-08-12-01`, and is irreversible. Full rule: `L-0000001`.

## Write order at session close

Entry file -> flush -> `HEAD` -> `STATE.md` -> release lock. Segments seal every 500 entries.

> Kept short on purpose. This file is read at every boot and `files[ledger-head]` declares
> `token_cap: null`, so nothing in the boot budget bounds it — `OI-C-08`. Prose belongs in the
> entry files, which are T4.
