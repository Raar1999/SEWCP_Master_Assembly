# Open Items — Register

> **Instance artifact.** Emitted by aief-compile Stage 3 from `framework.manifest.json` and live repository state.
> Partition `project` — never touched by framework upgrade. Owner `project-manager`. Mutability mutable.
> **Tier T4**, `token_cap: null` — `files[open-items-register]`. Loaded on explicit request, never at boot.

---

**This register is authoritative.** [`OPEN_ITEMS.md`](OPEN_ITEMS.md) is its bounded T1 index and is the file read at boot step B7; `STATE.md` is a derived cache and `ENGINEERING.md` is an index. The split was enacted by [`AIEF-AMD-014`](../../framework/AIEF-AMD-014_OQ-15_Enactment_Bounded_Register_Split.md) §AMD-49 under the human owner's OQ-15 decision, option (a). **Every byte of authoritative content the pre-split register carried is carried here** — every row is reproduced verbatim, and where a row is updated the original wording is retained and the update is appended and attributed. Nothing was deleted, summarised or weakened.

**Index mapping — normative**, declared at `metadata.reproducible.bounded_register_split` and checked by `V-03`. Every identifier appearing as the leading cell of a row below appears **exactly once** in `OPEN_ITEMS.md`, under the section whose heading is identical to the heading above that row; and every identifier listed in `OPEN_ITEMS.md` appears **exactly once** as such a leading cell here. Identifier equality is the whole mapping — no ordering assumption, no editorial judgement.

**Role in which this register was edited.** The register edits of session `S-2026-08-08-12` were made by `chief-systems-engineer · S-2026-08-08-12` acting in `project-manager` role at live human-owner direction (`core/PRECEDENCE.md` rank 1), following the VER-004 and VER-006 filing practice. This disposes **VER-007 FIND-Q7-5** for this touch.

## Blocking

| ID | Item | Blocks | Authority |
|---|---|---|---|
| CMP-BLOCK-004 | aief-compile Stage 6 increment now **implemented and certified** (`src/aief_stage6/`, `S-2026-08-08-07/-08`; TCR-001 CERTIFIED 15/15; VER-006 C1–C10 PASS). Canonical execution remains held by CMP-BLOCK-006, the OQ-B1..B5 rulings ([`STAGE-6_BUILD_OPEN_QUESTIONS.md`](STAGE-6_BUILD_OPEN_QUESTIONS.md)) and OQ-14 authorization. Full six-stage compiler still absent (gates Release 1.0.0 per AMD-25). **Updated `S-2026-08-08-12` (`AIEF-AMD-014` §AMD-52):** CMP-BLOCK-006 is **disposed** and no longer holds canonical execution; the OQ-B1..B5 rulings are made; what remains holding it is **OI-C-09** (the `src/aief_stage6/**` delta) and **OQ-14** (human authorization) | Compiler Stage 6 execution, V-10 | `software.*` roles assigned 2026-08-08 (ROSTER, rank-1) |
| CMP-BLOCK-005 | Stage 6 slice **delivered**: dual-tokenizer capability with trust-on-first-use corroboration (TF-1/TF-2 pins MATCH upstream). Remaining: multi-platform (V-10 second platform; V-12 three platforms), concurrency/fault harnesses (V-13..V-17), reference projects (V-18/19/21) — campaign/Release 1.0.0 scope | V-10, V-12, V-15, V-18 campaign evidence | `software.*` roles assigned 2026-08-08 (ROSTER, rank-1) |
| C-4 | LICENSE is an unresolved placeholder | Public or external release | Repository owner |

## Open, not blocking

| ID | Item | Authority |
|---|---|---|
| ECR-D-006 | ***Moved from Blocking to Open-not-blocking by `S-2026-08-11-06`.*** **DISPOSITIONED A — re-register at the measured digest and re-affirm the change sets**, under owner-delegated engineering authority (`DECISIONS_S-2026-08-11-06` DEC-05; approvals [`approvals/APR-033`](approvals/APR-033_Reregister_Framework_Manifest_ECR-D-006.md), [`approvals/APR-034`](approvals/APR-034_Register_AIEF-AMD-014.md)) — **not a human approval**. **The audit found a second defect the ECR record did not carry**: the `AIEF-AMD-014` change set had been applied with **every record of its authority omitted** — `APR-014` and `APR-015` never filed, the manifest row never re-registered, and `AIEF-AMD-014` itself never added to the registry under the AMD-21 criterion its own § *Blast Radius* declares. `check_v24` named it; no human or agent had noticed in three sessions. Repaired: registry 29 → 30, DC-2 `19989657…8ddc07e`, `V-24` **PASS 30/30**, `aief_approval verify` **APPROVAL CHAINS OK**. Attribution rests on a git object — `git show 8546960:…` reproduces `ae16ccac…`, `APR-006`'s subject — with 27 changed leaves, zero removals, every leaf carrying its own authorising citation, and the counts matching each instrument's own enumeration (3 / 13 / 11). **Residual, recorded not closed:** the `AIEF-AMD-013` intermediate state `8af8971b…` is unreproducible and always will be — the bytes were never committed. **Closure is a `qa-engineer` act on independent verification, not a self-declaration**; `closed_at` remains null **— ORIGINAL ROW, RETAINED VERBATIM AND STRUCK, NOT DELETED (the register's own convention: where a row is updated the original wording is retained and the update is appended and attributed). It stood in § *Blocking* from `S-2026-08-08-12` to `S-2026-08-11-06`, and its *Blocks* cell read:* Re-affirmation of `AIEF-AMD-013`'s manifest change; Compiler Stage 6 execution (a build binds `build_provenance.source_manifest_dc1` to this artifact). **Does not block the CMP-BLOCK-006 disposition**, which turns on V-09 alone —** ~~**Freeze-registry divergence on `framework/framework.manifest.json` — the registered digest does not reproduce against the artifact.** Raised by `chief-systems-engineer · S-2026-08-08-12` under LAW-02 (class D: a defect stops the affected work) and LAW-12. **Evidence, dual-computed and reproducible.** Before this session's edits the working-tree manifest normalised under DC-1 to `e87ae68e87ba1da61f05115d4abbe931808f00c1cecc512168a578e5a12cf892`. `FROZEN.md`, `APR-012` `subject_hash` and `STATE.frozen_set_hash`'s membership all carry `8af8971b78d762e5db2879e50585a78f4e6d497ea707c664a9c06e1ba7e42ff7`. The two do not agree, so **V-24 was failing at the boot of this session** on exactly one registered path. **Method.** The pre-change bytes were reconstructed by inverting this session's ten manifest edits and were verified three ways: (i) the reconstruction differs from the current file by exactly those ten edits and nothing else; (ii) reverting the three `AIEF-AMD-012` changes and the thirteen `AIEF-AMD-013` changes from it yields the `HEAD` blob byte-for-byte, whose DC-1 is `ae16ccaca5746b81a2a992841fc1d239fd1c8b0c34657c05611e025a9d8395aa` — the value `VER-007` §5c records; (iii) **the control:** reverting only the `AIEF-AMD-013` changes yields DC-1 `f06125d2f9bd0860ab72c73f7dd11318d5d4f3169ded23b86f33e9c469707638`, **exactly** `APR-010` `subject_hash` and `APR-012` `prior_hash`, a value recorded four sessions before this one. The pipeline and the DC-1 implementation are therefore proven exact on this very file, and the divergence is **confined to the `AIEF-AMD-013` change set**. Nine alternative constructions of the recorded value were tested (raw octets, CRLF, absent/doubled terminal LF, BOM, UTF-16LE, two re-serialisations, whitespace variants); **none** reproduces `8af8971b…`, so this is a content divergence, not a construction disagreement. **Content is nevertheless the approved content**: the sixteen hunks between `HEAD` and the pre-change file map 1:1 onto `APR-010`'s and `APR-012`'s enumerations, with nothing riding along. **Consequences.** Under LAW-10 clause 2 an approval *"is invalidated automatically when the bound content hash changes"*, so `APR-012`'s binding to its subject is **void as recorded**; LAW-01's *"changed only by an approved ECR and a recorded human approval"* is therefore unsatisfied for whatever byte or bytes differ, and that difference is not recoverable — sessions `-05`…`-12` are uncommitted (OI-V-08 / VER-007 FIND-Q7-8), so no prior state of this file exists in git. `VER-007` C2/C3 asserted 29 of 29 verify; that assertion does not reproduce today for this one path and does reproduce for the other 28. **Repair performed here, and its limit.** `AIEF-AMD-014` re-registers the manifest at its measured post-change digest, which restores V-24; `APR-014` records `prior_hash` as the **measured** `e87ae68e…`, never the unreproducible recorded value. Re-registration is the ECR-D-005 disposition-A precedent and it repairs the *registry*; it does not retroactively supply the missing approval for the `AIEF-AMD-013` bytes. **That re-affirmation is a human-owner act and is what this ECR seeks.** Root cause is OI-V-02: no standing check binds `FROZEN.md` to the tree, which is precisely what ECR-D-005 recorded and what left this undetected for two sessions | Dispositioned under delegation; closure awaits `OI-V-13` |
| ECR-D-014 | **`files[manifest-lock].token_cap` 200 is unsatisfiable by any conforming lock — DISPOSITIONED A.** Raised at the **first authorised Stage 6 build**, which halted on `core/MANIFEST.lock: governing 6469 > cap 200`. Two clauses of the same frozen artifact cannot both hold: `lock_self_measurement` (AMD-45) measures the whole serialised document, while `lock_serialisation` (AMD-27) says *"aggregate_digest precedes files **so the T1 digest read stays within the 200-token cap**"* — a statement that the cap bounds a **prefix**, and the only reading under which member ordering can bear on the count. Measured dual-family, not marginal: whole lock 6469 TF-2 / 5334 TF-1; `files` alone 5198 (75 schema-required path-digest pairs); **schema-required members with `files` removed still 285**. Content reduction determinately insufficient; a raise to 6469 puts MI-4 at 12173 against 6000 (`AIEF-AMD-013` §AMD-41 §5(iii)). Disposed by [`AIEF-AMD-015`](../../framework/AIEF-AMD-015_Lock_Boot_Read_Prefix_And_Member_Order.md) §§AMD-54/55: the measured quantity is the **boot-read prefix**, and `aggregate_digest` moves to second position so that prefix contains no run-scoped octet — **69 TF-2 against 200**, and **MI-4 unchanged at 5904 of 6000, no `token_cap` altered**. Approvals `APR-035`/`APR-036`; record [`ecr/ECR-D-014`](ecr/ECR-D-014_Lock_Token_Cap_Unsatisfiable_By_Any_Conforming_Lock.md). **Undetected until now for three reasons**, each recorded there: the certification harness injects a stub tokenizer probe; the only real-tokenizer path halted at `PRECONDITION-FAIL` while `V-24`/`V-25` were failing; and `TCR-001` certified that the code implements the declared constructions — which it does — while nothing compared the declared cap against the artifact those constructions produce | Dispositioned under delegation; closure awaits `OI-V-13` |
| ECR-Q-013 | **`spec/01` §2.1's wetted-area row is stale after `ECR-D-002`.** It states 0.080 m² for the 2.20 m path, which requires a perimeter of **36.4 mm**; the 10.0 × 6.0 channel has a perimeter of **32.0 mm**. The row's own parenthetical *"(perimeter 36 → 32 mm)"* shows the change was known and the product was not recomputed — `ECR-D-002`'s disposition enumerated *"all nine derived values recomputed"* and this was a tenth. `DECISIONS_S-2026-08-11-05` DEC-04 inherited it, reaching ≈ 0.060 m² by scaling the stale figure by the length ratio. Correct as-routed value **0.0525 m²**; film ΔT at 3 kW **8.79 K**, 14 % above DEC-04 and 51 % above what §2.1 implies. **Deliberately not applied to `spec/**`**: §2.1 is a design-basis table, the governing acceptance criteria are `CP-01` and `CP-11` (both physical), neither takes its value from §2.1, and correcting a derived cell would move a frozen digest and re-open an approval chain while changing no acceptance criterion. `ECR-Q-010` precedent — state the governing reading, resolve at the next baseline correction. Full derivation: [`verification/PVR-001`](verification/PVR-001_Physical_Verification_Record_And_Test_Matrix.md) §4.3 | Nothing today — no acceptance criterion moves | documentation-engineer + design-engineer |
| OI-C-14 | **The whole-document size of `core/MANIFEST.lock` is bounded by nothing.** Consequence of `AIEF-AMD-015` §AMD-54, raised there rather than hidden: the 200-token cap now bounds the boot-read prefix, so the document itself — 6469 TF-2 today, growing with the covered set — has no declared bound. No invariant ranges over it and nothing is violated; but *nothing is violated* is not *something is checked*, which is the `OI-V-02` lesson. **Expressly not cured** by inventing a second undeclared cap while disposing a defect about an undeclared measured quantity | Nothing — the lock is not boot-loaded context | A4 + human owner, later session |
| OI-C-15 | **Two desk-dischargeable verification items are not discharged, and no hardware blocks them.** (i) `SR-07` (*7.5 kg stack + 5 g, SF ≥ 3*) and `AP-08` (*5 g on the 7.5 kg stack = 123 N per pin*) are declared **Analysis**, and the analyses of record were run at **7.5 kg** against an as-modelled assembly of **7.6997 kg** — per-pin lateral load 122.62 → **125.89 N, +2.66 %**, absorbed by `ECR-D-007`'s recorded 4.4–6.9× margin but **not re-run at the mass the design now has**, and higher still once the `spec-only` BOM lines are added. (ii) `SR-03`, `SR-04`, `RF-09`, `RF-10`, `RF-11` are declared **Drawing**/**Drawing verification** and the drawing set exists, but **no creepage/clearance path trace against those drawings has been filed** — `aief_clearance` checks `spec/00` §3.2 feature clearance, a different property from a dielectric creepage path over a solid surface. Both are actionable now, without hardware. Raised by [`verification/PVR-001`](verification/PVR-001_Physical_Verification_Record_And_Test_Matrix.md) §6 | Nothing today; both are owed before either analysis is offered as verification | mechanical.design-engineer + mechanical.simulation-engineer |
| OI-V-13 | **Independent cold-context QA audit of `S-2026-08-11-06` — owed, not performed, and this session cannot perform it.** The session ruled `ECR-D-006` and `ECR-D-014`, authored `AIEF-AMD-015`, applied the manifest changes, implemented them in `src/aief_stage6/**` and `src/aief_approval/**`, wrote the tests, and executed Stage 6. That is a **wider** SOD-1 departure than `AIEF-AMD-013`'s or `AIEF-AMD-014`'s, both of which expressly declined to implement what they ruled. LAW-05 bars self-verification and LAW-02 clause 5 bars closure by the raiser — **`ECR-D-014` was raised by this session, so clause 5 is not satisfied on its own terms** and is recorded rather than finessed. The audit must independently re-measure the six `AIEF-AMD-015` §AMD-54 quantities under both families, re-derive the §AMD-55 prefix, re-attack the four §AMD-54 tests and the five multi-subject approval tests, and re-verify the `ECR-D-006` attribution from `8546960` forward. **Until it is filed, `ECR-D-006` and `ECR-D-014` are dispositioned and applied but not closed** | Closure of `ECR-D-006` and `ECR-D-014`; nothing else | qa-engineer, distinct session |
| ECR-D-012 | **The C6 verification-supersession relation was computed but undeclared, and bound to nothing — DISPOSITIONED A, ruled by the human owner `S-2026-08-10-04`.** Raised on `VER-016` F-01 and F-12, which named the remedy in terms: *"an instrument change to `C6` (an ECR and an approval)"*. `criteria.py` had enforced a supersession relation and a verdict rule that [`GATES.md`](GATES.md) — the authority the framework delegates the criteria to — did not state, and the relation was assembled globally with no proof that a superseding verifier had read what it retired. **Disposition A: declare it in `GATES.md`, seal it to the predecessor's DC-1, scope it per gated ECR, guard fork and self-reference, fail closed with no fallback; and make `status` a closed `CLEARED`/`NOT CLEARED` vocabulary.** Bound by [`APR-028`](approvals/APR-028_Verification_Supersession_Relation.md); attacked by `tests/test_verification_chain.py` (29 trials) and independently re-attacked at `VER-017` W1 (9 trials, 9 required failures). **Closed on `C6`** — `python -m aief_gate` exits 0. Not under `spec/**`, so outside `C5` and `C7` | LC-M04-EXIT C6 — now satisfied | Design Authority |
| OI-C-13 | **`APR-001`…`APR-013` carry no `ecr:` field**, so `ECR-D-005`, `ECR-Q-001`, `ECR-Q-002` and `ECR-Q-003` resolve to an approval that does not name them back. `VER-016` F-08, re-confirmed at `VER-017` W8: all fifteen ECR `approval:` paths now **resolve**, and exactly four fail the back-reference half. This is a **convention boundary, not a defect in those approvals** — the field enters at `APR-016`. **Deliberately not repaired**: closing it means rewriting thirteen records of human decisions to carry metadata their approver never wrote, which is not something an agent should do silently. The lawful remedies are a human-owner re-affirmation of the thirteen, or a declared rule that the back-reference is one-directional before `APR-016` | Nothing — none is under `spec/**` and none bears on a gated ECR | chief-systems-engineer + human owner |
| ECR-D-001 | **Alignment pin interface: two mutually exclusive geometries — DISPOSITIONED A, SEWCP-700 governs.** Independently verified across four cold rounds at `VER-014`. The recorded `spec/03` residual (an interface directing a press fit the governing volume forbids absolutely) is **closed** under `APR-024`. **Awaits the confirmatory C6 round (`VER-015`, discharging OI-V-11)** | LC-M04-EXIT C1/C6 | Design Authority |
| ECR-D-002 | **Channel cross-section did not close: 8+8+6=22 against 20.000 — DISPOSITIONED A, depth 8.00 → 6.00.** The disposition was **incompletely applied**: `spec/01` §6 step 3 still directed *"10 W × 8 D"*, the machining instruction, which would have re-cut the error. Completed here, together with the derived values that had survived in `spec/00` §4.3, `spec/README`, `spec/06` and `spec/07`. **Residual `CP-02` ΔP remains open — flow bench, no value asserted** | LC-M04-EXIT C2/C6 | Design Authority |
| ECR-D-003 | **Coolant stub interface undimensioned — DISPOSITIONED A.** Ø10.0 H9 bore, centreline 11.00 above Datum A, channel locally deepened to 10.00 so bore and channel are coaxial; SEWCP-201 bimetallic transition joint carries the ½ in. VCR gland, because 6061 cannot be fusion-welded to 316L and the specified orbital weld was not executable by any process. Applied to `spec/01`, registered, approval `APR-020`. **Awaits C6 verification (`VER-015`); residual `CP-02` ΔP shared with ECR-D-002** | LC-M04-EXIT C3/C6 | Design Authority |
| ECR-D-004 | **Choke counterbore undimensioned and M5 × 30 unbuildable — DISPOSITIONED A.** `CP-D26` 11.0 W × 12.5 L × 2.5 deep, radially slotted and anodize-masked; **M5 × 25**; `HP-D12` blind 6.50 with a 1×D insert leaving 1.50 mm to the ESC bond face. M5 × 30 engaged 7.90 mm of an 8.000 mm plate at *any* counterbore depth. `CP-D21`↔`HP-D12` closed reference loop broken. Applied to `spec/00`, `spec/01`, `spec/02`; approvals `APR-020`/`-021`/`-022`. **Awaits C6 (`VER-015`)** | LC-M04-EXIT C4/C6 | Design Authority |
| ECR-D-007 | **Geometric consequences of the locator counterbore — DISPOSITIONED A on all five actions.** §3.1 keep-out row added (8.5 mm radius / 3.5 mm wall); the 3.35 mm wall **rejected and answered** rather than excepted; `HP-D09a` slot depth 3.00 ± 0.10; `CP-D09a`/`CP-D10a` Ø3.30 drilled 7.80 max, 5.00 min full thread. **Action 3 dispositioned by reducing the flange Ø12.000 → Ø10.000**, wall 1.00 → 2.00 mm — rejected on *machinability* (13–26 µm of ligament deflection against an 18 µm H7 band), not on stress, which computes to a 4.4–6.9× margin | LC-M04-EXIT C7 | Design Authority |
| ECR-D-008 | **Alignment pin material specified two ways — DISPOSITIONED A, `spec/06` governs.** The ICD cell read *"316L (metal/metal); Al₂O₃ (ceramic IF)"* and `spec/06` §6 rejects **both** on quantified grounds. The **whole cell** was replaced with `Ti-6Al-4V Grade 5`: the parenthetical mandated two part numbers against §3.1's *"All six are the same part number"*, so substituting the metal alone would have frozen that contradiction. Zero cascade — every derived value was already computed against Ti-6Al-4V | LC-M04-EXIT C7 | Design Authority |
| ECR-D-009 | **The retaining screw head had no seat — DISPOSITIONED A, integral shouldered locator screw.** No standard M4 head fits a Ø6.000 boss (smallest is ISO 4762 at Ø7.00; M3 leaves a 0.1 mm rim), so the two-piece architecture itself failed. `AP-D07`/`AP-D08` struck; `AP-D13` spigot and `AP-D14` 3.0 mm hex socket added; `AP-D03` k6 → h6. **Torque 2.5 → 1.2 N·m** — 2.5 N·m put an M4 A4-70 at 114 % of yield, so it was wrong for the old screw too | LC-M04-EXIT C7 | Design Authority |
| ECR-D-010 | **Top locators shared three rays with three outer choke stations — DISPOSITIONED A.** Counterbore r 124–136 against the M5 slot r 131.5–138.5 and the Ø22 pad r 124–146, all at 30°/150°/270°. Re-clocked to **75°/195°/315°**, the maximum separation a 120°-spaced triad can achieve against a 30° pattern. Root cause: `spec/00` §3.2 omitted the Ø260 BC row entirely and closed with an unverified *"No conflicts"*. Now computed by `python -m aief_clearance`, with `tests/test_clearance.py` restoring the old clocking and requiring failure | LC-M04-EXIT C7 | Design Authority |
| ECR-D-011 | **Kinematic slots sever the outer heater spiral — DISPOSITIONED A.** Slot r 126–134 against turn centres at r 126 and 132, same face, 3.00 vs 3.20 deep. Shortening fails (6.87 mm needed against 6.00 pitch) and relocating fails (6.00 mm annulus against a 6.05 slot). `spec/02` §3.2 declares a ≥12 × ≥10 mm keep-out with the spiral routed tangentially. **Residual: `HP-08` re-verified by thermal map against the as-routed spiral — a measurement, not a waiver** | LC-M04-EXIT C7 | Design Authority |
| ECR-Q-009 | **Bushing bore stated two ways — CORRECTED.** `spec/00` §10 A2 read Ø5.05 H7 against **Ø5.60 +0.05/−0** corroborated six times (`spec/01` CP-IF-6 and §10 step 4; `spec/05` LP-IF-3, §7 table, `LB-D01`, §10 step 3). Controlling reference applied under `APR-021`. Found by the ECR-D-008 coherence sweep, unrecorded before it | Nothing — corrected | documentation-engineer |
| ECR-Q-010 | **The RF land is dimensioned two ways.** `CP-IF-8` gives *60 mm circumferential* at mean r = 137, which subtends **24.5°**; `spec/01` §3.1 declares the keep-out envelope as **93°–117°**, i.e. 24.0°. They differ by 0.55°, about 1.3 mm of arc. The clearance check uses the **declared envelope**, which is what governs CAD, and the choke station at 120° clears it by 1.58 mm. Not blocking; resolve at the next baseline correction | Nothing — clearance holds under the governing reading | documentation-engineer |
| ECR-Q-011 | (DISPOSITIONED, `S-2026-08-11-05`, DEC-03 — rib 5.0 + tool envelope, APR-029/031) **FSW internal-rib passes: no stated minimum rib width for tool shoulder engagement.** Raised exactly as `MDR-001` §5 prescribed — a manufacturing input the record does not have, raised rather than assumed. `SEWCP-200-REQ-002` routes the channel at a **5.0 mm design rib** (the MDR's own illustrative figure); if the FSW supplier's minimum exceeds it, the S3 pass schedule re-routes deterministically at the larger pitch and the path-length shortfall against §2.1 deepens. Record: [`ecr/ECR-Q-011`](ecr/ECR-Q-011_FSW_Minimum_Rib_For_Tool_Shoulder_Engagement.md) | Nothing today — build release requires the answer | mechanical.manufacturing-engineer |
| ECR-D-013 | (DISPOSITIONED A, `S-2026-08-11-05`, DEC-01 — strap holes tap-coincident, APR-030/032) **RF strap terminal hole pitch (25.0) cannot close against the plate tap clocking (98.73°/111.27°, chord 29.94).** Found at assembly integration placing the verified SEWCP-901 against the verified SEWCP-200; both parts conform to their own frozen volumes, which disagree by 4.94 mm — Ø6.6 over M6 absorbs ±0.3. Owner ruling required (spec/** change either way); raising agent recommends re-pitching RS-D07 to the tap chord. Record: [`ecr/ECR-D-013`](ecr/ECR-D-013_RF_Strap_Hole_Pitch_Versus_Plate_Tap_Clocking.md) | **LC-M04-EXIT C7** (an undispositioned ECR-D against spec/** fails the criterion by construction — the gate honestly reads NO until ruled) + RF joint hardware build | human-owner |
| ECR-Q-012 | (DISPOSITIONED, `S-2026-08-11-05`, DEC-02+addendum — plate-hung hanger built and integrated, APR-029/031/032) **SEWCP-902 saddle mounting architecture underdefined and internally contradicted.** SB-D01's derivation (7.5-high, base-seated) violates SB-D04 (≥8.0 to Base Plate) and RF-IF-3 (“mounts to the RF-hot plate, not to ground” — a grounded aluminium saddle under the strap is a short); the plate-hung form has no dimensions and names CP taps no package realises; the two packages also disagree 0.25 on the strap seat plane. Master assembly places the saddle at its own package nominal, marked PROVISIONAL. Record: [`ecr/ECR-Q-012`](ecr/ECR-Q-012_Saddle_Mounting_Architecture_Underdefined.md) | Bracket hardware build only | human-owner |
| OI-CAD-03 | **Four quarantined lineages await owner disposition** (all HISTORICAL/PRESERVE — superseded verified baselines, none referenced by the assembly): `ZZ-DERIVATIVE-STUCK_SEWCP-200-20260811` (broken cloud derivative, holds CP v1..v7 history), `ZZ-SUPERSEDED-FREE-S_SEWCP-901-20260811` (geometry-phase strap), `ZZ-SUPERSEDED-HOLES25_SEWCP-901-20260811` (pre-ECR-D-013 strap), `ZZ-SUPERSEDED-BLOCK_SEWCP-902-20260811` (pre-ECR-Q-012 saddle). Deletable via the guarded op on the owner's word. Failure-artifact ZZ designs were deleted `S-2026-08-11-04`; the lifecycle fix prevents new ones | Nothing — census is 11 authoritative + these 4 **UPDATED `S-2026-08-11-06` — DISPOSITIONED: ALL FOUR PRESERVE, NONE DELETED** (`DECISIONS_S-2026-08-11-06` DEC-08; full census, per-lineage analysis and the guarded deletion procedure at [`cad/LINEAGE_CENSUS.md`](../../cad/LINEAGE_CENSUS.md), roster evidence distilled into the tracked [`cad/runs/LINEAGE_ROSTER_S-2026-08-11-06.json`](../../cad/runs/LINEAGE_ROSTER_S-2026-08-11-06.json) because `cad/bridge/**` is gitignored and a clone would not carry the citation). **Roster confirmed by observation: 15 = 11 authoritative + 4 quarantined**, matching this row's count. **Reference scan over every `.md`/`.json`/`.csv`/`.py` in the repository and every `.json`/`.csv` in the external deliverable root: not one quarantined lineage is referenced by the assembly, the assembly run record, `FINAL_SYSTEM_VERIFICATION`, `SYSTEM_INTERFACES`, the BOM or any deliverable.** Every surviving reference is narrative and survives deletion of the document. **`ZZ-DERIVATIVE-STUCK_SEWCP-200` is at v7 - the only lineage above v2 in the project - and holds the entire modelling history of the Cooling Plate; the re-homed replacement is at v2 and carries the verified content with NONE of the history. Deleting it destroys required engineering history irrecoverably, and the same is true of the free-S strap.** The holes25 strap and the block saddle are recorded in full at `ECR-D-013` and `ECR-Q-012`, so deletion of those two is *defensible* and is still not recommended. **Remains OPEN, OWNER-RESERVED and NON-BLOCKING**: it blocks no gate, no deliverable, no verification and no release | human-owner |
| OI-V-12 | **The keep-out row adopted for the kinematic locators is conservative, and a cheaper construction exists.** `spec/01` §3.1 now applies a **radial** keep-out of 8.5 mm to the Ø10.000 counterbore. A depth-aware construction is defensible and less costly: the counterbore is blind, 3.00 deep, and stops 5.00 mm (4.70 worst case) above the channel roof, so an **axial** wall row would control it and only the M4 thread would need a radial row — recovering roughly 1 % of serpentine path length. Adopted the conservative form because it is what was ruled; recorded so the next authority meets arithmetic rather than a surprise | Nothing — the adopted row is satisfiable and safe | mechanical.design-engineer |
| OI-C-11 | **ECR-Q identifier namespace collision.** `ECR-Q-001…003` under `ecr/` are framework queries; `ECR-Q-001…008` in `implementation/01_…/SEWCP-200_CAD_Implementation_Package.md` §12.2 are *different* specification queries with the same identifiers. `ECR-Q-009` and `ECR-Q-010` were allocated above the high-water mark of both series to avoid deepening it. Needs one namespace or a prefix | Nothing today; a citation of "ECR-Q-002" is ambiguous | documentation-engineer |
| OI-P-03 | **The SEWCP-200 CAD implementation package is Rev X1, written against the pre-disposition baseline.** Its §3 parameter master carries `ch_depth = 8.0`, `lid_check` is described as evaluating to −2.0 when it now evaluates to 0.0, HOLD H1 and H2 still quarantine ~82 % of the part against defects that are now dispositioned, and §12.1's geometric note feeds an 8.00 mm channel height into ECR-D-003 reasoning. **Corrected across three sessions and now usable for CAD**; a full Rev X2 re-issue with a supersession marker is the proper remedy. **The `S-2026-08-10-01` claim that it was "corrected in this session" was false** — `VER-016` W10 found `ang_kin_top_*`, timeline step 11, sketch S11 and step 6.34 still carrying the superseded 30/150/270 clocking, and `choke_cbore_*` and the stub parameters still reading `UNSPECIFIED`. Those were repaired at `5e7ac74`. `S-2026-08-10-04` then found and repaired what `VER-016` had not looked for: **steps 6.08, 6.27 and 6.32 still instructed the modeller to insert *suppressed placeholder groups*** in place of the coolant circuit, the sixteen choke counterbores and the six kinematic locators, all three holds having been discharged — so §6 followed in order built **no** geometry rather than wrong geometry, silently, because suppressing a feature is a legal Fusion operation. Stale `HOLD` tags cleared from `CP-IF-1`, `CP-IF-4`, `CP-IF-10`, §3.2 and `ang_coolant_*`; §12/§13 keep theirs as the record of the defects as raised. `params/generated/SEWCP-200.csv` now exists, derived from §3 under `aief_params check`. Confirmed at `VER-017` W5 and W12 | CAD efficiency, not CAD correctness | mechanical.cad-engineer |
| OI-V-02 | No **standing** check binds `FROZEN.md` to the working tree. `V-24` is **declared** (AIEF-AMD-008 §AMD-19) and **emitted** (Compiler Stage 5, `S-2026-08-08-03`, `core/validation/`) but **not implemented as software** — execution requires CMP-BLOCK-004/-005 infrastructure. The registry is still verified only by hand. This is the root cause of ECR-D-005 and of FIND-4 | qa-engineer + A4 |
| OI-V-03 | **Everything session `S-2026-08-08-02` produced is unverified.** AIEF-AMD-008, the manifest amendment, APR-002, APR-003, the recomputed aggregate, the eight registry additions, the `BINDING.md` repair, the ECR dispositions and the VER-001 filing all post-date the VER-001 audit and are outside its scope. LAW-05 bars self-verification. Needs an independent cold-context audit | qa-engineer |
| OI-R-01 | **No `v0.2.0` tag exists**, though `a403059` is "Release 0.2". Tags jump `v0.1.0` → `v0.3.0` and `releases/TAGS.md` does not mention it. Not a LAW-07 breach — LAW-07 governs tag form, not coverage. **No tag is to be created retroactively and no history rewritten** without a human ruling; the gap is recorded so it is not mistaken for a lost release. FIND-8 | repository-engineer |
| OI-C-01 | `SCH-ledger-entry.schema.json` declares `additionalProperties: true`. DC-3 (AMD-17) covers only the seven declared fields, so any additional field an entry carries is **not protected by the chain**. Tightening to `false` removes the exposure and is a Stage 1 re-emission | chief-systems-engineer |
| OI-C-02 | `adapters/ADP-ci.md` is **stale**: it states *"All 22 checks are BLOCKING"* and lists phase ranges `V-01 .. V-10` / `V-18 .. V-21` that omit `V-23`, `V-24`, `V-25`. Requires a Stage 4 re-emission. The `adapters` partition is human-only write access and was deliberately not hand-edited. **Required content now fully determined** by AIEF-AMD-010 §AMD-32 (consequence-recording only): 25 checks, all BLOCKING; compile-time range `V-01…V-10` plus `V-23…V-25`; AMD-31 precondition semantics. The re-emission itself remains a Stage 4 action and was not performed | repository-engineer |
| OI-C-05 | **`referenced_by` completeness is undeclared.** MI-3 tests resolution, not completeness (AIEF-AMD-009 §AMD-24): `BOOT.md` is cited by `adp-claude-code`, `adp-chatgpt`, `adp-generic-llm`, `readme` and `core/CONTEXT_TIERS.md`, yet `boot.referenced_by` is empty and nothing makes that a defect. The three adapters' `depends_on: ["adapters-index", "boot"]` edges may themselves be citations miscoded as build order — the FIND-1 class, forward-directed and therefore V-23-invisible. Observed while ruling AMD-24; outside its directed scope; recorded so it is not lost | A4 ruling, later session |
| OI-C-04 | Nine `implementation/**/README.md` files carry a UTF-8 **byte-order mark**. They lie outside `.ai/` and are not registered, so `V-25` as scoped does not reach them, and they were **not** repaired — `implementation/` is PR-controlled. Found while validating V-25; recorded so the observation is not lost | documentation-engineer |
| OI-V-05 | **VER-003 residuals** ([`verification/VER-003`](verification/VER-003_Independent_Verification_Stage_5_Emission.md), Stage 5 emission audit: 8 PASS / 1 FAIL on C7, verdict VERIFIED WITH FINDINGS). FIND-Q3-1 (BLOCKING) **dispositioned**: `.gitignore:34` `MANIFEST` rule hid `core/validation/MANIFEST` from git; negation `!.ai/core/validation/MANIFEST` added and verified stageable. FIND-Q3-2 (MAJOR) **dispositioned** by the VER-003 filing itself (the pre-recorded VER-003 references are true from filing forward). FIND-Q3-3 folds into OI-C-02 (`ADP-ci` stale — now materially inconsistent with the emitted MANIFEST it reads). FIND-Q3-4 (INFO) **open**: `core.autocrlf=true` with no `.gitattributes` — a future checkout may rewrite working-tree files to CRLF and break the literal V-25 working-tree condition; a `.gitattributes` declaring LF for `.ai/**` is a `repository-engineer` action | repository-engineer |
| OI-P-01 | **Session-record gaps.** `project/sessions/` is empty: session `S-2026-08-08-01` filed no session summary, so its role assignments are unrecoverable and its ECRs carry `role-unrecorded`. That session performed no LAW-09 close and its work is permanently unlogged — a close cannot be performed retroactively by a later session. The VER-001 audit declared no session identifier and is unnumbered. No `.session.lock` exists (DEV-02) | project-manager |
| OI-V-04 | **VER-002 residual: FIND-Q2-1 only.** `AIEF-FRZ-001` Part 4 Stage 1 still carries the superseded barrier wording with no in-document errata pointer (A4, next amendment). FIND-Q2-2 and FIND-Q2-3 are **dispositioned** by the human-owner's commit-granularity ruling of `S-2026-08-08-03`, executed by `repository-engineer`: commits `d07e931` (AMD-008 state) and `655aa75` (AMD-009 state) make every approval subject recoverable as a git object — `636cf22b…` at `d07e931`, `9611d547…` and `86c8be7f…` at `655aa75` — and place all three sessions' registered work in git history. Report at [`verification/VER-002`](verification/VER-002_Independent_Verification_AIEF-AMD-009.md); filed verbatim by the audited party, VER-001 precedent | A4 |
| OI-P-02 | `ROSTER.md` marks `qa-engineer`, `project-manager` and `chief-systems-engineer` **UNASSIGNED**, and states *"A role marked UNASSIGNED cannot be dispatched."* Three such roles have been dispatched. The dispatches are authorised at `core/PRECEDENCE.md` rank 1 by live human instruction, which outranks the roster — but the roster record is stale and reviewer-independence cannot be checked against identities that are not recorded. Assignment is a project-manager action and was **not** self-assigned. **Narrowed `S-2026-08-08-12` (`AIEF-AMD-014` §AMD-52, disposing VER-007 FIND-Q7-6):** the three `software.*` rows are **not** part of this item — `ROSTER.md` § *Profile `software`* assigns all three to a named identity with workstreams. Only the three universal rows above remain UNASSIGNED | project-manager |
| OI-V-09 | **VER-006 residuals** ([`verification/VER-006`](verification/VER-006_Independent_Verification_Stage_6_Preparation_Phase.md), Stage-6 preparation phase audit: 10 criteria, 10 PASS, VERIFIED WITH FINDINGS). FIND-Q6-1 (MINOR) **dispositioned** at the VER-006 filing: CMP-BLOCK-006 registered, CMP-BLOCK-004/-005 rows and STATE synced. FIND-Q6-2 (INFO) **dispositioned**: the five build Open Questions filed as [`STAGE-6_BUILD_OPEN_QUESTIONS.md`](STAGE-6_BUILD_OPEN_QUESTIONS.md) — A4 ruling pending (OQ-B1..B5); **ruled `S-2026-08-08-10` at `AIEF-AMD-013` §§AMD-42…AMD-48**. FIND-Q6-3 (INFO) open: provenance note in gitignored `preconditions.json` cosmetic. FIND-Q6-4 (INFO) open by design: real-family AMD-33 emission evidence exists only after CMP-BLOCK-006 is disposed — **CMP-BLOCK-006 is now disposed (`AIEF-AMD-014` §AMD-52); the evidence still awaits an authorised build, OQ-14** | A4 / project-manager |
| OI-V-10 | **VER-007 residuals** ([`verification/VER-007`](verification/VER-007_Independent_Verification_AIEF-AMD-013_and_CMP-BLOCK-006.md), independent cold-context audit of `S-2026-08-08-10` — the SOD-1 mitigating control, including the required independent recomputation of §AMD-41: 10 criteria, **7 PASS / 3 PARTIAL / 0 FAIL**, verdict VERIFIED WITH FINDINGS; 29/29 registry, all six DC-2 lineage values, both approval subjects and every AMD-41 arithmetic sub-claim reproduce to the digit under dual-computed tooling). **FIND-Q7-1** (MINOR): the CMP-BLOCK-006 row's own figures were asserted as *"today"* but were superseded within the session that wrote them — `STATE.md` is 1083 (PASS, not a breach), `OPEN_ITEMS.md` 12000, aggregate 16012 / 12868 per `APR-012`; `project-manager` to restate or point at `APR-012`. **DISPOSED `S-2026-08-08-12`** (`AIEF-AMD-014` §AMD-52): every measurement in this register now carries an explicit instant and the CMP-BLOCK-006 row records the full measurement history. **FIND-Q7-2** (MINOR) **open**: AMD-48's stated ground for the empty-content rule is false — DC-1's *remove trailing blank lines* step already collapses an empty file and a one-blank-line file under either reading; the ruling's value is correct, the justification is not; A4, next amendment. **Expressly not corrected by `AIEF-AMD-014`**, whose authorising instruction is confined to the OQ-15 enactment and whose approval admits nothing riding along. **FIND-Q7-3** (MINOR): AMD-45's lock deferral leaves up to 200 tokens of the measured domain outside the 6000 ceiling test — the exact analogue of OI-C-08, unrecorded; A4, with OQ-15. **DISPOSED `S-2026-08-08-12`** (`AIEF-AMD-014` §AMD-51): the lock's `token_cap` is charged to the per-family totals whenever its row is `DEFERRED-SELF-MEASURED`, so the ceiling test can no longer under-count. **FIND-Q7-4** (MINOR) **open**: `APR-012`'s "seven new members" parenthetical contradicts its own nine-*New* enumeration; no approval voided. **FIND-Q7-5** (MINOR): the A4 session edited this `project-manager`-owned register without recording the role. **DISPOSED `S-2026-08-08-12`**: the role is recorded at the head of this register and of the index. **FIND-Q7-6** (MINOR): `ENGINEERING.md` §7 and the OQ-13 Closed row say the software roster rows are UNASSIGNED while `ROSTER.md` assigns all three and §8 says so too; §6's Stage 6 row omits CMP-BLOCK-006. **DISPOSED `S-2026-08-08-12`** (`AIEF-AMD-014` §AMD-52): §6 and §7 synced, the OQ-13 Closed row and OI-P-02 corrected. **FIND-Q7-7** (INFO): the `STATE.md` provenance header diverged from its five siblings' emitted form (lossless). **DISPOSED `S-2026-08-08-12`**: the sibling header form is restored, affordable inside the split. **FIND-Q7-8** (INFO) **open**: the pre-session baseline is unrecoverable, so three AMD-41 §2 figures cannot be reproduced by anyone — the OI-V-04 commit-granularity consequence, sessions `-05`…`-10` uncommitted, now `-05`…`-12` | project-manager / A4 / repository-engineer |
| OI-V-06 | **V-14 trial count undeclared** (Stage 6 pre-flight OQ-11). *"All trials"* / *"100% of trials"* carries no declared trial count. Explicitly **not ruled** by AIEF-AMD-010 §AMD-33 — the parameter does not fall out of the Stage 6 constructions, and a number without an engineering basis would be decoration. To be declared with the validation-campaign design, before any V-14 implementation | A4 + human owner, later session |
| OI-V-08 | **VER-004 residuals** ([`verification/VER-004`](verification/VER-004_Independent_Verification_AIEF-AMD-010.md), AIEF-AMD-010 audit: 9 criteria, 9 PASS, verdict VERIFIED WITH FINDINGS). FIND-Q4-1 (INFO) **open**: the tokenizer pin is trust-on-first-use — at the first authoritative Stage 6 build the measured artifact hashes must be compared against independently published upstream values before the lock is approved (human owner or QA). FIND-Q4-2 (MINOR) **open**: the ustar/pax long-path edge — before the first release build, verify no emitted path exceeds ustar limits and forbid extended headers, or specify pax-header canonicalization (A4 or implementation-spec note). FIND-Q4-3 (MINOR) **dispositioned** at the VER-004 filing: `STATE.open_non_blocking` synced with OI-V-06..08 | human-owner / A4 / qa-engineer |
| OI-V-07 | **`core/validation/CHECKS.md` and `MANIFEST` stale in two check texts.** AIEF-AMD-010 extended `validation[V-09].verifies` (tokenizer families TF-1/TF-2) and `validation[V-10].verifies` (platforms, DC-4/DC-5); the Stage 5 output emitted at `S-2026-08-08-03` carries the pre-amendment texts. Check count unchanged at 25. Requires a Stage 5 re-emission — compiler work (CMP-BLOCK-004); `core` is framework-only write access and was deliberately not hand-edited (AMD-010 § Blast Radius). **Deepened `S-2026-08-08-10`** (`V-09` extended again by AMD-42/AMD-45) and **again `S-2026-08-08-12`** (`V-09` extended by `AIEF-AMD-014` §AMD-51, `V-03` extended by §AMD-49): **three** check texts are now stale. Check count still unchanged at 25 | repository-engineer, after CMP-BLOCK-004 |
| OI-C-07 | **`core/agents/INDEX.md` lacks the enabled `software.*` roles.** Its role tables list universal + mechanical only; the duty-conflict list already names `software.test-engineer`'s conflict. Until a Stage 1 re-render, a strict V-22 reading (provenance roles resolve *in the INDEX*) does not resolve the three tokens — they resolve in the manifest, `BINDING.md` and `ROSTER.md`. V-22 is declared, not implemented (CMP-BLOCK-005). `core` is framework-only write access and was deliberately not hand-edited; re-emission path: Stage 1 re-render, compiler work | repository-engineer, after CMP-BLOCK-004 |
| OI-C-08 | **A boot-loaded file carries no cap and is therefore unmeasured.** `boot_sequence[B4].files` names `ledger-head`, and `files[ledger-head]` (`project/ledger/HEAD`) declares `tier: T1`, `token_cap: null`; it measures 411 (TF-1) / 504 (TF-2) — reproduced at the close of `S-2026-08-08-12`, unchanged across `-10`, `-11` and `-12`. The boot-loaded set is the eight capped files **plus** this one; the other eight T1 entries are read at no boot step at all. So the V-09 measured set — ruled to be exactly the capped T0∪T1 entries by [`AIEF-AMD-013`](../../framework/AIEF-AMD-013_Boot_Budget_Determination_and_Stage_6_Build_Constructions.md) §AMD-42 — under-covers the real boot cost by exactly one file and over-covers nothing. **Expressly not cured there**: assigning `ledger-head` a cap draws on the same 200-token MI-4 headroom OQ-15 must allocate, and would prejudge it. Named in the manifest at `budget_measurement_record.measurement_domain` so the gap is self-announcing. **Still not cured at `S-2026-08-08-12`, and `AIEF-AMD-014` §AMD-50 states why in arithmetic:** after the `BOOT.md` cap raise Σ `token_cap` = **5904** against MI-4's 6000, leaving **96** tokens of headroom against the **504** `ledger-head` would need. OQ-15 chose option (a), which allocates no headroom to this file; curing it now requires either a cap reduction elsewhere (the option (b) redistribution the owner did not choose) or a ceiling re-derivation, and both are human-owner architecture decisions **MEASURED CONSEQUENCE, `S-2026-08-12-01`.** The uncapped file grew for the first time: the `genesis → active` transition rewrote `HEAD`, and the first draft of that rewrite took it from 450 to 728 TF-1. Nothing in the boot budget noticed — `HEAD` has no `token_cap`, so `V-09` cannot fail on it. What caught it was `T-005`'s declared `context_budget`, which charges `HEAD` as mandatory context: `X-08` went to FAIL and two live tests with it. The prose was moved into `L-0000001` (T4, loaded on request) and `HEAD` trimmed to **497 TF-1**, restoring the shape. **Slack is now 3 tokens**, and the only standing guard on a file read at every boot is a *task record's* budget that happens to name it. That is the gap this item records, now with a number against it | A4 + human owner, later session |
| OI-C-09 | **Implementation delta owed in `src/aief_stage6/**` after AIEF-AMD-013.** One substantive change: §AMD-45 requires the lock's budget deferral be keyed on the path `core/MANIFEST.lock`, whereas `budget.measure` keys it on *absence from the tree* — the latent fail-open TCR-001 recorded as finding **F1**; under the ruling, any other measured file absent from the tree must halt, not defer. **This disposes TCR-001 F1.** Additionally, conformance re-certification (not redesign) of the interim choices now made normative: §AMD-43 lock JSON layout incl. the RFC 8259 escaping clause, §AMD-44 `build_provenance` six-member closed set, §AMD-46 `entry_types`, §AMD-47 `binding_pin_write` halt conditions, §AMD-48 `empty_content` and `run_fixed_values`. **Three test assertions are already stale** — full-suite run at the close of `S-2026-08-08-10`: 114 tests, **111 pass, 3 fail, no implementation defect**. `test_stage6_certification_evidence.py::TestV09Recomputation::test_three_breaching_files_confirmed` and `::test_totals_and_governing_family` pin the S-08 measurement of two `mutable` project files as constants (`project/STATE.md` 1791; totals 10296 / 12657) — exactly what §AMD-42 `measurement_instant` declares are **not** constants; `test_stage6_coverage_and_build.py::test_v24_live_registry` pins the registry at 28 while V-24 itself now **passes** at 29. Each failure is the ruling and the registry working correctly, asserted against by a snapshot. `src/` and `tests/` are PR-controlled; **not performed by the ruling session** — A4 does not implement what it ruled (SOD-1). **Extended `S-2026-08-08-12` by `AIEF-AMD-014`:** a second substantive change — §AMD-51 requires the lock's `token_cap` be **added to the per-family totals** before the aggregate ceiling comparison whenever the lock row is `DEFERRED-SELF-MEASURED`, and `budget.measure` today compares the un-charged totals; plus conformance certification of the §AMD-49 bounded-register 1:1 mapping now bound into `V-03`, of the two new `files[]` entries, and of the amended `files[boot].token_cap` 504. The three stale assertions are superseded again (registry 29 → 30; the `mutable`-file snapshots move again); test maintenance travels with the re-certification. Not performed by `S-2026-08-08-12` for the same SOD-1 reason | `software.software-engineer` + `software.test-engineer` |
| OI-C-10 | **The bounded index's own runway is finite, and it is measured.** `AIEF-AMD-014` §AMD-49 bounds the boot cost of the open-items register to one identifier per recorded item — the property AMD-41 §7 ground 3 required, and a change of kind: the register may now grow without end while the index grows only with the *count* of identifiers, not with the *volume* of recorded text. It is not a change of order: the index cost is still O(number of identifiers). The index's closing measurement against its 600 cap is recorded in [`approvals/APR-014`](approvals/APR-014_Amend_Framework_Manifest_AMD-014.md) § *The split as measured* — not here, because a file inside the measured set cannot state its own count without changing it — and the observed marginal cost of one further identifier is about six tokens under the governing family, so the remaining headroom admits on the order of a dozen further identifiers before the index itself breaches. Closing an item costs nothing (the identifier moves between sections); only a genuinely new identifier costs. **Not a defect and not cured here**: the owner's OQ-15 instruction authorised raising `BOOT.md`'s cap *only*, and after that raise MI-4 stands at 5904 of 6000 with 96 tokens of headroom, so no cap can be raised further without a reduction elsewhere. Recorded so the next authority meets arithmetic rather than a surprise; the lawful successors are a `token_cap` redistribution (OQ-15 option (b)), a ceiling re-derivation, or a further grammar reduction | A4 + human owner, later session |

## Closed

| ID | Item | Closed by | Resolution |
|---|---|---|---|
| OI-CAD-01 | (CLOSED `S-2026-08-11-04`) **CLOSED — repaired live**: material re-assigned to Aluminum Oxide (observed 7.63 g), saved, re-exported; evidence `cad/runs/REPAIRS_S-2026-08-11-04.json`. Original record: **SEWCP-600 model material defect: Steel where `spec/05` §6 and the package require 99.8% alumina (LP-10 forbids metal).** Geometry verified correct (vol 1957.2 mm³); the build acceptance checked extents/volume only, so the material row slipped. Repair is one `assign_material` + save + re-export, scripted in [`cad/BRIDGE_RESUME.md`](../../cad/BRIDGE_RESUME.md); BOM carries the spec material with the defect flagged | Mass-property truth of the model; nothing at LC-M04 | `mechanical.cad-engineer` · `S-2026-08-11-04` — repaired live; evidence `cad/runs/REPAIRS_S-2026-08-11-04.json` | **Row relocated from § *Open, not blocking* to § *Closed* by `S-2026-08-11-06`, with its text carried verbatim.** The row said CLOSED in its own leading cell while sitting in the open section, so the index (which listed it as Closed) and the register disagreed - a break of the AMD-49 mapping that `V-03` declares BLOCKING and did not check. The check is now implemented; this is one of the two breaks it found. |
| OI-CAD-02 | (CLOSED `S-2026-08-11-04`) **CLOSED — repaired live**: the stale lineage's cloud reference-derivative proved durably stuck at v4 (opens resolved the verified tip, inserts never did, across cold sessions); the verified content was re-homed to a fresh lineage which now backs the assembly (verify PASS, 7.6731 kg, saved v5, STEP+f3d exported). Old lineage quarantined under OI-CAD-03. Original record: **Master assembly CP occurrence binds stale v4 content; bridge down pending one human click.** The verified CP state was re-committed and round-trips as V5; the old Fusion session's lineage cache kept serving v4 to `addByInsert`, bounded recovery (update-refs, reload, re-insert, latest-version pin) failed, and the programmatic Fusion restart lost the bridge because the add-in run state is a per-user setting no API or UIA reaches. Assembly is otherwise verified PASS (19 occurrences) and saved. Resume procedure: [`cad/BRIDGE_RESUME.md`](../../cad/BRIDGE_RESUME.md); assembly STEP/f3d export pending the same. UI-automation of the one click is **abandoned** - the desktop is in active human use and synthetic input lands in the foreground window; the QML dialog is UIA-opaque. The document-creation lifecycle that produced the blank/interim artifacts is **fixed and regression-tested** (`cad/DOCUMENT_LIFECYCLE.md`, `tests/test_document_lifecycle.py`): identity binds without persistence, first-save happens only on verified PASS, failed attempts discard/revert. ZZ-* disposition: blank shell and interim strap classified DELETE (commands scripted in BRIDGE_RESUME Step 1b); the free-S strap is HISTORICAL/PRESERVE, owner-reserved | Assembly export + CP rebind; nothing at LC-M04 | `mechanical.cad-engineer` · `S-2026-08-11-04` — repaired live; evidence `cad/runs/REPAIRS_S-2026-08-11-04.json` | **Row relocated from § *Open, not blocking* to § *Closed* by `S-2026-08-11-06`, with its text carried verbatim.** The row said CLOSED in its own leading cell while sitting in the open section, so the index (which listed it as Closed) and the register disagreed - a break of the AMD-49 mapping that `V-03` declares BLOCKING and did not check. The check is now implemented; this is one of the two breaks it found. |
| OI-V-11 | **`VER-014` evidence-integrity defects block `LC-M04-EXIT` C6 for ECR-D-001.** The engineering disposition is independently verified across four cold rounds (round 4: `W9 RECORD ACCURATE: PASS`, controls byte-identical, approval chain reproducing, registry honest). The C6 *report* has failed four rounds on its own bookkeeping: the identifier collision moved four times (APR-014→APR-016; VER-008 reserved by T-003; VER-010 reserved by T-005 and cited 5× by R-014; now VER-014), and round-3 finding labels `V1..V8` collided with the round-1 criteria labels `V1..V10` in the same file. Both repaired at `2bd2c1a` and awaiting a confirmatory C6-scoped round. **Clerical; no engineering authority required. Blocks C6, therefore blocks the gate; does NOT block ECR-D-002..004.** Human-owner ruling `S-2026-08-09-14`: the remaining Design Authority decisions proceed in parallel | Closed by [`VER-017`](verification/VER-017_Confirmatory_Round_On_The_C6_Instrument_And_VER-016_Residue.md), `qa-engineer` · `S-2026-08-10-05`, the confirmatory C6-scoped round this row was waiting for | **Discharged.** `VER-017` W9 re-checked both surviving conditions: the identifier collision is settled at `VER-014` and the round-3/round-1 label collision is resolved by the round-prefixed scheme its own header declares. `VER-017` covers `ECR-D-001` and supersedes `VER-014` under the sealed relation, so `C6` reads the governing report rather than the four-round bookkeeping history. **A defect this row did not know it had**: while it correctly said `VER-014` blocks `C6`, the `C6` predicate was reading `VER-014`'s declared `ECR-D-001 NOT CLOSED` as *clearing* - it matched none of the three keywords scanned for - so the gate reported `C1`/`C6` satisfied for `ECR-D-001`. **The register was more truthful than the instrument.** Closed by the `ECR-D-012` verdict vocabulary |
| OI-C-12 | **Four exec-layer tests pinned a six-task snapshot of live, growing project state.** `test_every_task_record_loads`, `test_the_live_gap_between_declared_and_effective_is_five_tasks`, `test_the_live_dependency_state_is_derived_end_to_end` and `test_x09_guards_the_publication_channel_in_both_directions` asserted a fixed task set and producer count; `T-007`/`T-008` broke them. The row ruled the repair - test the property, not the count - and recorded why it was deferred: the files are `T-001`'s deliverable and `R-014` pins their digests | Repaired by `T-009` · `S-2026-08-10-04`, published at [`R-017`](results/R-017.md), which supersedes `R-014` and seals it | **Repaired at the property, as ruled.** Each test now derives its expectation from the records: the task set from `T-*.md` on disk; the write-scope gap from both sides of the declaration; currency from `CURRENT`/`SUPERSEDED` partitioning the results, with each `CURRENT` record the head of its own chain; and X-09's reached set from `T-001`'s scope against every other task's `produces`. **Three further instances of the same pin were found while repairing these four** - two more `len(current) == 1` assertions and one that read *the* CURRENT result T-001 produces, which stopped naming anything once a different task published the exec-layer head. `len(current) == 1` was never a count of anything: it held only while the repository had one result chain, and it now has three. No test was weakened - `VER-017` W11 confirms 620 pass against a baseline of 565 |
| CMP-BLOCK-006 | **V-09 token-budget breach — first authoritative Stage 6 build lawfully halted.** As registered per VER-006 FIND-Q6-1, measured under both normative families (TCR-001 and VER-006 independently confirmed to the digit at their instant): `BOOT.md` 504 > cap 400; `project/STATE.md` 1791 > 1100; `project/OPEN_ITEMS.md` 7937 > 600; T0∪T1 aggregate 12657 (TF-2) / 10296 (TF-1) > ceiling 6000. Not family-marginal. **A4 determination filed** — [`AIEF-AMD-013`](../../framework/AIEF-AMD-013_Boot_Budget_Determination_and_Stage_6_Build_Constructions.md) §AMD-41, `chief-systems-engineer · S-2026-08-08-10`, on independent re-measurement: (i) five of the eight values still reproduce to the digit; the three project-partition values do **not** — **at the start of `S-2026-08-08-10`** `STATE.md` 1747, `OPEN_ITEMS.md` 8673, aggregate 13349 (TF-2) / 10889 (TF-1) — because V-09 measures live `mutable` project registers and the act of recording this blocker enlarged one of them (declared a property by §AMD-42 `measurement_instant`); (ii) **deterministic content reduction is determinately insufficient** — `BOOT.md` is `framework-only` write access, its 400 cap is unachievable against its own frozen render floor of 475 TF-2, and `OPEN_ITEMS.md` is authoritative at 14.5× its cap; (iii) a **cap/ceiling raise alone is determinately inadmissible** — it breaches MI-4 and destroys the `AIEF-FRZ-001` §1.8 derivation; (iv) the choice of remedy among the recorded options is a discretionary architecture decision and is **reserved to the human owner as OQ-15** (LAW-12, following the AIEF-AMD-010 §AMD-34 precedent). ~~**Remains BLOCKING**: until OQ-15 is decided and enacted the AMD-31 gate fails and no conforming `core/MANIFEST.lock` can lawfully exist~~ — **superseded**: OQ-15 is decided and enacted. **Measurement history, each figure stamped with its instant** (`measurement_instant`; VER-007 FIND-Q7-1 disposed by this stamping): `S-2026-08-08-08`/`-09` — `STATE.md` 1516/1791, `OPEN_ITEMS.md` 6261/7937, totals 10296/12657. Start of `-10` — 1503/1747, 6867/8673, totals 10889/13349. Close of `-10` (`APR-012`) — 921/1083 **PASS**, 9428/12000, totals 12868/16012. Close of `-11` (`VER-007` §4e) — 10065/12773, totals 13505/16785. `BOOT.md` 445/504 at every one of them | **Decided by the human owner** (`core/PRECEDENCE.md` rank 1) as **OQ-15 option (a)**, 2026-08-08; enacted by `chief-systems-engineer · S-2026-08-08-12`; instrument [`AIEF-AMD-014`](../../framework/AIEF-AMD-014_OQ-15_Enactment_Bounded_Register_Split.md) §§AMD-49…AMD-52; approvals [`approvals/APR-014`](approvals/APR-014_Amend_Framework_Manifest_AMD-014.md), [`approvals/APR-015`](approvals/APR-015_Register_AIEF-AMD-014.md) | **DISPOSED — V-09 PASSES under both families, per file and in aggregate.** Defect class A cured by §AMD-50: `files[boot].token_cap` 400 → **504**, the measured governing value and the minimum admissible cap, derived rather than rounded. Defect class B cured by §AMD-49: `project/OPEN_ITEMS.md` and `project/STATE.md` keep their ids, tiers, caps and boot steps and become **bounded T1 indexes** under a declared grammar, with the full authoritative content moved to the new cap-null **T4** entries `project/OPEN_ITEMS_REGISTER.md` and `project/STATE_REGISTER.md` under a 1:1 mapping — which bounds boot cost under unbounded register growth, the property AMD-41 §7 ground 3 required. The AMD-51 lock charge is applied to the totals. **The closing per-file and aggregate figures are recorded in [`approvals/APR-014`](approvals/APR-014_Amend_Framework_Manifest_AMD-014.md) § *The split as measured*** — deliberately not here: a file inside the measured set cannot state its own count without changing it, and the index cannot carry them at all. Residual expressly **not** cured: **OI-C-08** |
| OQ-15 | **Remedy for CMP-BLOCK-006 — reserved to the human owner, open.** [`AIEF-AMD-013`](../../framework/AIEF-AMD-013_Boot_Budget_Determination_and_Stage_6_Build_Constructions.md) §AMD-41 §§5–7 determines that an architecture amendment is required and rules out both pure strategies, then reserves the choice among the lawful alternatives: **(a)** split the authoritative register — a bounded T1 `project/OPEN_ITEMS.md` index plus a new cap-null T4 `files[]` entry carrying the full register, every byte preserved at a declared home; **(b)** re-derive the budget from a higher `version.min_context_window`, raising the ceiling at the frozen 20% share and redistributing all eight caps; **(c)** narrow V-09's build-time domain to the integrity-hashed framework-emitted files and enforce the project-partition tier caps at session close under LAW-09 — **admissible only in combination**, never alone; **(d)** any consistent combination, in every case with `BOOT.md`'s cap raised to at least its measured governing value. **A4 recommendation: (a) plus a `BOOT.md` cap raise, optionally with (c); (c) alone is recommended against.** Binding arithmetic: Σ `token_cap` = 5800 against MI-4's 6000, so **200 tokens of cap headroom exist in total** and every raise draws on the same pool — which is why §AMD-41 §6 declines to set `BOOT.md`'s cap in isolation. **Observation added at the close of `S-2026-08-08-10`, for the owner's decision:** `project/STATE.md` is a second, milder instance of the same defect class. Reducing it to 921 / 1083 against its 1100 cap required stripping it to near-index form — every narrative fact replaced by a pointer, no recorded fact lost — and left **17 tokens of headroom**, which the next session's write will consume. Whichever option is chosen must therefore address `project/STATE.md`, not `project/OPEN_ITEMS.md` alone: under (a) that means an index grammar bounding per-entry cost in both registers, under (b) a redistribution that gives `STATE.md` real room. Blocking status is carried by CMP-BLOCK-006, not by this row | **Decided by the human owner** (`core/PRECEDENCE.md` rank 1), 2026-08-08 — **option (a)**: *"Implement the lossless register split exactly as specified in the existing OQ-15/A4 record: bounded T1 `OPEN_ITEMS.md` index; full authoritative register in the declared cap-null T4 `files[]` location; deterministic 1:1 mapping; preserve all information; raise `BOOT.md` cap only to the minimum required value; apply the same required bounded-register treatment to `STATE.md`. Use the existing architecture and OQ-15 record as authority. Do not redesign it."* Enacted by `chief-systems-engineer · S-2026-08-08-12`; instrument [`AIEF-AMD-014`](../../framework/AIEF-AMD-014_OQ-15_Enactment_Bounded_Register_Split.md) §§AMD-49…AMD-52; approvals [`approvals/APR-014`](approvals/APR-014_Amend_Framework_Manifest_AMD-014.md), [`approvals/APR-015`](approvals/APR-015_Register_AIEF-AMD-014.md) | Option (a) enacted as recorded. Option (c) **not** adopted — the owner named (a) alone, and A4 had recommended against (c) standing alone. Two new `files[]` entries, tier **T4**, `token_cap: null`, partition `project`, layer L4, generator 3, `integrity: unhashed`: `open-items-register` → `project/OPEN_ITEMS_REGISTER.md` (owner `project-manager`) and `state-register` → `project/STATE_REGISTER.md` (owner `chief-systems-engineer`). `open-items` and `state` keep their ids, paths, tiers, caps, owners and boot steps (B7, B3). `files[boot].token_cap` **400 → 504** (§AMD-50) — the measured governing value, not the 550 AMD-41 §6 used illustratively, because the owner directed *"only to the minimum required value"*. **No other `token_cap`, and not the 6000 ceiling, changed**; `version.min_context_window` is untouched, so the ratified 32,000-token portability floor is unchanged and option (b)'s cost is not incurred. MI-4: 5800 → **5904** ≤ 6000, headroom 200 → **96**. The 1:1 index↔register mapping and the index grammar are declared normatively at `metadata.reproducible.bounded_register_split` and bound into `validation[V-03]` — a ruling without a check is a convention (the AMD-19/AMD-26 lesson). The `STATE.md` requirement of the reservation's closing observation is satisfied by the same mechanism, not by a further reduction of prose |
| OQ-B1…B5 | *(row key shortened from the pre-split leading cell `OQ-B1…OQ-B5, and the three lower-order items`, whose wording is retained here verbatim; §AMD-49 requires a compact row key.)* The five Stage 6 **build** open questions filed at [`STAGE-6_BUILD_OPEN_QUESTIONS.md`](STAGE-6_BUILD_OPEN_QUESTIONS.md) per VER-006 FIND-Q6-2 and TCR-001 F2 — lock JSON layout; `build_provenance` content; the lock self-measurement fixed point; archive "nothing else" vs the declared directory mode; BINDING pin-line layout — plus DC-1 of a zero-length file, the budget-totals scope, and the AMD-33 timestamp/build-id capture. Each defined bytes of the first canonical `MANIFEST.lock`/distributable and was in force only as a disclosed interim choice | Ruled by `chief-systems-engineer` · `S-2026-08-08-10` — a cold session that differs from the raiser `software.software-engineer · S-2026-08-08-07` in **both role and session** (AMD-20 identity rule; the raising artifact's own bar, confirmed in writing at [`AIEF-AMD-013`](../../framework/AIEF-AMD-013_Boot_Budget_Determination_and_Stage_6_Build_Constructions.md) § *Independence declaration*); instrument §§AMD-42…AMD-48; approvals [`approvals/APR-012`](approvals/APR-012_Amend_Framework_Manifest_AMD-013.md), [`approvals/APR-013`](approvals/APR-013_Register_AIEF-AMD-013.md) | **OQ-B1** (AMD-43): interim layout adopted and completed as `core_aggregate.lock_json_layout` — 2-space indent, LF, one terminal LF, no trailing whitespace, one space after the name separator, one member per line, **RFC 8259 escaping only** (the clause byte-identity actually turns on), emission order never sorted. **OQ-B2** (AMD-44): `build_provenance` is a **closed set of six** — `source_manifest`, `source_manifest_dc1`, `selected_profile`, `compiler_stage`, `build_id`, `timestamp`; host, user, working directory, tool versions, per-execution time and environment capture **prohibited**, because each varies across the executions byte-identity requires to agree. **OQ-B3** (AMD-45): the lock's row is `DEFERRED-SELF-MEASURED` with null counts contributing nothing to totals, and the serialised octets are cap-checked post-serialisation — with the substantive correction that the deferral is keyed on the **path**, never on absence from the tree, disposing TCR-001 **F1** (delta owed as OI-C-09). The under-count this left in the aggregate ceiling test was raised as VER-007 FIND-Q7-3 and closed by `AIEF-AMD-014` §AMD-51, which charges the lock's cap to the totals. **OQ-B4** (AMD-46): regular file entries only; `contents` governs and the 0755 directory-mode clause is vacuous for a conforming archive, retained not deleted. **OQ-B5** (AMD-47): value-token-only replacement on the single `core_digest_pin` line, every other octet preserved incl. the inline comment; zero/multiple pin lines or a non-hex64 value **halt**. **DC-1 empty content** (AMD-48): the SHA-256 of zero octets, `e3b0c442…7852b855`; no non-empty digest changes and no empty artifact is licensed. **Budget-totals scope** (AMD-42): the interim reading **confirmed** — exactly the non-null-`token_cap` T0∪T1 entries, MI-4's own domain and §1.8's own Sum row; residual raised as OI-C-08. **AMD-33 capture** (AMD-48): build id and timestamp fixed once per **release run** and supplied to every execution — the only reading under which AMD-33 is satisfiable |
| OI-C-06 | The three `software.*` role artifacts (AIEF-AMD-011) sat outside DC-4's declared coverage (`core/profiles/<selected>/**`, selected `mechanical`) and outside any Stage 1 re-render — once Stage 6 ran, `MANIFEST.lock`/B2a would not have bound them. Recorded by AMD-011 §AMD-35, deliberately unresolved there | Ruled by `chief-systems-engineer` · `S-2026-08-08-06` — cold session, at live human-owner direction (rank 1), before Stage 6 execution as directed; instrument [`AIEF-AMD-012`](../../framework/AIEF-AMD-012_DC-4_Coverage_of_Enabled_Role_Artifacts.md) §§AMD-39/AMD-40; approvals [`approvals/APR-010`](approvals/APR-010_Amend_Framework_Manifest_AMD-012.md), [`approvals/APR-011`](approvals/APR-011_Register_AIEF-AMD-012.md) | **DC-4 coverage extended** (option (a) of the AMD-011 §AMD-35 alternatives): the covered set now adds, for every `BINDING.enabled_agents` token `<profile>.<name>` outside the selected profile, the `files[]` entry at `core/profiles/<profile>/agents/AGT-<name>.md` — declared normatively in the manifest's new `core_aggregate.enabled_role_coverage` member. Deterministic and evaluable from the manifest + BINDING alone; the working tree is never a coverage input, so B2a is strengthened, never weakened; agent artifacts only — the unemitted software `PROFILE.md`/`LC-S*` stay uncovered (no contradiction with AMD-36). The DC-4 worked example is synthetic and **stands unchanged** (AMD-40). The re-render prong is now self-announcing: loss of the three files after an upgrade halts B2a; re-emission remains owed under AMD-011's authority. Interim (pre-Stage-6) integrity record remains the APR-008 DC-1 digests, now time-bounded. Manifest re-registered `ae16ccac…` → `f06125d2…`; registry 27 → 28 |
| OQ-13 | Allocation of the compiler implementation work — the choice recorded as reserved to the human owner at [`AIEF-AMD-010`](../../framework/AIEF-AMD-010_Stage_6_Constructions_and_Preflight_Dispositions.md) §AMD-34 | **Decided by the human owner** (`core/PRECEDENCE.md` rank 1), 2026-08-08 — option (a): *"ENABLE the three software.\* roles for this SEWCP instance, following the existing AMD-006 profile-activation pattern. Do not activate them silently. The three software roles must be explicitly defined and persisted before implementation begins."* Enacted by `chief-systems-engineer · S-2026-08-08-05`; instrument [`AIEF-AMD-011`](../../framework/AIEF-AMD-011_Software_Role_Enablement.md) §§AMD-35…AMD-38; approvals [`approvals/APR-008`](approvals/APR-008_Enable_Software_Roles.md), [`approvals/APR-009`](approvals/APR-009_Register_AIEF-AMD-011.md) | `software.software-engineer`, `software.test-engineer`, `software.platform-engineer` enabled additively in `BINDING.enabled_agents` (A1 each, per the manifest contracts); contracts persisted at `core/profiles/software/agents/AGT-*.md`; `active_profile` unchanged at `mechanical`; software lifecycle **not** instantiated; ~~roster rows added UNASSIGNED — dispatch requires project-manager assignment or rank-1 instruction (OI-P-02)~~ — **corrected `S-2026-08-08-12`** (`AIEF-AMD-014` §AMD-52, disposing VER-007 FIND-Q7-6): the rows were added UNASSIGNED and were **assigned within the same day** at rank-1 direction; `ROSTER.md` § *Profile `software`* now names an identity and workstreams for all three, so the software rows are not part of OI-P-02. **No manifest change** (AMD-37). No implementation started; CMP-BLOCK-004/-005 remain open as implementation blockers. Residuals recorded as OI-C-06 and OI-C-07 |
| OQ-1…OQ-12 | *(row key shortened from the pre-split leading cell `OQ-1…OQ-10, OQ-12`, retained here verbatim; the set is OQ-1 through OQ-10 and OQ-12 — OQ-11 is **not** in it, it became OI-V-06.)* Stage 6 pre-flight specification gaps recorded in [`STAGE-6_PREFLIGHT_CMP-BLOCK-004_005.md`](STAGE-6_PREFLIGHT_CMP-BLOCK-004_005.md) (commit `dc811a6`): compiler scope, tokenizer families, release digest, core aggregate, root-file coverage, budget record, distributable, check ordering, campaign scope, build-time reproducibility, V-10 platforms | Ruled by `chief-systems-engineer` · `S-2026-08-08-04` — cold session, raiser (`project-manager · S-2026-08-08-03c`) differs in role and session (AMD-20); instrument [`AIEF-AMD-010`](../../framework/AIEF-AMD-010_Stage_6_Constructions_and_Preflight_Dispositions.md) §§AMD-25…AMD-33; approvals [`approvals/APR-006`](approvals/APR-006_Amend_Framework_Manifest_AMD-010.md), [`approvals/APR-007`](approvals/APR-007_Register_AIEF-AMD-010.md) | OQ-1: Stage-6-only increment admissible for Stage 6; full compiler still required for Release 1.0.0 (AMD-25). OQ-2: TF-1 `cl100k_base` BPE + TF-2 T5 SentencePiece, artifact-pinned (AMD-26). OQ-4/5: **DC-4** declared, root files covered, `MANIFEST.lock` self-excluded, worked example published (AMD-27). OQ-3: **DC-5** raw-octet archive digest, distinct from the aggregate (AMD-28). OQ-6: `budget_measurement` member of `MANIFEST.lock` (AMD-29). OQ-7: deterministic uncompressed ustar tar, not tracked in-repo (AMD-30). OQ-8: compile-time checks precede emission, V-10 excepted (AMD-31). OQ-9: campaign is V-01…V-25, §6.4 enumeration superseded in reading (AMD-32). OQ-10/12: two-execution build-time check; V-10 platforms Windows + one of Linux/macOS (AMD-33). DC-1/DC-2/DC-3 untouched. **Not closed:** OQ-11 → OI-V-06; OQ-13 and OQ-14 → open with the human owner |
| ECR-Q-003 | Stage 1's barrier *"No later stage may emit into core"* contradicted by the declared outputs of Stages 2, 5 and 6; held one question Compiler Stage 5 had to answer before emitting | Disposition **A**, ruled by `chief-systems-engineer` · `S-2026-08-08-03` — a cold session that did not raise it (AMD-20 identity rule); instrument [`AIEF-AMD-009`](../../framework/AIEF-AMD-009_Stage_1_Barrier_and_MI-3_Namespace.md) §AMD-23; approval [`approvals/APR-004`](approvals/APR-004_Amend_Framework_Manifest_AMD-009.md) | The barrier protects the **Stage 1 output set**, not the `core/` prefix. Later stages emit only into their own declared subtrees; evaluable as pairwise disjointness of `generation_order[].outputs`, now bound into `V-01`. Stage 2's completion is conforming; `AIEF-FRZ-001` Part 4 Stage 1 barrier row superseded in reading, bytes unchanged. **Stage 5 may lawfully emit `core/validation/**`** |
| OI-C-03 | `boot.referenced_by` carried `framework` and `sch-state.referenced_by` carried `V-06`; neither a `files[]` id — strict MI-3 reading failed V-01, halting any Stage 1 re-emission. FIND-9 | Ruled by `chief-systems-engineer` · `S-2026-08-08-03`; instrument `AIEF-AMD-009` §AMD-24; approval `approvals/APR-004` | **Strict reading governs:** MI-3 targets range over `files[]` ids only. Both dangling tokens removed — `framework` unrecoverable and not guessed; the `V-06`→`sch-state` relation preserved in `validation[V-06]` and MI-7. Manifest now **passes MI-3 strictly** over all 106 entries. Residual observation recorded as OI-C-05 |
| ECR-Q-001 | Freeze-set aggregate construction undefined; the recorded value not reproducible by any of thirteen tested constructions | Disposition **A — declare the construction explicitly**, ruled by `chief-systems-engineer` · `S-2026-08-08-02`; instrument [`AIEF-AMD-008`](../../framework/AIEF-AMD-008_Digest_Constructions_and_QA-001_Dispositions.md) §AMD-16; approval [`approvals/APR-002`](approvals/APR-002_Amend_Framework_Manifest_AMD-008.md) | **DC-2** declared normatively in the manifest. Aggregate recomputed over the 24-member registry: `080771b0…f6b6367a`, full 64 characters in both `FROZEN.md` and `STATE.md`. The superseded value is **not recovered** and is marked so. Residual: **V-24 declared, not implemented** — OI-V-02 |
| ECR-Q-002 | Ledger `entry_hash` construction undefined; held the LAW-09 close and the irreversible `genesis → active` transition | Disposition **A — declare the construction explicitly**, ruled by `chief-systems-engineer` · `S-2026-08-08-02`; instrument `AIEF-AMD-008` §AMD-17; approval `approvals/APR-002` | **DC-3** declared: seven covered fields, canonical line-oriented preimage, `entry_hash` self-excluded, `prev_hash` feeds the successor digest, genesis `null`, two published worked examples. YAML rejected as the serialisation. **No ledger entry was written**; `HEAD` remains at `genesis` |
| OI-V-01 | Independent QA verification of **ECR-D-005 + Compiler Stage 2** outstanding | Verification report filed at [`verification/VER-001`](verification/VER-001_Independent_Verification_ECR-D-005_and_Stage_2.md) by `chief-systems-engineer` · `S-2026-08-08-02` | Cold-context `qa-engineer` audit: **10 criteria, 10 PASS, 0 fail**. Body filed verbatim; the qa-engineer neither filed nor attested to the filing. Nine findings raised; dispositions at [`reviews/DR-001`](reviews/DR-001_QA-001_Finding_Dispositions.md). Scope reconciled to the wider of the two previously recorded (FIND-6). **Does not extend to `S-2026-08-08-02`** — see OI-V-03 |
| ECR-D-005 | Freeze registry did not verify: `framework.manifest.json` diverged from its registered hash across releases 0.4, 0.6 and commit `6ce3508` | Disposition **A — re-register**, human-owner; approval [`approvals/APR-001`](approvals/APR-001_Reregister_Framework_Manifest.md) | Registered digest updated; **all four execution actions now complete** — the aggregate and `STATE.frozen_set_hash` were released by ECR-Q-001's disposition. Action 5, the standing check, remains OI-V-02. Divergence was fully attributable to approved amendments AMD-004/006/007 |
| OI-F-01 | `session_timeout` had no declared value | AIEF-AMD-003 §AMD-09 | 14400 s (4 h), project-overridable |
| OI-F-02 | Ledger genesis semantics unspecified | AIEF-AMD-003 §AMD-10 | `HEAD.state ∈ {genesis, active}`. At genesis B4 check 1 is vacuous; checks 2 and 3 are operative |
| CMP-BLOCK-014 | Build-order cycle `LAW-11 ↔ agent-contract` | AIEF-AMD-002 §AMD-06 | `depends_on` corrected; citation preserved as bidirectional `references` edges |
| OQ-14 | **Stage 6 execution authorization — reserved to the human owner, open.** Standing instruction of `S-2026-08-08-03`, unchanged by AIEF-AMD-010, AIEF-AMD-013 or AIEF-AMD-014: Stage 6 does not execute without explicit human authorization, independent of every specification ruling **UPDATED `S-2026-08-11-06`: THE AUTHORIZATION HAS BEEN GIVEN, BY THE HUMAN OWNER.** The owner's written instruction of 2026-08-11 names Stage 6 expressly - *"If Stage 6 can be executed without changing verified CAD: execute it autonomously"*, *"If Stage 6 requires MANIFEST.lock or equivalent frozen release state: generate it only from the authoritative current state"*, *"Complete Stage 6"*. That is `core/PRECEDENCE.md` rank 1 and it satisfies this reservation on its own terms. **It is recorded as the owner's act, NOT as an exercise of delegated authority** - `OQ-14` is an authorization, not an engineering judgement, and the delegation of engineering decision authority does not reach it (`DECISIONS_S-2026-08-11-06` DEC-10). **What remains is not authorization but execution.** The build carries the record in an `Authorization` object that travels into `build/stage6/canonical/preconditions.json`, and `assert_write_allowed` widens by exactly two paths - `.ai/core/MANIFEST.lock` and `.ai/project/BINDING.md` - and by nothing else, held there by four tests including one asserting the default is still refused. **The PREVIEW build passes end to end**: 12 of 12 AMD-31 compile-time preconditions PASS, byte-identical across two executions, lock boot-read prefix 69 of 200, DC-4 and DC-5 computed. **The CANONICAL emission was NOT performed**, and the reason is not an engineering condition: the host permission layer refused the command. `B2a` therefore remains unsatisfied and `BINDING.core_digest_pin` remains `PENDING-STAGE-6` **UPDATED `S-2026-08-12-01` — CLOSED: THE EXECUTION HAS NOW BEEN PERFORMED, UNDER THE SAME OWNER AUTHORIZATION.** The canonical emission ran 2026-08-12 with the owner's 2026-08-11 instruction carried verbatim into the build's `Authorization` object: mode `CANONICAL`, status `OK`, 75 covered files, DC-4 `2180df02…5ac92f0`, DC-5 `25e67bdb…40b1f9`, twelve of twelve AMD-31 preconditions PASS, lock boot-read prefix 69 of 200. **Exactly the two declared writes occurred** — `.ai/core/MANIFEST.lock` and `.ai/project/BINDING.md` — and nothing else, so the owner's condition *"without changing verified CAD"* holds on the change set itself rather than on assurance. **`B2a` now executes and PASSES**, recomputed independently at `S-2026-08-12-01` boot from the `AIEF-AMD-010` §§AMD-25/27 and `AIEF-AMD-015` §§AMD-54/55 texts by a verifier importing nothing from `src/aief_stage6`: 75 of 75 DC-1 records reproduce, DC-4 over them equals `MANIFEST.lock.aggregate_digest`, and that equals `BINDING.core_digest_pin`; the covered set is every `.ai/core/**` file except the self-excluded lock, plus the three root-partition files. `PENDING-STAGE-6` is gone from `BINDING`. **What this closure is not:** it records the discharge of an authorization already given and verifies nothing this session produced. It is **not** the independent QA act `OI-V-13` owes, and `ECR-D-006` and `ECR-D-014` remain open on that ground. | human-owner | Authorization given by the **human owner** at `S-2026-08-11-06` (`decisions/DECISIONS_S-2026-08-11-06` DEC-10 — `core/PRECEDENCE.md` rank 1, recorded verbatim, expressly **not** an exercise of delegated authority); execution performed and independently re-verified at `S-2026-08-12-01` by `repository-engineer`. Evidence: `core/MANIFEST.lock`, `BINDING.core_digest_pin`, ledger `L-0000001`. **The third cell is this row's original *Authority* cell carried verbatim** — under the § *Closed* header it reads as *Closed by*, and no cell was dropped in the move |

## Recorded risk, not blocking

| ID | Item |
|---|---|
| CDR-C3 | Independent cold-context ratification of the AIEF CDR not performed. Recorded at AIEF-FRZ-001 section 6.2. Four consecutive artifacts carry this exposure. |
| SOD-1 | **Separation of duties.** `core/agents/INDEX.md` states `chief-systems-engineer` *"may not implement what it approved."* Session `S-2026-08-08-02` both ruled and applied, at the direction of the human owner (`core/PRECEDENCE.md` rank 1, recorded in APR-002 and APR-003). Session `S-2026-08-08-03` repeated the departure in identical form for AIEF-AMD-009 (recorded in APR-004 and APR-005 and at `AIEF-AMD-009` § *Separation of Duties*); its mitigating control **was executed**: independent cold-context QA audit filed at [`verification/VER-002`](verification/VER-002_Independent_Verification_AIEF-AMD-009.md) — 9 criteria, 9 PASS, verdict VERIFIED WITH FINDINGS. The departures are authorised, not erased. Mitigating control for `-02` is OI-V-03, still open. Session `S-2026-08-08-04` repeated the departure in identical form for AIEF-AMD-010 (recorded in APR-006/APR-007 and at `AIEF-AMD-010` § *Separation of Duties*); its mitigating control **was executed**: independent cold-context QA audit filed at [`verification/VER-004`](verification/VER-004_Independent_Verification_AIEF-AMD-010.md) — 9 criteria, 9 PASS, VERIFIED WITH FINDINGS. Session `S-2026-08-08-05` repeated the departure in identical form for AIEF-AMD-011 (recorded in APR-008/APR-009 and at `AIEF-AMD-011` § *Separation of Duties*); its mitigating control **was executed**: independent cold-context QA audit filed at [`verification/VER-005`](verification/VER-005_Independent_Verification_AIEF-AMD-011.md) — 9 criteria, 9 PASS, VERIFIED. Session `S-2026-08-08-06` repeated the departure in identical form for AIEF-AMD-012 (recorded in APR-010/APR-011 and at `AIEF-AMD-012` § *Separation of Duties*); its mitigating control — an independent cold-context QA audit of that session's work — follows later this phase, dispatched by the same directing authority, and is open until filed; whether `VER-006` C8 discharges it is an A4 judgement, noted as not recorded at `VER-007` §10.6. Session `S-2026-08-08-10` repeated the departure in identical form for AIEF-AMD-013 (recorded in APR-012/APR-013 and at `AIEF-AMD-013` § *Separation of Duties*), with one narrowing recorded there: the implementation delta its own §AMD-45 requires in `src/aief_stage6/**` was **declined** and left to an A1 role as OI-C-09, so this departure covers ruling-and-applying the *records*, not the code. Its mitigating control **was executed**, including the required independent recomputation of the §AMD-41 re-measurement: independent cold-context QA audit filed at [`verification/VER-007`](verification/VER-007_Independent_Verification_AIEF-AMD-013_and_CMP-BLOCK-006.md) — 10 criteria, 7 PASS / 3 PARTIAL / 0 FAIL, VERIFIED WITH FINDINGS, 8 findings, none voiding any digest, approval, registry value or ruling. Session `S-2026-08-08-12` repeated the departure in identical form for AIEF-AMD-014 (recorded in APR-014/APR-015 and at `AIEF-AMD-014` § *Separation of Duties*), with the same narrowing: the `src/aief_stage6/**` delta its §AMD-51 requires was **declined** and added to OI-C-09. Its mitigating control is an independent cold-context QA audit of that session's work, open until filed. |

## Deviations recorded

| ID | Deviation |
|---|---|
| DEV-01 | ~~Compiler Stage 3 executed before Stage 2 at explicit direction.~~ **CLOSED** 2026-08-08 — Stage 2 emitted `core/templates/TPL-current-state.md`; `state.depends_on → tpl-current-state` is satisfied. |
| DEV-02 | `project/.session.lock` template not emitted. Not declared in `framework.manifest.json`. Runtime artifact created by boot step B4a; format specified in AIEF-FRZ-001 section 1.3. Emitting it requires an A4 manifest amendment. **Consequence observed:** boot step B4a has not executed in any session to date — see OI-P-01. |
