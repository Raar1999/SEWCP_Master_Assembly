# R-013 — Execution Layer, After the Pass-6 Findings Repair

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-012`](R-012.md)**, whose pinned architecture, layer and test digests this
> pass moved while correcting the three MAJOR and three MINOR findings `VER-009` pass 6
> raised against the pass-5 repair. `R-012` is retained unedited apart from the two
> supersession fields, and its DC-1 as it now stands is sealed below.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-013
produced_by:
  task:      T-001
  role:      software.software-engineer
  session:   S-2026-08-09-12
status:      SUPERSEDED
superseded_by: R-014
supersedes:  R-012
supersedes_seal:
  path:   .ai/project/results/R-012.md
  digest: be67c646ca12cad54a6c880662ce4d83a3edf9f07290bcb41d74ab585200197e

inputs:
    - path: .ai/BOOT.md
      digest: 5a97d6ad763f8720b3e9e5c9cf9be6ad62f6ba953b5b70eb78a3624b580b82db
    - path: .ai/core/CONTEXT_TIERS.md
      digest: dad8a47ad994cce45820b9b89f016872f259b7b44b10e8a87ed42f32e31ade4f
    - path: .ai/core/templates/TPL-task-package.md
      digest: a51c5d27fa44f85b3d6a291b28099161561e8cc367e2ce64dc8ae0553501e9b2
    - path: .ai/core/schemas/SCH-task.schema.json
      digest: e874fdd70b56f55b6205e26c7ef537e090e46b5f05ab2964f8b1ee4ad6cccc80
    - path: framework/framework.manifest.json
      digest: 920eb6eec217732152c452d51f01e471940df6f2e2ffe608c377fccc37814090
    - path: src/aief_stage6/digests.py
      digest: 311247d6fdb11ecffa160adad4a884371ee9d6a779dcfc5308ca8e15a358b0a4
    - path: src/aief_stage6/tokenizers.py
      digest: 26a6c5bd424aef5459b0049181158163793e38402733adf2ef3d74536caef95f

deliverables:
    - path: .ai/project/EXECUTION_ARCHITECTURE.md
      digest: 531174776a2eff231a5d2befdba3d879469b81475a3e60980fdbb487b3c8ac1d
    - path: src/aief_exec/__init__.py
      digest: b8813ebc7fb18c820721517de771f9ed8ca224971824e2e9680f227ef5f77b69
    - path: src/aief_exec/__main__.py
      digest: 04d89d567dc1b3885024128e0d4cfb22a2e69f3083bfb69b332304dee6d32d96
    - path: src/aief_exec/records.py
      digest: 0b9a7670afa0e0d775209d1f82a460389f6280b3cf61b35b6172b255ebcee6c5
    - path: src/aief_exec/scope.py
      digest: 51836c5c65e6da42cf05af2d7c7026da3a61a8b956d99bec3cf7f45252462be4
    - path: src/aief_exec/graph.py
      digest: badedbd3341a01ee37a7cf41b86c4c32d91cd64863b9b6567bd1b4865fa1e729
    - path: src/aief_exec/checks.py
      digest: e499d169a6bd6c066d766d7ef70a97909abd25af6ce3c61970ab748bd4ba08ea
    - path: tests/test_exec_records.py
      digest: f49a7d8440a7aae42e18eeea91ad2ea0b29f0db2885f1e16d88c309954219269
    - path: tests/test_exec_scope.py
      digest: e05e2772c31115c6c7cdc689c7ec94f8c0eed65dd271a15db2600dc16a041186
    - path: tests/test_exec_graph.py
      digest: e00e36cae3144789b51beea886bddfe9624652143d30ef7467a9f587595e4df4
    - path: tests/test_exec_checks.py
      digest: 1e8ffc9df413f39b21b8582f2e7b9a2c39d96136bdd49e47049f3b2ec2dd6108

acceptance:
  - id: AC-1
    outcome: FAIL
    evidence: |
      `PYTHONPATH=src python -m aief_exec check` reports 7 of 10 PASS and exits
      1. X-01 through X-07 PASS. X-08, X-09 and the new X-10 FAIL, and none of
      the three fails for a defect in the layer.

      The campaign has ten checks rather than nine because FIND-Q9-45 split the
      two verdicts X-08 used to carry into two checks. That is a change in which
      boolean each quantity feeds, not in what is measured:

        * X-08 now carries the `acquisition` dispatch gate ALONE - six rows,
          the same six (task, family) pairs as under R-012: T-002 TF-1 12880 /
          TF-2 16680, T-003 TF-1 8193, T-004 both families, T-006 TF-1 12154.
        * X-10 carries the `total_measurable` bound - eight rows: the four
          R-012 reported under X-08 (T-004 both families, T-005 TF-1 7161 /
          TF-2 9763) plus four that R-012's suppression withheld (T-002 both
          families, T-003 TF-1, T-006 TF-1, each equal to that task's
          acquisition figure because its `revision` is zero). No new breach was
          created; four real breaches stopped being suppressed.
        * X-09 is unchanged at ten rows.

      No cap was changed and no write_scope was widened by this pass.

      T-004'S FIGURES MOVED and no number is quoted for them here, which is a
      deliberate refusal rather than an omission. T-004 `consumes` this record,
      so its `dependency` component is this file, and its `optional` component
      is EXECUTION_ARCHITECTURE.md, which this pass extended by three sections.
      Its gate therefore rose from R-012's TF-1 22414 to something above 25000,
      and the figure moves again with every edit to this record - including the
      edit that would state it. That is the self-referential movement U-10
      records, met from the measurement side: the act of publishing a repair
      moves the figure of the task that must read the repair. Run
      `python -m aief_exec scope T-004` for the value at the moment you ask.

      Every other figure is unchanged to the token, including T-005's, which is
      the pair FIND-Q9-45 turns on.

      The criterion itself is still stale: it reads `X-01 through X-08`, written
      before X-09 and X-10 existed. Left as written rather than restated to fit
      the result - see U-5.
  - id: AC-2
    outcome: PASS
    evidence: |
      `python -m aief_exec brief T-002` emits the five entries T-002 declares
      mandatory and nothing else, and its reported total equals the resolved
      mandatory scope, TF-1 4910 / TF-2 6411. Unchanged by this pass.
  - id: AC-3
    outcome: PASS
    evidence: |
      `python -m aief_exec scope T-002`: mandatory resolved TF-1 4910 / TF-2
      6411 against the same files unanchored at TF-1 66437 / TF-2 87780. Factor
      13.5x TF-1, 13.7x TF-2, both above the required five. Unchanged by this
      pass.
  - id: AC-4
    outcome: PASS
    evidence: |
      `conflict_reasons` is empty for T-002 x T-003 and for T-002 x T-004, and
      non-empty for T-002 x T-005 - the scope property AC-4 tests, unchanged.
      `classify T-002 T-005` is CONFLICT on `[write/write]` and both
      `[write/observe]` directions. `classify T-002 T-004` is PARALLEL and
      prints the five comparisons the verdict rests on, each with its two
      operand sets and an EMPTY intersection. T-002 x T-003 remains BLOCKED on
      the unassigned qa-engineer role.

      Re-run rather than carried forward, because this pass restructured
      `scope.acquisition_units` - the enumeration `graph.read_surface` reads -
      and because T-004's consumed record changed from R-012 to this one. The
      verdicts are identical to R-012's. U-8 is unchanged and still open.
  - id: AC-5
    outcome: PASS
    evidence: |
      `git status --porcelain` lists exactly the same seven tracked
      modifications as before this pass - BINDING.md, FROZEN.md, OPEN_ITEMS.md,
      ROSTER.md, STATE.md, ENGINEERING.md, framework/framework.manifest.json -
      none of them written by it. Every path this pass wrote is either inside
      `.ai/project/` or under `src/aief_exec/` and `tests/test_exec_*.py`, and
      every one of those was already untracked. No path under `.ai/core/`,
      `framework/`, `spec/`, `implementation/`, `cad/`, `src/aief_stage6/` or
      `tests/test_stage6_*.py` was written, and neither was
      `.ai/project/verification/`, `.ai/project/approvals/`,
      `.ai/project/ledger/`, `FROZEN.md` or `STATE.md`.
  - id: AC-6
    outcome: NOT MEASURED
    evidence: |
      Reported, not disposed, and still not disposable as written - its baseline
      reads "the same 4 pre-existing failures" against a Stage 6 partition that
      shows 2. Restating that baseline is T-002's act.

      What this pass can establish is narrower and is asserted: it wrote nothing
      under `src/aief_stage6/` or `tests/test_stage6_*.py`, and
      `pytest tests/test_exec_*.py` is 275 passed, 0 failed. The Stage 6 suite
      was not run by this pass, by contract.

files_changed:
  - path: src/aief_exec/checks.py
    change: |
      X-08 reduced to the `acquisition` gate alone and X-10 added to carry the
      `total_measurable` bound, with `_charged_rows`, `_measurable`,
      `_cap_verdict` and `_moving_clause` factored out so both checks measure
      through one path (FIND-Q9-45). X-06 gains the `superseded_by`
      cross-check in both directions and the `_corroborates` helper
      (FIND-Q9-43). X-08's split notice no longer claims a verdict it did not
      emit (FIND-Q9-46). The seal-epoch notice cites §6.2 (FIND-Q9-48). The
      `run_all` error-path id derivation was `.replace("X0", "X-0")`, which
      yields `X10` for the first two-digit check; it is now zero-padded
      correctly.
  - path: src/aief_exec/scope.py
    change: |
      `ACQUISITION_EMITTERS` added as the one declaration of the acquisition
      component set, `ACQUISITION_COMPONENTS` derived from its key order, and
      `acquisition_units` rewritten to iterate it rather than hardcode the same
      set a second time (FIND-Q9-44). A component declared with no emitter is
      now a `ScopeError` naming it, not a silent zero. `ChargedContext`'s
      `revision` and `total_measurable` docstrings said X-08 carries the bound;
      they name X-10 (FIND-Q9-45/46, the same defect class in a docstring).
  - path: src/aief_exec/records.py
    change: |
      `ResultRecord.superseded_by` added - the predecessor's half of the
      supersession link, present in every superseded record since R-001 and
      read by nothing until now (FIND-Q9-43).
  - path: src/aief_exec/graph.py
    change: |
      `seal_epoch`'s docstring cites EXECUTION_ARCHITECTURE.md §6.2, which
      records the constant, rather than §6.1, which does not (FIND-Q9-48).
  - path: src/aief_exec/__main__.py
    change: |
      The `check` command's help line reads X-01 to X-10, and the `scope`
      command's TOTAL line no longer says "X-08 BOUNDS this ... and fails
      separately" - the same FIND-Q9-46 defect class in the CLI, naming the
      wrong check after the split and asserting a verdict it had not tested. It
      names X-10 as the check that compares the figure and asserts nothing about
      the outcome.
  - path: tests/test_exec_checks.py
    change: |
      Eleven tests added and five restated. `TestX06SealEpoch` gains the
      FIND-Q9-43 attack end to end plus four cases of the cross-check;
      `TestVer009Regressions` and `TestLiveRepositoryOpenFailures` gain the
      FIND-Q9-45 monotonicity property, the FIND-Q9-46 notice-honesty test and
      the X-08/X-10 separation. N-6 lists every restatement with its reason.
  - path: tests/test_exec_scope.py
    change: |
      `TestAcquisitionSurfaceIsOneDeclaration` (six tests) and
      `TestArchitectureCitations` (three tests) added, for FIND-Q9-44 and
      FIND-Q9-48 respectively.
  - path: .ai/project/EXECUTION_ARCHITECTURE.md
    change: |
      §6.3 added - the two-sided supersession cross-check and its residual.
      §11.1 added - why the gate and the bound are two checks. §12 rows for
      X-06 and X-08 restated and a row for X-10 added. §14 item 12 restated and
      item 13 plus §14.1 added - the mutation kill floor, its membership, why it
      is not a defect and how to recompute it.
  - path: .ai/project/results/R-013.md
    change: This record.
  - path: .ai/project/results/R-012.md
    change: status CURRENT -> SUPERSEDED, superseded_by R-013. No other field touched.
  - path: .ai/project/tasks/T-004.md
    change: |
      consumes and read_scope.dependency re-pointed R-012 -> R-013, with the two
      checkpoint lines naming R-012.
  - path: .ai/project/tasks/T-001.md
    change: R-013 added to `produces`.

validation:
  - check: X-01 through X-07
    outcome: |
      PASS on the live repository, with notices printed. Reproduce with
      `PYTHONPATH=src python -m aief_exec check`, which reports 7 of 10 and
      exits 1. X-02 and X-06 went FAIL mid-pass, exactly as R-012 N-7 predicts
      they must - this pass rewrote six files R-012 pinned - and are cleared by
      this record, not by any change to a check.
  - check: X-06 under the FIND-Q9-43 attack
    outcome: |
      Reproduced on a throwaway copy before the repair was accepted, and now
      guarded by a test. Deleting `supersedes: R-011` and its `supersedes_seal`
      block from R-012 and then appending `TAMPERED: ...` to R-011 returned X-06
      to PASS with a single notice and no alarm of any kind. Against this layer
      the same edit is an X-06 FAIL naming R-011, its declared successor and the
      contradiction, before the predecessor is touched at all.
      `tests/test_exec_checks.py::TestX06SealEpoch::
      test_deleting_the_successors_supersedes_does_not_disarm_the_seal` runs the
      attack in both steps.
  - check: X-08
    outcome: |
      FAIL, on six rows - the `acquisition` gate alone. T-002 (TF-1, TF-2),
      T-003 (TF-1), T-004 (TF-1, TF-2), T-006 (TF-1). The same six (task,
      family) pairs R-012 reported, against the same caps. Four of the six
      figures are unchanged to the token; T-004's two moved, and are not quoted,
      for the reason given under AC-1. T-001's breach is reported and not gated
      because T-001 is COMPLETE.

      T-005 has NO X-08 row, before or after its deliverable exists. That is the
      property FIND-Q9-45 was raised on and it is now the check's verdict and
      not merely its detail list: measured both ways round on a throwaway copy,
      X-08's status, details and gated figure for T-005 are identical.
  - check: X-10
    outcome: |
      FAIL, on eight rows - the `total_measurable` bound. T-002 (TF-1 12880,
      TF-2 16680), T-003 (TF-1 8193), T-004 (both families, not quoted - see
      AC-1), T-005 (TF-1 7161, TF-2 9763), T-006 (TF-1 12154).

      Four of these were reported by R-012 under X-08; four - T-002, T-003 and
      T-006 - are breaches R-012 measured and then suppressed, and told the
      reader had "failed separately" when they had not. That is FIND-Q9-46 and
      the suppression is gone with it. No new cost was found and no cap was
      changed: `total_measurable` for those tasks equals their `acquisition`
      exactly, which the row states in terms.
  - check: X-09
    outcome: |
      FAIL, on ten rows, byte-identical to R-012's. Five mode-1 rows, five
      mode-5 rows on T-001. Neither is repaired here - both turn on open
      decision A4. No write_scope was widened; X-09 must still fail on
      T-002...T-006 while A4 is open, and it does.
  - check: supersession seal over R-012
    outcome: |
      Verified as present, not by the alarm's silence.
      `graph.supersession_seal` resolves R-012's sealer to R-013 and its sealed
      digest to be67c646ca12cad54a6c880662ce4d83a3edf9f07290bcb41d74ab585200197e,
      equal to the recomputed DC-1 of R-012 as it stands after its two
      supersession fields were written - which is when the digest was taken.
      `seal_epoch` is the declared constant S-2026-08-09-06 and this record's
      session is S-2026-08-09-12, so an omitted seal here would have been an
      X-06 failure and not a notice. R-012's own `superseded_by: R-013` and this
      record's `supersedes: R-012` now corroborate each other, which is the
      FIND-Q9-43 repair applied to the link this record creates.
  - check: the seal chain behind R-012
    outcome: |
      Unbroken and re-verified rather than assumed. R-009's seal over R-008,
      R-010's over R-009, R-011's over R-010 and R-012's over R-011 all still
      equal the recomputed DC-1 of their sealed files. R-001 and R-007 remain
      unsealed and unsealable - see U-4. Every superseded record in the chain
      declares a `superseded_by` that its successor corroborates, so the new
      cross-check is blind for no link;
      `tests/test_exec_checks.py::TestX06SealEpoch::
      test_the_live_chain_declares_the_link_from_both_sides` asserts that on the
      live records.
  - check: pytest tests/test_exec_*.py
    outcome: |
      275 passed, 0 failed - 257 before this pass, 18 added, none removed, none
      skipped, xfailed or weakened. Collected per file: test_exec_records 31
      (unchanged), test_exec_scope 73 (was 64, +9), test_exec_graph 64
      (unchanged), test_exec_checks 107 (was 98, +9). Reproduce with
      `PYTHONPATH=src python -m pytest tests/test_exec_*.py -q`. Five tests were
      restated - two of them renamed, which is why the added count is 18 and not
      20 - each carrying in its own body what changed and why; N-6 lists them.
  - check: new tests fail against the unrepaired behaviour
    outcome: |
      Established by mutation rather than asserted. Six mutants, each reverting
      exactly the mechanism one fix introduced, applied to `src/aief_exec/` and
      reverted by digest-verified restore. Every one is killed. N-8 carries the
      table, and every count in it has the three-test pin floor subtracted -
      see N-10 and EXECUTION_ARCHITECTURE.md §14.1.

consumed:  [R-012]

notices:
  - id: N-1
    note: |
      IMPLEMENTED PENDING INDEPENDENT VERIFICATION. This record closes nothing
      and certifies nothing. `VER-009` pass 6 raised FIND-Q9-43, 44, 45, 46, 47
      and 48; this pass repairs five and documents the sixth, which is a
      property of the design rather than a defect in it. Whether each repair
      holds is `qa-engineer`'s to decide, and LAW-05 requires the closing pass
      to be taken by a role that authored none of this.
  - id: N-2
    note: |
      What is deliberately not pinned, unchanged in reasoning from R-012 N-2.
      VER-009 is T-004's deliverable and pinning it made R-007 self-staling
      (FIND-Q9-13). Task records and EXEC.md are mutable registers and pinning
      them froze the backlog (FIND-Q9-14). STATE.md, BINDING.md and ROSTER.md
      stay dropped for the same reason.
  - id: N-3
    note: |
      src/aief_stage6/digests.py and tokenizers.py are pinned as inputs although
      they lie in another task's write scope, unchanged from R-012 N-3. It is
      the same coupling U-8 records as an undetected hazard, met from the one
      side this layer can see.
  - id: N-4
    note: |
      The seal over R-012 is taken over R-012 **after** its two supersession
      fields were written. A seal taken before the closing edit seals a version
      that no longer exists.
  - id: N-5
    note: |
      Identifier collision, carried forward from R-012 N-5. `A4` in X-09's
      notices and in EXECUTION_ARCHITECTURE.md §14 item 9 is the open
      architecture decision on whether `produces` carries a write grant. `A4` in
      STATE.md and AIEF-AMD-013 AMD-41 is a role identifier.
  - id: N-6
    note: |
      The five restated tests, named so that no reader has to infer from a green
      suite which assertions moved. None was skipped, xfailed, deleted or
      reduced to a bare status comparison, and each carries in its own body what
      changed and why. `VER-009` §34.3 named the first two in terms, as the
      tests that locked FIND-Q9-45 in as intended.

        test_exec_checks.py::TestVer009Regressions::
          test_x08_measures_an_existing_deliverable_without_gating_on_it
          test_x10_fails_on_the_cost_the_gate_excludes
            (was test_x08_fails_on_the_cost_the_gate_excludes)
          test_the_bound_is_not_suppressed_when_revision_is_zero
            (was test_x08_does_not_double_report_when_revision_is_zero)
        test_exec_checks.py::TestLiveRepositoryOpenFailures::
          test_x08_open_because_the_measurement_is_now_honest
        test_exec_checks.py::TestX09PublicationReachability::
          test_x09_is_registered_in_the_campaign

      What changed in each, and why:

        * `test_x08_measures_an_existing_deliverable_without_gating_on_it`
          asserted that X-08's status flips PASS -> FAIL when the task writes its
          own deliverable, with the gate under cap both times. That is a check
          verdict a task moves by doing its own work - FIND-Q9-35 at the boolean
          instead of at the number - and asserting it as intended is why the
          suite could not see FIND-Q9-45. It now asserts that X-08's status,
          details and gated figure are IDENTICAL before and after, and that
          X-10's status is the thing that flips. Strictly more, and it is the
          property a dispatch gate has to have.
        * `test_x08_fails_on_the_cost_the_gate_excludes` -> `test_x10_...`. It
          asserted the bound row and the absence of an acquisition row in ONE
          details list, which is the arrangement that let one boolean carry two
          quantities: it passed just as happily when X-08's FAIL was caused
          entirely by the bound. Every original assertion survives, against
          X-10's row, and the separation is now structural - X-08 PASSes at the
          same instant.
        * `test_x08_does_not_double_report_when_revision_is_zero` ->
          `test_the_bound_is_not_suppressed_when_revision_is_zero`. This is the
          one restatement that INVERTS an assertion, and the reason is given in
          the test body as well as here. It asserted a suppression whose stated
          ground was that one overrun printed twice in one details list reads as
          two findings. That ground died with the split. Keeping the suppression
          would have made X-10's boolean a function of whether `revision`
          happened to be zero - and `revision` is the non-monotonic quantity, so
          the check would have gone silent on a real breach because the task had
          not yet written a deliverable. The name changed with the property,
          which is the only honest thing to do when a node id states something
          no longer claimed.
        * `test_x08_open_because_the_measurement_is_now_honest` asserted that
          X-08's details partition into two verdicts distinguishable by
          substring. It now asserts that X-08's details are acquisition rows and
          nothing else, and the bound half moved to a new sibling test,
          `test_x10_open_on_the_cost_the_gate_excludes`, which carries every
          assertion the bound half used to make.
        * `test_x09_is_registered_in_the_campaign` asserted the campaign ids are
          `X-01`...`X-09`. It now asserts zero-padded two-digit ids through
          `X-10`, which is what caught the `run_all` error-path id derivation
          emitting `X10`.

      Two fixtures changed shape rather than assertion.
      `TestX06SealEpoch.SEALED` gains a `superseded_by` line and `_chain` writes
      the back-link on every superseded record, because the live records have
      carried that field since R-001 and a fixture without it cannot exercise
      the cross-check that reads it. The missing-back-link case became a test of
      its own rather than the fixture's silent default.
  - id: N-7
    note: |
      Ordering, unchanged in mechanism from R-012 N-7 and load-bearing again.
      A record that pins the layer is staled by any change to the layer, so the
      code and test work landed first, in full; the digests were taken after it
      and this record was published from those digests. Nothing after that step
      touches a pinned path - T-001.md and T-004.md are deliberately unpinned
      (N-2), and R-012's status edit precedes the seal that covers it.
  - id: N-8
    note: |
      Why the added tests are believed to bite, established by mutation on the
      live `src/aief_exec/` and not by inspection. Each mutant reverts exactly
      the mechanism one fix introduced; every source file was restored by
      digest-verified overwrite afterwards and the six DC-1 values were
      re-confirmed identical before this record was written.

      **Every count below has the three-test pin floor subtracted** - see N-10.
      The raw pytest count for each row is three higher.

      | Mutant | Reverts | Finding | Kills (floor removed) |
      |---|---|---|---|
      | W1 | the `superseded_by` cross-check deleted from X-06 | Q9-43 | **4** |
      | W2 | the bound folded back into X-08, X-10 emptied | Q9-45 | **8** |
      | W3 | X-10 removed from the `ALL` registry only | Q9-45 | **3** |
      | W4 | the "BOUNDED ... and failed separately" notice restored | Q9-46 | **1** |
      | W5 | `acquisition_units` hardcodes its kinds; `telemetry` added to the constant | Q9-44 | **4** |
      | W6 | the §6.1 citation restored in graph.py and checks.py | Q9-48 | **2** |

      W4 is deliberately narrow - it changes one sentence in one notice, and one
      test asserts that sentence is true of the tree. A single kill is the
      correct score for a single-sentence defect, and the test that kills it is
      `test_the_split_notice_asserts_no_verdict_it_did_not_emit`, which checks
      the claim against the tree rather than checking that the claim is present.

      W5 is the mutant `VER-009` §35.2 recorded as **MU14, the only survivor of
      a twenty-one mutant campaign**. It is killed four times over now.
  - id: N-9
    note: |
      Reproduce the mutation campaign: for each row of N-8, apply the single
      textual revert to the named function, run
      `PYTHONPATH=src python -m pytest tests/test_exec_*.py -q`, subtract three
      from the failure count, and restore. No mutant is left in the tree and
      every restored file was re-digested before this record was written.
  - id: N-10
    note: |
      **The mutation kill floor is 3 and every score in N-8 has it subtracted.**
      `VER-009` FIND-Q9-47. This record pins `src/aief_exec/*.py` and
      `tests/test_exec_*.py`, and three live-tree tests recompute those pins, so
      a semantically inert edit to any pinned source kills three tests:

        test_exec_checks.py::TestLiveRepositoryOpenFailures::
          test_x02_open_on_the_consumer_of_a_staled_result
          test_x06_open_on_the_result_that_pins_the_layer_it_describes
        test_exec_graph.py::TestLivePlan::
          test_the_live_dependency_state_is_derived_end_to_end

      Measured directly this session by appending an inert comment to
      `scope.py`: 3 failed, 272 passed, and the three are exactly those names.

      This is not a defect and was not repaired. Pinning the layer is what makes
      an unpublished change to it detectable rather than silent, and it is the
      same mechanism §6.1 relies on. What it means is that a mutant reported as
      "3 failures, KILLED" has in fact SURVIVED. It is documented at
      EXECUTION_ARCHITECTURE.md §14.1 with its membership, the reason it must
      not be eliminated, and the command that recomputes it.

unresolved:
  - id: U-1
    statement: |
      X-09 fails on ten rows and neither mode may be repaired from here.
      Unchanged from R-012 U-1 in every particular. Mode 1: T-002 through T-006
      each declare `produces: R-nnn` with no write_scope pattern reaching
      `.ai/project/results/R-nnn.md`. Mode 5: T-001's write_scope
      `.ai/project/results/**` reaches R-002 through R-006. Both turn on open
      architecture decision A4, recorded at EXECUTION_ARCHITECTURE.md §14 item
      9. This pass widened no write_scope and took neither repair.
    owner: project-manager
    blocks: X-09, AC-1
  - id: U-2
    statement: |
      X-08 fails on six rows and X-10 on eight, and no cap was touched.

      What changed from R-012 U-2 is which check owns which failure, and that
      four suppressed breaches are now visible. The gate rows are the same six.
      The bound rows are eight rather than four because R-012 emitted the bound
      only where `revision` was non-zero, which withheld the breaches of T-002,
      T-003 and T-006 while its notice told the reader they had "failed
      separately" - FIND-Q9-46. Their `total_measurable` equals their
      `acquisition` exactly, so nothing new is charged; four real breaches
      simply stopped being hidden.

      The substance of the question for project-manager is unchanged and is
      sharper for being split across two booleans: T-005 passes the gate and
      fails the bound, so "what should a cap bound" cannot be answered by
      looking only at rows that were already red. Whether `total_measurable`
      deserves a cap of its own remains undecided; the repository declares one
      `context_budget` per task and inventing a second here would be resolving
      by assumption (LAW-12).
    owner: project-manager
    blocks: X-08, X-10, AC-1
  - id: U-4
    statement: |
      R-001 and R-007 are unsealed and cannot be retro-sealed: their successors
      were published before the field existed. The seal epoch makes that a
      notice rather than a failure, and §6.3's cross-check deliberately accepts
      `supersedes` OR a seal as corroboration so that it does not fail them
      retroactively.

      Two residuals remain on the seal control, both narrowed rather than
      closed by this pass.

      First, carried forward: a successor declaring a `produced_by.session`
      earlier than the epoch still evades the seal rule. That is a false session
      identifier, a different defect from an absent seal, and nothing here
      detects it. `VER-009` §31.1 confirmed it live as D2.

      Second, new with the FIND-Q9-43 repair: a tamperer who strips
      `superseded_by` from the predecessor as well as `supersedes` and the seal
      from the successor removes both halves of the link, and the two records no
      longer contradict each other. What remains is a record declaring
      `status: SUPERSEDED` with no successor anywhere, reported as unsealed and
      not failed. Making that a failure means deciding that a record may not be
      retired without a successor, which is a schema question and not this
      pass's to answer. The attack now costs three edits across two files
      instead of two edits in one file.
    owner: chief-systems-engineer
  - id: U-5
    statement: |
      Register drift in the task records, carried forward from R-012 U-5 and
      still not this pass's to restate. T-001's deliverable list still names two
      prose entries that resolve to no path; its AC-1 still reads `X-01 through
      X-08`, which this pass makes one check staler rather than two. T-004's
      `inputs` still names R-001, its AC-3 still reads "R-008 is confirmed
      CURRENT", its AC-4 still names the R-007 finding closures and its
      escalation condition still names R-001. Only `consumes`, the coupled
      `read_scope.dependency` and the two checkpoint lines were re-pointed.
    owner: project-manager
  - id: U-6
    statement: |
      F-007 remains open with chief-systems-engineer - an authority citation is
      checked for form and never for referent. Unchanged by this pass.
    owner: chief-systems-engineer
  - id: U-8
    statement: |
      FIND-Q9-38 E4 is NOT repaired and is disclosed rather than closed,
      unchanged from R-012 U-8. A task's runtime dependencies are on no declared
      surface, so `classify T-002 T-004` is PARALLEL while T-002 writes
      `src/aief_stage6/**`, which holds the tokenizer backend every figure T-004
      reports flows through. Closing it means deriving an import graph, which is
      a design decision with its own failure modes. Disclosed at
      `graph.read_surface`, EXECUTION_ARCHITECTURE.md §14 item 10 and
      `tests/test_exec_graph.py::TestReadSurface::
      test_runtime_tooling_stays_undetected_and_is_disclosed`.
    owner: chief-systems-engineer
  - id: U-9
    statement: |
      FIND-Q9-38 E5, undeclared observation, is unchanged and remains a limit of
      the repository rather than of this layer: there is no execution trace, so
      what `pytest tests/` actually read is not recoverable.
    owner: software.software-engineer
  - id: U-10
    statement: |
      The gate is honest about not being invariant, and that is a disclosure
      rather than a repair, unchanged from R-012 U-10. FIND-Q9-36 has two lawful
      fixes and the repository chooses neither: exclude self-written paths from
      `acquisition`, or forbid a task's own record from lying inside its write
      scope.

      What FIND-Q9-45 adds, and what this pass did about it: the undecided
      question had reached the CHECK's verdict through `total_measurable`. That
      part is now contained - the non-monotonic quantity decides only X-10's
      boolean, which is named for it - but the residual movement inside
      `acquisition` itself is untouched and still real. 34% of T-001's gated
      figure and 24% of T-002's are self-written, printed on every run.
    owner: chief-systems-engineer
  - id: U-11
    statement: |
      The suite's green state still depends on a concurrent session's
      uncommitted file. `VER-009` FIND-Q9-47, first limb:
      `test_exec_scope.py::TestBudgetSplit::
      test_the_live_t005_shape_reproduces_the_incident` asserts
      `cc.non_monotonic == ["tests/test_stage6_crash_trials.py"]`, and that file
      is T-005's in-flight deliverable, untracked and produced by another
      session. Removing it turns the suite red.

      Not repaired here, and the reason is worth stating rather than leaving as
      an omission: `tests/test_stage6_*.py` is outside this pass's write scope,
      and the test in question is a live-tree assertion whose value is exactly
      that it is live - it reproduces the incident on the real records. Making
      it hermetic would be the FIND-Q9-36 fixture-avoidance pattern, met from
      the other side. Whether a live-tree test may depend on another task's
      in-flight output is a question about the test contract, not a coding
      decision, and it is recorded here for project-manager rather than resolved
      by assumption.

      The second limb of FIND-Q9-47 - the three pin-drift tests - is not a
      defect and is documented rather than repaired: see N-10 and
      EXECUTION_ARCHITECTURE.md §14.1.
    owner: project-manager

conclusion: |
  The execution layer with the three MAJOR and two MINOR findings of `VER-009`
  pass 6 corrected and the third MINOR documented. IMPLEMENTED PENDING
  INDEPENDENT VERIFICATION - nothing here is closed and nothing here is
  self-certified.

  WHAT THIS PASS DID

  1. FIND-Q9-45, the boolean-irrelevant gate, repaired by splitting the check.
     `total_measurable >= acquisition` always, both were compared against the
     same cap, and both landed in one `details` list, so X-08 failed if and only
     if some `total_measurable` breached: the gate contributed nothing to the
     verdict a machine reads, and the only discriminator was an English
     substring. Live, T-005 had zero X-08 rows before it wrote its deliverable
     and two FAILs after, gate under cap both times - the FIND-Q9-35 defect
     reinstated at the check's boolean.

     The bound is now X-10, a check of its own, named and documented for the
     non-monotonic quantity it carries. X-08 keeps the gate alone and is
     monotone with respect to deliverable creation. Both may fail
     independently and `row["id"]` says which did. Neither was folded back
     together, no second cap was invented, and no `context_budget` value
     changed. The suppression that hid four breaches went with the split,
     because keeping it would have made X-10's own verdict a function of
     whether `revision` happened to be zero.

  2. FIND-Q9-43, the disarmable seal, narrowed by reading the evidence the
     repository already held. The rule was guarded by
     `if result.supersedes and not <seal>`, and `supersedes` is a line in a file
     the tamperer is already editing - deleting it and the seal returned X-06 to
     PASS and left the predecessor freely rewritable. Meanwhile R-011 carried
     `superseded_by: R-012` and no code in the package had ever parsed the
     field. X-06 now compares both declarations of every link, in both
     directions, and a successor that denies the supersession its predecessor
     declares is a detail-level FAIL. Corroboration is `supersedes` OR a seal,
     not both, so R-001 and R-007 are not failed retroactively. The remaining
     three-edit attack is disclosed at U-4 and §6.3 rather than left to be
     found.

  3. FIND-Q9-46, the notice that asserted a falsehood, corrected. Every task in
     every state was told its `total_measurable` had "failed separately" -
     unconditionally, with no test on a breach and none on the suppression that
     withheld it. It was live-false for T-002, T-003 and T-006. The clause now
     states the number and names the check that compares it, which is true of
     every task in every state, and a test checks the claim against the tree.

  4. FIND-Q9-44, the untied declarations, repaired by removing one of them.
     `ACQUISITION_EMITTERS` is the single declaration; `ACQUISITION_COMPONENTS`
     is its key order; `acquisition_units` iterates it. A name cannot be added
     to the set without an emitter, and a component with no emitter is a
     `ScopeError` naming it rather than a silent zero. Mutant MU14 - the only
     survivor of `VER-009`'s twenty-one mutant campaign - is killed four times.

  5. FIND-Q9-48, the wrong citation, corrected in both places to §6.2, with a
     test that reads the architecture document so the pointer cannot rot
     silently.

  6. FIND-Q9-47, the kill floor, documented rather than eliminated. The floor is
     3, its membership is named, and EXECUTION_ARCHITECTURE.md §14.1 states why
     pinning the layer is the design, what a "3 failures, KILLED" report
     actually means, and how to recompute the value when the pin set changes.
     Every count in N-8 has it subtracted. The other limb of FIND-Q9-47 - the
     live T-005 test that depends on another session's uncommitted file - is
     NOT repaired and is escalated at U-11.

  7. Nothing else. No cap was changed, no write_scope was widened, no check was
     removed, no test was skipped, xfailed, deleted or weakened, EXEC.md was not
     written, and no path under `.ai/core/`, `framework/`, `spec/`,
     `src/aief_stage6/` or `tests/test_stage6_*.py` was touched.

  WHAT A DOWNSTREAM AGENT NEEDS TO KNOW

  1. Read `.ai/project/EXEC.md` after boot - a bounded index, one task id per
     line, naming the state of every task.
  2. Read the single `tasks/T-nnn.md` you are assigned. Read no other.
  3. Run `python -m aief_exec brief T-nnn` for the resolved minimum context, and
     `python -m aief_exec scope T-nnn` to see the budget quantities.
  4. **There are two budget checks and they answer different questions.** X-08
     is the dispatch gate: it bounds what you must hold before you start, and
     its answer does not move when you do your work. X-10 bounds everything this
     layer can measure, including the deliverables you must read to rewrite -
     its answer DOES move when you write them, which is why it is not a gate and
     why its name says so. Failing X-10 while passing X-08 means your total load
     is over the cap and your dispatch is still lawful.
  5. Classification is computed, never declared. So is supersession, and so is
     the agreement between a record's `superseded_by` and its successor's
     `supersedes` - if you supersede a record, write both halves.
  6. `check` reports 7 of 10 and exits 1. X-08, X-09 and X-10 are open,
     enumerated under `unresolved` with the record and the role that owns each
     repair. None is cleared by widening a scope or raising a cap, and none was.
  7. **A PARALLEL verdict is a claim about declared surfaces only.** It does not
     see the code your task imports (U-8) and it does not see what a command you
     run reads (U-9).

  MEASURED EFFECT

  T-002's mandatory scope resolves to TF-1 4,910 / TF-2 6,411 against TF-1
  66,437 / TF-2 87,780 for the same files unanchored - 13.5x and 13.7x.
  Reproduce with `python -m aief_exec measure T-002`.

  No sweep figure is stated here. FIND-Q9-31 established that the envelope
  contains the result and verification registers, so it moves whenever an audit
  or a result is filed - including by the filing of this record. Run the command.
```

## Notes

**Immutability.** Seven inputs and eleven deliverables are DC-1-pinned, and `R-012` is
sealed by a nineteenth digest taken over the whole record after its supersession fields were
written. `X-06` recomputes the eighteen pins on every run and compares the seal; any drift
makes this result STALE, and any subsequent edit to `R-012` raises
`REWRITTEN AFTER SUPERSESSION`. `R-012` also declares `superseded_by: R-013`, and `X-06`
now fails if this record ever stops corroborating it — the seal is no longer disarmable by
deleting the `supersedes` line above.

**Status.** `IMPLEMENTED PENDING INDEPENDENT VERIFICATION`. This record does not close a
finding and does not certify itself. `T-001`'s verifier is `qa-engineer`; `LAW-05` requires
the pass that disposes the findings above to be taken by a role that authored none of it.
