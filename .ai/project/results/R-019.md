# R-019 — Independent Verification of the C6 Instrument and the VER-016 Residue

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-019
produced_by:
  task:      T-010
  role:      qa-engineer
  session:   S-2026-08-10-05
status:      SUPERSEDED
superseded_by: R-021
supersedes:  null

inputs:
    - path: .ai/project/results/R-018.md
      digest: 127d4f6c325ab53e860faa1e8cff0db971cacb5f9b40a7640cf6e54ba7a32f72

deliverables:
    - path: .ai/project/verification/VER-017_Confirmatory_Round_On_The_C6_Instrument_And_VER-016_Residue.md
      digest: bdb16753e603a4f8ec7ebc38dd8ab77927cc206b93f113f1ce2231e126e3402a

conclusion: |
  CLEARED, 12 PASS 0 FAIL. LC-M04-EXIT C6 is satisfied for all four gated ECRs and the gate
  computes LC-M04 CAD-READY: YES at exit 0. VER-017 supersedes VER-014, VER-015 and VER-016,
  each sealed at the DC-1 of the bytes retired. Four findings recorded, none against the
  subject: N-1 this round is role-separated but not session-separated; N-2 T-001's result
  write scope reaches three more foreign records; N-3 R-016 was never filed; N-4
  aief_approval verify exits 1 on two pre-existing defects. CAD is authorised to begin.
```

---

## 1 · What was verified, and how

Twelve criteria, `W1`–`W12`. Every digest was recomputed by an implementation of `DC-1`/`DC-2`
written for this round **without importing `src/aief_*`**. Every check was **attacked** rather than
run: nine adversarial trials against the `C6` supersession relation in throwaway repository copies
invoking the real gate, the `ECR-D-010` clocking restored against `aief_clearance`, the `C7`
evasion re-filed, the parameter CSV mutated and deleted, and `ECR-D-002`'s disposition blanked with
a clearing `C6` in place.

**9 of 9 supersession trials behaved as required.** The three that answer the obvious objection —
that a supersession relation is what someone wanting the gate open would build — are A2 (an
unsealed claim retires nothing), A3 (a seal over the wrong bytes fails) and A4 (a report rewritten
after being sealed **fails the gate**). The retired reports are harder to tamper with after this
ruling than before it.

## 2 · What was found

**Nothing against the subject.** All fourteen `VER-016` findings are accounted for by name: ten
repaired and verified here, one recorded with reasons (`F-08` → `OI-C-13`), one deferred to
`.ai/core/**` (`F-14`), and `F-12` repaired **and shown to have run in the opposite direction to
the one `VER-016` recorded** — the old predicate had been passing `ECR-D-001` on `VER-014`'s
`ECR-D-001 NOT CLOSED`, which it matched none of its keywords against. `OI-V-11` is discharged.

**One false result of my own, discarded rather than reported.** My first `ECR-D-010` trial made its
substitution through a PowerShell round-trip and reported `CLEARANCE OK` — which would have been a
finding that the check had stopped working. It had not; the round-trip damaged the file's encoding
and unrelated feature rows began reporting `skip`. Re-run byte-safely with an assertion on the
substitution, the check fails as required. Recorded in `VER-017` W6 because a verifier's own
instrument can produce a false negative as easily as the instrument under audit.

## 3 · What this round could not establish

`T-009` and `T-010` run in **one host invocation**. The declared roles, write scopes and session
identifiers differ and this verifier authored none of `T-009`'s artifacts, which satisfies the
repository's operative reading of `LAW-05` (`T-008` AC-5). It does not give cold context, and
`VER-017` §0b says so rather than covering the gap. Recorded as `N-1`; a cold-context ratification
is the residual, in the shape `CDR-C3` already records for the AIEF CDR.

## 4 · Exact next action

Nothing by this record. **OPEN FUSION 360.**
