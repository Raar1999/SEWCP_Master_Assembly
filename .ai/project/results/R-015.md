# R-015 — LC-M04 Specification Coherence Package

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-015
produced_by:
  task:      T-007
  role:      chief-systems-engineer
  session:   S-2026-08-10-01
status:      CURRENT   # re-pinned after APR-027 and the VER-015 repairs
supersedes:  null

deliverables:
    - path: spec/00_SEWCP-ENG-001_Architecture_and_Interface_Control.md
      digest: fa2a84ccf3a837176eade00c502916a7a1492c9c58785448a4c26ba0fbaab95d
    - path: spec/01_SEWCP-200_Cooling_Plate.md
      digest: 55b47ca30eeac99ca231d960a1066411b827bf6da139d5e7d178db6a182c3a39
    - path: spec/02_SEWCP-300_Heater_Plate.md
      digest: 0290580066829963de1c9bbbd059f5f088e442031d679cf4ddf5046693d66aef
    - path: spec/03_SEWCP-400_Chuck_Support_Ring.md
      digest: a2f951a1c749b688141d8245f8575ed6aabf28df03b55788a0d762f4e6c7dcbf
    - path: spec/06_SEWCP-700_Alignment_Pins.md
      digest: 75cda88184e5ae50acd05fb86dfb61ffc6238219462e8854120c05f14d04f396
    - path: spec/07_SEWCP-800_Vacuum_Port.md
      digest: 7558bc5b0f613ab9184d66dba6afd6674214d33302775eee2515bd25c8122afe
    - path: spec/README.md
      digest: 1d7720723ddd42028a9536ec20cfb50a9a8b803bc3172423b31f108a7f93416c
    - path: src/aief_approval/chain.py
      digest: be61c76c870166c634e2a43d56110c57f607b12ef927cc378b5973a2b62ac8b1
    - path: src/aief_gate/criteria.py
      digest: d60b10fd1a762b0f47d11489ade7068d3d2eb52e69ca6a9760a84adf381af97b
    - path: src/aief_clearance/check.py
      digest: 1b5c08cccb2344f0ba53e61718ccdf708cdc23eef01525f6b88ae447c919fb85
    - path: tests/test_approval_chain.py
      digest: bef2b2c8c0f3f3d1371544d8c660011c7e58ab06eeab6ef0159604c99205328d
    - path: tests/test_clearance.py
      digest: 3f411f4c149adb6d547d52fbcbbabe03a47420f9fabf07af8c8a8679b3e6e4ae

conclusion: |
  Nine ECRs against the frozen specification are dispositioned, approved, applied and
  re-registered. LC-M04-EXIT C1..C5 and C7 PASS on computed evidence; C6 is reserved to
  T-008 under LAW-05 because this task authored every artifact under audit.
```

---

## 1 · What was inspected

`spec/00`–`spec/09`, `spec/README.md`, every record under `.ai/project/ecr/`, every approval
under `.ai/project/approvals/`, `FROZEN.md`, `GATES.md`, `OPEN_ITEMS.md` and its register, and
the SEWCP-200 CAD implementation package.

## 2 · What was changed

**Seven frozen artifacts**, each re-registered and bound by an approval: `spec/00`, `spec/01`,
`spec/02`, `spec/03`, `spec/06`, `spec/07`, `spec/README.md`. Approvals `APR-020`…`APR-026`.
Aggregate `c56e75bc…` → `55904b93…`.

**Nine dispositions**, each ruled by the human owner across three interactive decision pauses:

| ECR | Disposition |
|---|---|
| `ECR-D-001` | Already A; the recorded `spec/03` press-fit residual closed |
| `ECR-D-002` | Already A; its **unapplied half** completed — §6 step 3 still said *"10 W × 8 D"* |
| `ECR-D-003` | A — Ø10.0 coaxial port, channel locally deepened, SEWCP-201 transition joint |
| `ECR-D-004` | A — `CP-D26` slotted masked counterbore, `M5 × 25`, `HP-D12` blind 6.50 |
| `ECR-D-007` | A on all five actions; action 3 answered by Ø12.000 → Ø10.000 |
| `ECR-D-008` | A — `spec/06` governs; the whole ICD cell replaced |
| `ECR-D-009` | A — integral shouldered locator screw; `AP-D07`/`AP-D08` struck |
| `ECR-D-010` | A — top locators re-clocked to 75°/195°/315° |
| `ECR-D-011` | A — heater-groove keep-out, spiral routed tangentially |

**Five ECR records created that had never existed**: `ECR-D-003`, `-004`, `-006`, `-010`, `-011`.
`C3` and `C4` had been gate criteria for a dozen sessions with **no artifact to evaluate**, and
`C7` was *undecidable* rather than unsatisfied because `ECR-D-006` had no record to read.

**Schema conformance repaired on every ECR record.** Not one of the nine pre-existing records
carried `affected_artifacts`, `evidence`, `impact` or `requested_action`, all four of which
`SCH-ecr` requires — and `C7` is defined over `affected_artifacts`.

## 3 · What was NOT changed

Nothing under `.ai/core/`. No Stage 6 execution, no `core/MANIFEST.lock`, no ledger entry, no
tag, no push. `ECR-D-006` is recorded, not dispositioned — it is reserved to the human owner and
is not under `spec/**`. `framework/framework.manifest.json` was not touched.

## 4 · Root causes, and where each was repaired

Three defects shared one shape: **a declared property that nothing computed.**

1. **Approval supersession.** `LAW-10` binds an approval to a whole-file digest, so each new
   approval on a file voided every earlier one. `C1` was FAIL for a mechanical reason and the
   gate was unsatisfiable for any file carrying two ECRs. Repaired at the owner: the human owner
   ruled the relation, `GATES.md` states it, `aief_approval` computes it.
2. **The gate itself.** `C1`–`C7` were declared *"binary and evidence-based"* and evaluated by
   hand. `aief_gate` computes them and exits non-zero.
3. **Feature clearance.** `spec/00` §3.2 declared its map *"binding on every component"* and
   closed with *"No conflicts"* over four hand-picked pairs, omitting the Ø260 BC locators
   entirely. `aief_clearance` computes every pair — and **found `ECR-D-010`**.

Two of the three checks found real defects they had not been told about: `aief_approval`
independently re-derived `ECR-D-006`, and `aief_clearance` found the locator/choke collision.

## 5 · Evidence

```
PYTHONPATH=src python -m aief_gate            C1-C5, C7 PASS; C6 on VER-015
PYTHONPATH=src python -m aief_approval verify  every spec/** chain resolves
PYTHONPATH=src python -m aief_clearance        CLEARANCE OK
PYTHONPATH=src python -m pytest tests/ -q      569 passed, 3 failed
```

The three failures are **pre-existing and on the Stage 6 track**, unchanged in kind from the
session baseline of 3 failed / 542 passed: the V-09 budget breach, `V-24`'s one unverifiable
registry row (`ECR-D-006`), and the pipeline stub that depends on both. All 27 tests added by
this task pass.

## 6 · Decisions required, and taken

Twelve, across three interactive pauses, all by the human owner: the supersession rule; the
stub bore geometry; the stub joint method; the choke fastener stack; the locator architecture;
the D-007 derived values; the pin material; the clocking remedy; the heater keep-out; and the
OD-wall remedy. Every option presented but **not** approved is recorded in the ECR it belongs
to, so a later reader can see what was rejected and why.

## 7 · Remaining dependencies

**One: `C6`.** Reserved to `T-008` / `VER-015` under LAW-05.

## 8 · Exact next action

Nothing by this task. After `C6`: **OPEN FUSION 360.**
