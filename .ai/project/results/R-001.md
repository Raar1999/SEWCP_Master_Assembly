# R-001 — Execution Layer Delivered

> **SUPERSEDED by [`R-007`](R-007.md).** Retained unedited as the record of what was
> published, per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.1:
> correction is by supersession, never by mutation. Only the two status fields above the
> fold were changed, which is what supersession is. **Do not consume this record.**
>
> `VER-009` FIND-Q9-3 found two numbers below already wrong at publication — the suite
> figure and the exec-test count — because this record was published while its own
> deliverables were still moving, and it pinned only `inputs`, so `X-06` could not see it.
> `R-007` pins deliverables as well.

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.

```yaml
result_id:   R-001
produced_by:
  task:      T-001
  role:      software.software-engineer
  session:   S-2026-08-09-01
status:      SUPERSEDED
superseded_by: R-007
supersedes:  null

inputs:
    - path: .ai/BOOT.md
      digest: 5a97d6ad763f8720b3e9e5c9cf9be6ad62f6ba953b5b70eb78a3624b580b82db
    - path: .ai/project/STATE.md
      digest: 1cde83cde8698ac758f043a04cede54e8e44258a16531c2a52312dfff56c175d
    - path: .ai/project/BINDING.md
      digest: 64a9ca216606e502ca186985dc4ef22d5f7fd0504ed281b392c84910ac81a15f
    - path: .ai/core/CONTEXT_TIERS.md
      digest: dad8a47ad994cce45820b9b89f016872f259b7b44b10e8a87ed42f32e31ade4f
    - path: .ai/core/schemas/SCH-task.schema.json
      digest: e874fdd70b56f55b6205e26c7ef537e090e46b5f05ab2964f8b1ee4ad6cccc80
    - path: framework/framework.manifest.json
      digest: 920eb6eec217732152c452d51f01e471940df6f2e2ffe608c377fccc37814090

affected:
  - .ai/project/EXECUTION_ARCHITECTURE.md
  - .ai/project/EXEC.md
  - .ai/project/tasks/T-001.md
  - .ai/project/tasks/T-002.md
  - .ai/project/tasks/T-003.md
  - .ai/project/tasks/T-004.md
  - .ai/project/tasks/T-005.md
  - .ai/project/tasks/T-006.md
  - .ai/project/results/R-001.md
  - src/aief_exec/__init__.py
  - src/aief_exec/__main__.py
  - src/aief_exec/records.py
  - src/aief_exec/scope.py
  - src/aief_exec/graph.py
  - src/aief_exec/checks.py
  - tests/test_exec_records.py
  - tests/test_exec_scope.py
  - tests/test_exec_graph.py
  - tests/test_exec_checks.py

validation:
  - check: X-01
    outcome: PASS
  - check: X-02
    outcome: PASS
  - check: X-03
    outcome: PASS
  - check: X-04
    outcome: PASS
  - check: X-05
    outcome: PASS
  - check: X-06
    outcome: PASS
  - check: X-07
    outcome: PASS
  - check: X-08
    outcome: PASS
  - check: pytest tests/
    outcome: 205 passed, 4 failed - the same four that failed before this task began

conclusion: |
  A bounded task-and-result execution layer is in place and operating on this
  repository's real backlog.

  WHAT A DOWNSTREAM AGENT NEEDS TO KNOW

  1. Read `.ai/project/EXEC.md` after boot. It is a bounded index, one task id
     per line, and it names the state of every task.
  2. Read the single `tasks/T-nnn.md` you are assigned. It carries objective,
     role, dependencies, read scope, write scope, forbidden paths, binary
     acceptance criteria, the assigned verifier, a context budget and a
     checkpoint. Read no other task record.
  3. Run `python -m aief_exec brief T-nnn` to obtain the resolved minimum
     context: anchored excerpts, not whole files, plus the conclusions of any
     results your task consumes.
  4. Classification is computed, never declared. `python -m aief_exec status`
     gives task states, derived blocking reasons and dispatch groups whose
     members are pairwise safe to run concurrently.

  MEASURED EFFECT, exact counts under TF-1 and TF-2

  For T-002, the representative backlog task, the mandatory read scope resolves
  to TF-1 4,295 / TF-2 5,626 against TF-1 65,822 / TF-2 86,995 for the same
  files unanchored - a 93 percent reduction under TF-1. The citation-chain route
  the repository forced before this layer existed measured TF-1 128,236.

  WHAT IS ENFORCED

  X-01 record conformance including LAW-05 verifier distinctness; X-02
  index/record bijection; X-03 read-scope resolution; X-04 write-scope
  containment against the framework-protected set; X-05 graph acyclicity;
  X-06 result currency by DC-1 recomputation; X-07 concurrent write safety;
  X-08 context budget. All eight PASS on the live repository, and each is
  additionally exercised against the exact fault it exists to catch.

  WHAT IS NOT CLAIMED

  Enforcement is by tooling and tests, not by a manifest-registered check, so
  nothing in the boot sequence compels it. Read scope is advisory to the agent
  and auditable afterwards - the host cannot intercept a read. The conflict test
  is conservative for paths that do not yet exist. Concurrent dispatch remains
  bounded by the global session lock at B4a for any task whose write scope
  includes STATE.md or the ledger.

  AUTHORITY POSITION

  No file under `.ai/core/` was edited, no manifest field changed, no frozen
  artifact changed, no check was added to the V-01..V-25 namespace, no ledger
  entry was written and Stage 6 was not executed. The extension fields are
  lawful under `SCH-task.schema.json`'s `additionalProperties: true`.

findings:
  - id: F-001
    severity: MEDIUM
    statement: manifest files[] declares state-register at project/STATE_REGISTER.md and metadata.reproducible.bounded_register_split declares the state pair, but the file does not exist in the working tree. V-03 as amended by AMD-49 requires the mapping to hold in both directions and V-20 requires declared paths to resolve.
    owner: chief-systems-engineer
  - id: F-002
    severity: MEDIUM
    statement: STATE.md lists CMP-BLOCK-006 under blockers and omits OQ-15, while OPEN_ITEMS.md - which STATE.md itself names authoritative - lists both under Closed. ENGINEERING.md section 7 also still describes CMP-BLOCK-006 as live.
    owner: project-manager
  - id: F-003
    severity: LOW
    statement: The four pre-existing test failures are unrelated to this task and were present before it began. Three are snapshot assertions superseded by AMD-42 and AMD-51; one is V-24 reporting the uncommitted AMD-014 work as unregistered. OI-C-09 already records all four.
    owner: software.test-engineer
```

## Notes

**Immutability.** The six `inputs` digests are DC-1 values pinned at publication. `X-06`
recomputes them against the working tree on every run; any drift makes this result STALE and
blocks `T-004`, which consumes it. Correction is by supersession, never by editing this
record in place.

**Findings F-001 and F-002 are reported, not fixed.** Both lie outside `T-001`'s write scope
and both are owned by roles other than this task's. Raising them here is the
`EXECUTION_ARCHITECTURE.md` §6 blocking-finding path in use.
