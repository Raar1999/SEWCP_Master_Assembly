# R-017 — Execution Layer, After the OI-C-12 Property Repair

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-014`](R-014.md)**, whose pinned test digests this task moved while repairing
> `OI-C-12`. `R-014` is retained unedited apart from the two supersession fields, and its DC-1
> as it now stands is sealed below.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-017
produced_by:
  task:      T-009
  role:      chief-systems-engineer
  session:   S-2026-08-10-04
status:      SUPERSEDED
superseded_by: R-023
supersedes:  R-014
supersedes_seal:
  path:   .ai/project/results/R-014.md
  digest: cb27a2aff813f2cde073e7388fe3c27f7afe2ddace391c50a9769621e755ea4a

deliverables:
    - path: .ai/project/EXECUTION_ARCHITECTURE.md
      digest: bb81925dc76f0163e790f572513caeaee1345bcb3b9e31b36d0095cc376da3e4
    - path: src/aief_exec/__init__.py
      digest: b8813ebc7fb18c820721517de771f9ed8ca224971824e2e9680f227ef5f77b69
    - path: src/aief_exec/__main__.py
      digest: 04d89d567dc1b3885024128e0d4cfb22a2e69f3083bfb69b332304dee6d32d96
    - path: src/aief_exec/records.py
      digest: dd89352f92a0f9e46342e09ebbd5370122cdade00816fa227d575d54bc0583a7
    - path: src/aief_exec/scope.py
      digest: 816f2d98d73be840cb3aad4a374eb7ee9c369f9bb449eae8c100dbead32d9b2a
    - path: src/aief_exec/graph.py
      digest: 0c9a596a11d5682e1c5dfd799b196d09c8d0f4934ffb5478c74b860e64524716
    - path: src/aief_exec/checks.py
      digest: e5f5b1103e6bd26fd28da8c5560f83c215af3bb61e9e05c46beb53c31aa65610
    - path: tests/test_exec_records.py
      digest: 322b719295755cb0c502ae7ea767d396259eca9fee55e0b141ea781ef3625778
    - path: tests/test_exec_scope.py
      digest: 556ca014861ceef67f0e9773f6bf071e8917b317ce558970d3955be5cc73c4ec
    - path: tests/test_exec_graph.py
      digest: 3b3e64b185f213eea73c68d04887865dcdc9123bd95e511d536fb7a1c2d51589
    - path: tests/test_exec_checks.py
      digest: 8b33e1640c01e8ba66de9d98c3ad45d53a741c1b42f6a65a5d04f2f65909ff70
    - path: tests/test_exec_supersession.py
      digest: 3f0fcf4ce5b4194c2254ef25f8154ab8ada61fd0b071afb382cfb90eb7c37c6a
    - path: tests/test_exec_admission.py
      digest: 85166a54ed18928111d34f86e24563f91ea865def2f98e99b9c21650d896a996

conclusion: |
  OI-C-12 is repaired at the property. Four exec-layer tests pinned a six-task, one-result
  snapshot of live project state and failed whenever the project grew; each now derives its
  expectation from the records. No exec-layer source changed - src/aief_exec/** is
  byte-identical to R-014. X-06 reports PASS.
```

---

## 1 · What changed, and why the counts were the defect

`OI-C-12` recorded four tests that *"pin a six-task snapshot of live, growing project state"* and
ruled the repair: **test the property rather than the count.** It also recorded why it was not
done then — the files are `T-001`'s deliverable and `R-014` pins their digests, so repairing them
stales `R-014`. That is discharged here by superseding `R-014` rather than by leaving the tests
broken.

| Test | Was | Now |
|---|---|---|
| `test_every_task_record_loads` | `set(tasks) == {T-001…T-006}` | equals the set of `T-*.md` **on disk**, and each record is keyed by its own id |
| `test_the_live_gap_between_declared_and_effective_is_five_tasks` | `len(producers) == 6`, fixed list of five | the gapped set is **derived from both sides** — producers whose declared write scope does not reach the record they publish |
| `test_the_live_dependency_state_is_derived_end_to_end` | `len(current) == 1` | `CURRENT` and `SUPERSEDED` **partition** the results, and every `CURRENT` record is the head of its own chain |
| `test_x09_guards_the_publication_channel_in_both_directions` | `reached == [R-002…R-006]` | the reached set is **derived** from `T-001`'s write scope against every other task's `produces` |

A fifth test, `test_x06_open_on_the_result_that_pins_the_layer_it_describes`, was **not** an
`OI-C-12` defect and is not repaired here. It asserts `X-06 PASS`, and it was failing because
`R-015` was genuinely stale. It passes now because the staleness was fixed, which is the test
working.

`len(current) == 1` deserves its own note: it was never a count of anything meaningful. It held
only while the repository had exactly one result chain. `R-015` opened a second, and this session
adds a third; a count cannot express *"one head per chain"* and the partition property can.

## 2 · What was NOT changed

**No exec-layer source.** `src/aief_exec/**` is byte-identical to `R-014`, and so is
`EXECUTION_ARCHITECTURE.md` — the six digests above reproduce `R-014`'s exactly. This result
exists because three **test** files moved.

Nothing under `.ai/core/`. No Stage 6 execution, no `MANIFEST.lock`, no ledger entry, no tag, no
push.

## 3 · Consequences, recorded

`T-004` consumed `R-014` and now consumes `R-017`; its `read_scope.dependency` and checkpoint text
are re-pointed with it, so `X-02` derives `READY` and agrees with `EXEC.md`.

`T-001`'s `.ai/project/results/**` write scope now reaches three more results it does not
produce — `R-017`, `R-018`, `R-019`. That is `FIND-Q9-42`'s converse, which `X-09` reports and
explicitly reserves to A4, and it deepens by three with every session that files results.
Recorded at `VER-017` **N-2**; not decided here.

## 4 · Verification

`T-009` authored this; `T-010` verified it. `VER-017` W10 and W11 obtained the currency and suite
figures by execution.

## 5 · Exact next action

Nothing by this record.
