# R-014 — Execution Layer, After the Pass-7 Supersession-Integrity Repair

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-013`](R-013.md)**, whose pinned architecture, layer and test digests this
> pass moved while executing the authorised Option-A mitigation for `VER-009` pass 7.
> `R-013` is retained unedited apart from the two supersession fields, and its DC-1 as it now
> stands is sealed below.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-014
produced_by:
  task:      T-001
  role:      software.software-engineer
  session:   S-2026-08-09-13
status:      CURRENT
supersedes:  R-013
supersedes_seal:
  path:   .ai/project/results/R-013.md
  digest: 9562d51ebb2723457d034330b9bc2232f106e13b47c1a3d78325653f7ca55348

inputs:
    - path: .ai/project/verification/VER-009_Independent_Verification_Execution_Architecture.md
      digest: f9e74cc2bdeb288894e0d23c97696f0d2a82a3c3b8565b9ba4085b4778592b27

deliverables:
    - path: .ai/project/EXECUTION_ARCHITECTURE.md
      digest: b67bfab68b328cc4833d1eb7fa208dbf5c30feb73528ef23fd7ea5c0054115a3
    - path: src/aief_exec/__init__.py
      digest: b8813ebc7fb18c820721517de771f9ed8ca224971824e2e9680f227ef5f77b69
    - path: src/aief_exec/__main__.py
      digest: 04d89d567dc1b3885024128e0d4cfb22a2e69f3083bfb69b332304dee6d32d96
    - path: src/aief_exec/records.py
      digest: 0b9a7670afa0e0d775209d1f82a460389f6280b3cf61b35b6172b255ebcee6c5
    - path: src/aief_exec/scope.py
      digest: 816f2d98d73be840cb3aad4a374eb7ee9c369f9bb449eae8c100dbead32d9b2a
    - path: src/aief_exec/graph.py
      digest: 0c9a596a11d5682e1c5dfd799b196d09c8d0f4934ffb5478c74b860e64524716
    - path: src/aief_exec/checks.py
      digest: ee65f32bd9ab0e58389a02602f8dddc35f6feb310d32102f4827e2f2119032fd
    - path: tests/test_exec_records.py
      digest: f49a7d8440a7aae42e18eeea91ad2ea0b29f0db2885f1e16d88c309954219269
    - path: tests/test_exec_scope.py
      digest: 556ca014861ceef67f0e9773f6bf071e8917b317ce558970d3955be5cc73c4ec
    - path: tests/test_exec_graph.py
      digest: e00e36cae3144789b51beea886bddfe9624652143d30ef7467a9f587595e4df4
    - path: tests/test_exec_checks.py
      digest: 1e8ffc9df413f39b21b8582f2e7b9a2c39d96136bdd49e47049f3b2ec2dd6108
    - path: tests/test_exec_supersession.py
      digest: cf657f24e4479a754bdfa52f2fc0fc125e8b90b116deb65dc9911260320f74d8

acceptance:
  - id: AC-1
    outcome: PASS
    evidence: |
      OPTION A, REQUIREMENT 1 - ONE AUTHORITATIVE SUCCESSOR RELATIONSHIP.

      `graph.link_of` is the single reading of a record's successor link and it
      reads both fields. `graph.successor_of`, `graph.supersession_seal` and
      X-06's cross-check all route through it. `checks._corroborates` - the
      second definition - is deleted; the site that called it now calls
      `graph.is_successor_link`, which is the same function `successor_of` is
      built from.

      The two representations can no longer create two independent links:

        * A record linked only by its seal path IS a successor. `successor_of`
          follows it, `supersession_seal` returns its digest, and
          `result_currency` compares it.
        * A record whose `supersedes` and seal name different predecessors is a
          detail-level FAIL naming both. The check does not choose a side.
        * Two records asserting the same predecessor is a detail-level FAIL.
          `successor_of` returns the first in sort order and that choice is
          never made silently.
        * A link naming a record that does not exist is a detail-level FAIL -
          the symmetric half of the `superseded_by` existence check, which the
          link direction never had.

      The seal path must be exactly `.ai/project/results/R-nnn.md`.
      `_corroborates` matched on the basename, which was tolerable while the
      match only confirmed a link something else had established.
  - id: AC-2
    outcome: PASS
    evidence: |
      OPTION A, REQUIREMENT 2 - THE REDUNDANT LIMB, AND WHICH ONE IT WAS.

      The authorisation permits removing the redundant seal-path limb IF AND
      ONLY IF it is genuinely redundant under the unified relationship. It is
      not, and the reasoning is recorded rather than the deletion performed.

      MU30 - deleting the seal-path limb of `_corroborates` - survived pass 6's
      suite, and `VER-009` read that as evidence the mitigation is a deletion.
      Deleting the limb restores a SINGLE-FIELD definition of a link, and a
      single editable field is the precondition of all four recorded disarms.
      The audit's own diagnosis says so directly: "`successor_of` is the single
      point of truth, it is derived from one editable field, and every seal
      comparison hangs off it. Nothing derives the link from the seal itself."
      Deleting the limb satisfies the letter of "one definition" by discarding
      the half that is not under the tamperer's hand.

      What was genuinely redundant is `_corroborates` ITSELF - the second
      definition, in a different module, of one relationship. That is removed.
      The seal-path limb is retained inside the single definition, where it is
      load-bearing: the field that carries the evidence is also a field that
      establishes the link, so neither can be deleted without the other becoming
      visible.

      The remaining integrity path is demonstrated, not asserted. Mutant MU-A1
      re-runs MU30 against the unified definition - `seal_target` always empty -
      and dies to 27 semantic test failures, against MU30's 0.
  - id: AC-3
    outcome: PASS
    evidence: |
      MU30 / FIND-Q9-51 / FIND-Q9-52 - CLOSED AT ROOT, demonstrated by executing
      the audit's own attacks against the repaired mechanism.

      `tests/test_exec_supersession.py`, 78 tests, is a negative suite written
      against the repair rather than around it. Every attack that can be run on
      the head and on an interior link is run on both, because FIND-Q9-52
      established the interior link is the invisible one.

        M1  delete `supersedes`, keep the seal   BLOCKED. The link survives, the
            seal is compared, and the rewrite it was meant to enable fires
            REWRITTEN. On an interior record the edit is caught immediately -
            editing R-101 at all breaks R-102's seal over it.
        M2  the seal-path limb (MU30)            KILLED, 27 semantic failures.
        M3  delete the seal                      FAIL on the successor, epoch
            rule. Delete both halves: FAIL on the predecessor's back-link.
        M4  change the successor target          FAIL, divergence.
        M5  change the corroboration target      FAIL, divergence.
        M6  make the two disagree                FAIL for every arrangement.
        M7  alter the predecessor after
            publication                          FAIL, REWRITTEN. FIND-Q9-52's
            delete-reseal-rewrite sequence is caught; the reseal no longer
            launders the edit.
        M8  successor with no valid predecessor  FAIL, and it holds with the
            predecessor's back-link stripped first.
        M9  two competing successors             FAIL, including a claimant that
            competes through its seal alone.
        M10 alter a result after publication     FAIL, STALE or REWRITTEN.

      One limit is recorded rather than left to be inferred: nothing seals the
      CURRENT record, because a seal is written by a successor and the head has
      none. Its deliverables are pinned, so an edit to what it produced is
      caught; an edit to its own prose is not.
      `test_editing_the_head_record_itself_is_not_claimed_to_be_caught` asserts
      that limit so it cannot be mistaken for coverage.
  - id: AC-4
    outcome: PASS
    evidence: |
      FIND-Q9-49 - CORRECTED AT ROOT.

      Charged units were deduplicated by `(path, anchor)` keeping the FIRST, and
      acquisition units are built before deliverables, so a path declared as
      both a read entry and a deliverable was charged to `acquisition` and never
      reached `revision`. The gate then moved on the one channel the split
      exists to close.

      Attribution is now decided from the declarations, not from build order: a
      declared deliverable is the task's own output, its existence at dispatch
      is the task's to decide, and a quantity the task decides has no business
      inside a dispatch gate. On a shared path the deliverable classification
      wins.

      Re-measured on the audit's own counter-example - a task declaring
      `deliverable: [out/shared.md]` and `optional: [out/shared.md]`:

        cap 100000   acquisition 21 -> 21   revision 0 -> 2704   X-08 PASS -> PASS
        cap 400      acquisition 21 -> 21   revision 0 -> 2704   X-08 PASS -> PASS
                     X-10 PASS -> FAIL at cap 400

      Acquisition delta is ZERO at both caps, against the audit's measured
      251 -> 855 and 249 -> 853 (+243%). X-10 moves, which is correct: it is the
      declared non-monotonic bound and §11.1 says its verdict moves when the
      task writes its own deliverable.

      The model was not changed to make a test pass. The unit is charged exactly
      once either way, so `total_measurable` - and therefore X-10 - is unchanged
      to the token; only which named quantity carries it changed. Asserted by
      `test_it_is_charged_exactly_once_and_the_total_is_unchanged`.

      The second limb is closed too: the overlap is REPORTED. Nothing asserted
      it was empty and no check reported one when it was not. A notice names
      every shared path and the component it came from. The live tree has no
      overlap for any of the six tasks, and
      `test_the_live_tree_has_no_overlap_and_says_so` records that as a
      measurement of this tree rather than a property of the model - which is
      the distinction FIND-Q9-49 was raised about.
  - id: AC-5
    outcome: PASS
    evidence: |
      MUTATION CAMPAIGN, WITH THE PIN-INDUCED FLOOR SUBTRACTED.

      The floor was re-measured independently before scoring, by applying a
      semantically inert edit to each mutated file in turn:

        null mutation to graph.py    3 failures
        null mutation to checks.py   3 failures
        null mutation to scope.py    3 failures

      Three, and the SAME three tests in every case - the pin-drift tests named
      in §14.1. The floor is confirmed at 3 and is subtracted from every count
      below. Scoring is against the failing test NAMES, not the count, exactly
      as §14.1 requires.

        MU-A1  seal_target always empty (MU30)          total 30  semantic 27  KILLED
        MU-A2  Link.divergent always False              total  7  semantic  4  KILLED
        MU-A3  Link.target ignores the seal             total  4  semantic  1  KILLED
        MU-A4  competing-successor detail removed       total  5  semantic  2  KILLED
        MU-A5  epoch guard reverts to the bare field    total  4  semantic  1  KILLED
        MU-A6  cross-check reverts to single-field      total  4  semantic  1  KILLED
        MU-A7  seal match unanchored (basename only)    total  8  semantic  5  KILLED
        MU-A8  missing-predecessor detail removed       total  5  semantic  2  KILLED
        MU-A9  FIND-Q9-49 attribution removed           total  7  semantic  4  KILLED
        MU-A10 FIND-Q9-49 deliverable set emptied       total  7  semantic  4  KILLED
        MU-A11 is_successor_link reverts to `supersedes` total 10  semantic  7  KILLED
        MU-A12 derived_seal_epoch reverts to the field  total  4  semantic  1  KILLED
        MU-A13 successors_of reverts to pre-repair      total 10  semantic  7  KILLED
        MU-A14 canonical_path stops collapsing noise    total  6  semantic  3  KILLED
        MU-A15 dangling-seal detail removed             total  9  semantic  6  KILLED
        MU-A16 seal path reconciliation removed         total  4  semantic  1  KILLED

      16 of 16 killed, 0 survivors. MU-A14, MU-A15 and MU-A16 were added after
      independent verification, one per limb of the V-2 and V-4 repair, so that
      the new code is not the only code in the layer that no mutant attacks.

      MU-A1's count rose from 11 to 27 between the first campaign and this one:
      the negative suite grew by the V-2/V-4 tests, and those tests exercise the
      seal limb, which is what MU-A1 removes. The earlier figure was correct
      when it was taken and is superseded rather than corrected.

      A NOTE ON THE DRIVER, recorded because it is evidence for the mechanism
      under repair. A harness timeout killed the campaign runner mid-mutant and
      left MU-A15 applied to `checks.py`. The pin comparison against this
      record's own deliverable digests detected the unintended source state
      immediately and named the file. That is the FIND-Q9-47 pinning design
      doing exactly what §14.1 says it is for, on an accident rather than an
      attack.

      REPRODUCIBILITY - THE HONEST BOUND (VER-010 F-8, F-9). MU-A14's score is
      corrected above from 5/2 to 6/3, the independently reproducible value; the
      original was measured against a smaller suite and is superseded, not
      defended. Of the sixteen mutants, four - MU-A1, MU-A14, MU-A15, MU-A16 -
      have been independently reproduced by a cold verifier. THE OTHER TWELVE
      HAVE NOT, AND CANNOT BE FROM THIS REPOSITORY ALONE: their constructions
      live only in a scratch driver outside the repository, and this record
      carries one-line labels rather than the substitutions. Under LAW-05 a
      reader can neither confirm nor refute them without inventing a
      construction of their own and hoping it matches. That is a real limit on
      this campaign's evidentiary value and it is stated rather than papered
      over. THE FULL CAMPAIGN IS NOT INDEPENDENTLY REPRODUCIBLE. The seven
      mutants of AC-9 are recorded with their exact substitutions, which is the
      standard the next campaign should meet.

      MU-A16's coverage width is exactly one test - the thinnest margin in this
      work, and named so it is not mistaken for depth.

      THE FIRST CAMPAIGN HAD THREE SURVIVORS AND THAT IS RECORDED RATHER THAN
      HIDDEN. MU-A3, MU-A5 and MU-A12 survived the suite as first written. They
      shared one uncovered shape - a record linked ONLY by its seal path - and
      MU-A5 was a live bypass, not a coverage nicety: reverting the epoch guard
      to `if result.supersedes` skipped the missing-seal rule entirely for such
      a record while the back-link cross-check stayed satisfied by the surviving
      path, leaving the predecessor rewritable with X-06 PASS.
      `TestASealOnlyLinkIsStillGovernedByTheEpoch` was written for exactly that
      shape and kills all three. The class docstring records why it exists.
  - id: AC-6
    outcome: PASS
    evidence: |
      REGRESSION - NO PREVIOUS REPAIR REGRESSED.

      `python -m pytest tests/test_exec_*.py -q`

        Baseline before this pass:  275 passed, 0 failed.
        After this pass:            362 passed, 0 failed.

      Per file, so the total is checkable rather than asserted:
      test_exec_records 31 + test_exec_scope 82 + test_exec_graph 64 +
      test_exec_checks 107 + test_exec_supersession 78 = 362.

      NET +87. 0 deleted, 0 weakened, 0 skipped, 0 xfailed. No test was made
      hermetic to avoid a live-tree assertion and no cap, write_scope or
      `context_budget` value was changed.

      CORRECTION - VER FINDING V-1. The first form of this record stated "340
      passed" and "65 tests added". The true figures at the moment the verifier
      measured were 315 and +40; they are 362 and +87 now that the V-2, V-4, VER-010
      F-1/F-2 and VER-011 G-1 tests exist. Both original figures were wrong, neither was measured, and
      the record named no command, which made the claim unfalsifiable as
      written. That is the overstated-evidence class FIND-Q9-36b, 46 and 43-D
      were, committed in the record whose subject is that class. The figures
      above are per-file collection counts and the command that produces them is
      named. `tests/` is untracked, so "0 deleted" is bounded by the net delta
      and by the five prior-audit test names being present and unaltered; it is
      not independently derivable from the repository and is recorded as such.

      Whole-suite `pytest tests/ -q` reports 2 failures in
      `tests/test_stage6_*.py`. Both PRE-EXIST this pass, are outside its write
      scope, and are FIND-Q9-53 - a live-tree test depending on another task's
      untracked in-flight deliverable. Not repaired here; see U-4.

      The four repairs the authorisation requires be preserved were re-run and
      hold:

        bounded task scope             X-03 PASS
        observation surface            X-07 PASS, undeclared-observation notice
                                       intact
        read/write hazard classes      three classes, `acquisition_units` still
                                       the enumeration `graph.read_surface`
                                       compares - FIND-Q9-38 asserted directly
                                       by `test_the_hazard_model_still_sees_the_path`
        result publication path        X-09 unchanged at ten rows
        non-monotonic budget treatment X-08 / X-10 split intact, MU-A9 and
                                       MU-A10 confirm the split is load-bearing
        acquisition/revision/telemetry telemetry still UNMEASURABLE and never
                                       estimated

      `PYTHONPATH=src python -m aief_exec check` reports 7 of 10 PASS. X-08,
      X-09 and X-10 FAIL, with the same rows and the same figures as under
      R-013 except where FIND-Q9-49 moved an attribution - and it moved none on
      this tree, because the overlap is empty for all six tasks. None of the
      three fails for a defect in the layer; all three are declaration and cap
      decisions recorded as open items for `project-manager`.

      T-004's figures moved again and no number is quoted, for the reason R-013
      gave: T-004 consumes this record and reads EXECUTION_ARCHITECTURE.md,
      which this pass extended by two sections. Run `aief_exec scope T-004`.
  - id: AC-7
    outcome: PASS
    evidence: |
      SCOPE DISCIPLINE - WHAT THIS PASS DID NOT DO.

      The authorisation is Option A plus FIND-Q9-49 plus independent
      verification plus an architectural record for Option B, and nothing more.
      Confirmed not done:

        no Stage 6 execution           `src/aief_stage6/**` untouched
        no MANIFEST.lock               absent
        no L-0000001                   ledger HEAD unchanged at seq 0, genesis
        no ledger transition           `.ai/project/ledger/**` untouched
        no CAD, no Fusion              untouched
        no frozen specification change `spec/**`, `framework/**`,
                                       `.ai/project/FROZEN.md` untouched
        no `.ai/core/**` write         untouched
        no F-007, task-grant, or
        supersession-ledger migration  out of scope by §12 of the authorisation
        no GitHub CI dependency        all testing local; nothing pushed

      Option B is recorded as a future direction at
      EXECUTION_ARCHITECTURE.md §6.5 with STATUS: FUTURE ARCHITECTURE DIRECTION
      - NOT IMPLEMENTED, and with its authority prerequisites enumerated.
  - id: AC-8
    outcome: PASS
    evidence: |
      INDEPENDENT VERIFICATION DISPOSITIONS.

      A cold-context verifier re-ran the pass-7 attacks against the repaired
      mechanism, built the FIND-Q9-49 counter-example against a pre-repair build
      of its own construction, re-measured the pin floor, spot-checked six
      mutants, and mounted five attacks of its own devising. Verdict VERIFIED,
      with four MINOR findings and no blockers. All four are disposed here; none
      is deferred.

      V-1  MINOR  This record's test counts were false.
           CLOSED. See AC-6, which now carries the per-file derivation and the
           command. The verifier measured 315 against the tree as it then stood;
           the figure is 362 after V-2, V-4 and the VER-010/VER-011 findings were closed.

      V-2  MINOR  `seal_target` voided several spellings of a real predecessor
           path in silence, enabling a two-character edit to delete the seal
           limb while leaving a block that reads as a correct digested seal.
           CLOSED BY SPELLING FAMILY, NOT AT ROOT - the first form of this row
           said "CLOSED AT ROOT" and that was WRONG. A second independent
           verification (VER-010 F-1, MAJOR) reproduced the identical attack
           through a family the repair had not considered, at X-06 PASS with
           zero details. See AC-9. The wording is corrected here rather than
           left standing: a disposition that overstates its own reach is the
           defect this chain has been tracking since FIND-Q9-46, and claiming
           "at root" for a fix that enumerated spellings was an instance of it.

      V-3  MINOR  §6.4 presented "three edits across two files" as THE residual
           cost when two cheaper routes exist.
           CLOSED AS A DISCLOSURE DEFECT, not a code defect. §6.4 now tabulates
           all three routes with their costs and their residues, and names the
           tamper-then-reseal route - 2 edits, 2 files, ZERO residue - as the
           honest floor. That route is not repairable inside the result channel:
           nothing seals the CURRENT record because a seal is written by a
           successor and the head has none. It is the argument of §6.5 and the
           reason Option B is the recorded direction.

      V-4  MINOR  A seal path was never reconciled against the predecessor's
           actual file, so renaming the record file was invisible.
           CLOSED AT ROOT by the same X-06 limb as V-2: a seal naming a record
           that lives in a different file is a FAIL naming both paths.

      Two prior MINOR findings the verifier confirms still open are NOT claimed
      corrected here and were not in scope: FIND-Q9-50 (X-08 FAIL still implies
      X-10 FAIL - the independence is one-directional) and FIND-Q9-53 (two
      live-tree Stage 6 tests depend on another task's untracked deliverable).
  - id: AC-9
    outcome: PASS
    evidence: |
      VER-010 - THE SECOND VERIFICATION, AND THE DEFECT IT FOUND IN THE FIRST
      REPAIR.

      A confirmatory cold pass over the V-2/V-4 closure returned NOT CONFIRMED
      with one MAJOR blocker. It was right, and the blocker was the V-2 defect
      reproduced inside the V-2 repair.

      F-1  MAJOR  `graph.canonical_path` mapped a DECLARED path to "" for the
           family `./ . .. / // ../.. ././ ./.` and whitespace-only.
           `graph.seal_path` returned "" for that AND for "no path declared" -
           one value, two facts - and `checks.py` used it as its declaredness
           predicate, so the V-2 limb never fired for the family. Reproduced
           before repair: 10 variants, each X-06 PASS with 0 details after a
           post-supersession rewrite, detail-set-identical to a clean run, the
           seal block still carrying the predecessor's correct digest.
           CLOSED. `graph.seal_declared` reads the RAW declaration and is what
           X-06 guards on; `seal_path` is canonical form; `seal_target` is the
           record named. All 10 variants now FAIL.
           THE GENERAL CLAIM THAT ONE VALUE CAN NO LONGER CARRY TWO FACTS WAS
           WRONG and is withdrawn - see AC-10. It was true of the canonical
           form and false of the coerced one, one level down.

      F-2  MINOR  A seal block carrying `digest:` with the `path:` line deleted
           was equally silent - a SMALLER diff (3 lines) than the residual §6.4
           discloses.
           CLOSED. `graph.seal_declared_block` reports whether a block was
           declared - reading the value BEFORE coercion, which is the AC-10
           correction; a block that declares no usable path is a FAIL. Covered
           for the deleted line and for `null`, `~`, empty and whitespace.
           R-001, R-007 and R-008 carry no block and are not accused - asserted
           by `test_a_record_with_no_seal_block_is_not_accused`.

      F-3  MINOR  A leading `..` and a leading `/` were silently discarded,
      F-4  MINOR  and `..` was resolved, so
           `.ai/project/results/R-101.md/../R-100.md` minted R-100 while a
           reviewer reads R-101.md.
           CLOSED, and NOT by inventing a policy. §6.4 already required the seal
           path to be exactly `.ai/project/results/R-nnn.md`; resolving `..` and
           absorbing a leading `/` GUESSED at paths whose meaning depends on
           where the reader stands, in the same function that refuses to guess
           at case. `..` is now a literal segment and a leading `/` is
           preserved, so neither matches the required form, neither mints a
           link, and every one is reported. 31 spellings enumerated and
           measured: 0 silent.

      F-5  MINOR  A parametrised case (pure backslash path) passed against the
           pre-fix implementation - backslash normalisation pre-dates V-2.
           CLOSED. Removed from the V-2 parameter set and retained as
           `test_backslashes_normalise_which_pre_dates_this_repair`, named for
           what it is. A test that passes against the pre-fix code is not
           evidence for the fix.

      F-6  INFO   `test_the_two_character_edit_no_longer_hides_a_rewrite` never
           performed the rewrite its name and docstring claimed.
           CLOSED. The rewrite is performed. Name, docstring and behaviour
           agree.

      F-7  INFO   §6.4 said the class "runs all seven spellings"; it ran nine.
           CLOSED. §6.4 states 40 tests and itemises them; the figure is
           `pytest --collect-only` on the class, not an estimate.

      F-8  INFO   MU-A14's recorded score did not reproduce; mutant
      F-9  INFO   constructions are stored nowhere and MU-A16's margin is one
           test.
           CORRECTED, NOT DEFENDED. See AC-5: MU-A14 restated to the
           reproducible 6/3, the twelve unreproduced mutants declared
           unreproducible, and MU-A16's one-test margin named.

      MUTATION CAMPAIGN FOR THIS CLOSURE - constructions recorded, so this one
      IS re-derivable. Floor re-measured at 3 across FOUR modules (graph,
      checks, records, scope), same three test names every time, and subtracted.

        MU-F1a  checks.py: the F-2 limb guard -> `if False:`        6 semantic KILLED
        MU-F1b  checks.py: `elif declared_raw` -> `elif declared_path`
                                                                   5 semantic KILLED
        MU-F1c  graph.py: seal_declared returns canonical_path(raw) -
                the empty-string conflation restored at its source  6 semantic KILLED
        MU-F1d  checks.py: BOTH declaredness limbs -> `if False:`  30 semantic KILLED
        MU-F1e  graph.py: canonical_path resolves `..` and drops a
                leading `/` - F-3/F-4 reinstated                    7 semantic KILLED
        MU-F2a  checks.py: block-without-path limb disabled         6 semantic KILLED
        MU-F2b  checks.py: declaredness `details.append` ->
                `notices.append` - text preserved, verdict demoted  6 semantic KILLED

      7 of 7 killed, 0 survivors - BY VERDICT. By DISTINCT CONSTRUCTION it is
      six, and VER-011 C7 was right to say so: MU-F1a and MU-F2a disable the
      same guard and have byte-identical kill sets. MU-F2b as recorded was also
      ambiguous - `details.append` -> `notices.append` has two candidate limbs,
      and only the F-2 limb reading reproduces at 6; both limbs scores 30. The
      reading is the F-2 limb. Neither defect is repaired by restating the
      count; both are recorded so the next reader is not misled by it.

      MU-F2b is still the load-bearing one: it preserves every emitted string
      and flips only the verdict, so its death proves the tests assert the
      VERDICT and not the presence of a message.

      ONE DEGENERATE MUTANT IS RECORDED RATHER THAN COUNTED. The first form of
      MU-F1c made `seal_declared` return `seal_path(result)`, which is mutual
      recursion: it "killed" 103 tests by RecursionError, not by any assertion.
      That is a crash, not evidence, and inflating a kill count with it would be
      the defect this record spent AC-5 correcting. It was rebuilt
      non-recursively and scores 6.

      The F-1 seam has its own test -
      `test_a_declared_path_is_never_read_as_no_declaration` - which asserts
      that `seal_path` returning "" is NOT readable as "nothing was declared".
      It is the test that kills MU-F1c.

  - id: AC-10
    outcome: PASS
    evidence: |
      VER-011 - THE THIRD VERIFICATION, AND THE SAME DEFECT A FOURTH TIME.

      The confirmatory pass over the F-1/F-2 closure returned NOT CONFIRMED with
      one MAJOR blocker. It was right, and the finding is the reason AC-9's
      general claim is withdrawn above.

      G-1  MAJOR  `ResultRecord.supersedes_seal` coerces anything that is not a
           mapping to `{}` (`records.py`), and `graph.seal_declared_block` read
           `bool(result.supersedes_seal)`. So `{}` meant NO BLOCK and A BLOCK
           THAT DID NOT PARSE AS A MAPPING, and both the F-1 and the F-2 guard -
           built to close precisely this shape of defect one level up - skipped
           the malformed case in silence.

           It needs no exotic syntax. The sequence form `- path:` / `digest:` is
           the idiom every `inputs:` and `deliverables:` block in every record in
           this repository already uses. Reproduced independently on a copy of
           the LIVE records: reshape R-014's seal block, delete `supersedes`,
           delete R-013's `superseded_by`, rewrite R-013 -

             clean     X-06 PASS  details=0
             attacked  X-06 PASS  details=0     detail sets identical: True

           with R-014 still displaying R-013's correct published digest to any
           human reading the file. Three edits, two files - the same cost as
           F-1.

           CLOSED. `graph.seal_block_raw` returns the value BEFORE coercion,
           `seal_declared_block` reads that, and `seal_block_malformed` is a
           detail-level FAIL naming the shape found. A malformed block is
           declared AND malformed: two failures' worth of information, neither
           of them silence.

      MUTANTS, constructions recorded. Floor 3, subtracted.

        MU-G1a  graph.py: seal_declared_block -> `bool(result.supersedes_seal)`
                - the G-1 defect verbatim                       4 semantic KILLED
        MU-G1b  checks.py: malformed-block limb -> `if False:`  4 semantic KILLED
        MU-G1c  checks.py: malformed `details.append` ->
                `notices.append` - text kept, verdict demoted   4 semantic KILLED
        MU-G1d  graph.py: seal_block_malformed -> `return False`
                                                                4 semantic KILLED

      4 of 4 killed. STATED HONESTLY, AND THIS IS THE AC-9 LESSON APPLIED: the
      four scores are equal because the kill sets overlap heavily - these are
      four constructions of ONE boundary, not four independent coverage
      measurements. MU-G1c is the load-bearing one for the same reason MU-F2b
      was: it preserves every emitted string and flips only the verdict.

      ALSO CORRECTED FROM VER-011:
        C4 - the whitespace-padded resolving spelling was VACUOUS. The record
             parser strips an unquoted scalar before any code sees it, so the
             case passed against the pre-fix implementation - F-5's own defect,
             reintroduced by F-5's repair. It is quoted now, which is the only
             spelling that reaches `canonical_path`'s `.strip()`.
        C6 - §6.4's itemisation enumerated 35 of 40 tests. It now itemises all
             three classes to 78, the `--collect-only` figure.
        C8 - the false claims are withdrawn above and at §6.4, not softened.

      NOT CLOSED, AND NOT CLAIMED: VER-011 records that `test_the_two_character_
      edit_no_longer_hides_a_rewrite` performs its rewrite but the rewrite is not
      load-bearing - the seal-path detail fires first, so the test would pass
      without it. The name is now accurate about what the test DOES; it is not
      evidence that rewrite detection fires in that scenario. Recorded rather
      than dressed up.

conclusion: |
  The execution layer with `VER-009` pass 7's authorised Option-A mitigation
  executed and FIND-Q9-49 corrected. IMPLEMENTED PENDING INDEPENDENT
  VERIFICATION - nothing here is closed and nothing here is self-certified.

  WHAT THIS PASS DID

  1. FIND-Q9-51 and FIND-Q9-52, the one-line disarm, closed at root by
     UNIFYING the two definitions of a successor link rather than by deleting
     one of them. `checks._corroborates` accepted `supersedes` OR a seal path;
     `graph.successor_of` accepted `supersedes` alone. One relationship, two
     definitions, and the gap between them was an attack cheaper than the one
     the pass-6 repair had just closed: delete `supersedes:  R-012` from R-013,
     leave the seal, and the cross-check saw a link while the seal machinery saw
     none. The digest that would have convicted the tamperer was in the
     repository with no code path comparing it.

     `graph.link_of` reads both fields and is the only reading. A seal-only
     successor is a successor, so its seal is compared. A record whose two
     fields name different predecessors fails. Two records claiming one
     predecessor fails. A link to a record that does not exist fails.

     THE SEAL-PATH LIMB WAS KEPT, AND THAT IS A DEPARTURE FROM WHAT MU30 WAS
     READ TO RECOMMEND. Deleting it restores a single-field definition, which
     is the precondition of every recorded disarm. The redundant thing was the
     second definition, not the second field, and the second definition is what
     was deleted. MU-A1 re-runs MU30 against the unified mechanism and dies to
     27 semantic failures where MU30 died to none. (The first form of this
     line said 11, the figure measured before the suite grew; AC-5 records the
     rise. VER-011 C8 caught it still standing here - a record contradicting
     itself in two places is the defect this chain tracks, committed inside the
     correction for it.)

  2. FIND-Q9-49, the gate's headline property, made structural. Dedup kept the
     first unit on a path and acquisition is built first, so a lawfully shared
     read/deliverable path was charged to the GATED quantity and the gate moved
     when the task created its own output. Attribution is now decided from the
     declarations. Acquisition delta on the audit's counter-example is zero at
     both caps, against +243%. The unit is still charged exactly once, so
     `total_measurable` and X-10 are unchanged to the token, and the overlap is
     reported instead of assumed empty.

  3. The mutation methodology was applied honestly. The pin-induced floor was
     re-measured at 3 before scoring, three mutants survived the first campaign,
     one of them was a live bypass, and the tests written for it are named after
     the finding rather than folded in silently.

  4. Independent verification returned VERIFIED with four MINOR findings, and
     all four are closed in this record rather than deferred. Two were real
     holes in the repair itself - a seal path that named nothing was voided in
     silence (V-2), and a seal was never reconciled against the file it sealed
     (V-4) - and both are now X-06 failures. One was a false claim in the first
     form of THIS record (V-1). One was an overstated disclosure in §6.4 (V-3),
     corrected by tabulating all three residual routes and naming the cheapest
     rather than the most flattering.

  5. A SECOND independent verification rejected that closure, and it was right.
     The V-2 repair had reproduced the V-2 defect one level in: canonicalisation
     mapped a DECLARED path to the empty string, the check could not tell that
     from "nothing was declared", and ten variants rewrote a superseded record
     at X-06 PASS with zero details. F-1 is closed by separating the three
     questions - was a path declared, what does it canonicalise to, what record
     does it name - so that no single value carries two facts. F-2, F-3 and F-4
     are closed with it; F-5 through F-9 are corrections to tests, documentation
     and this record's own evidence claims.

     The pattern is worth stating plainly, because it has now happened three
     times: FIND-Q9-43's repair created FIND-Q9-51, whose repair created V-2,
     whose repair created F-1. Each was a control weakened by a value that meant
     two things at once. That is the argument of §6.5, not a coincidence.

  WHAT THIS PASS DID NOT DO

  Option B - anchoring supersession integrity in the hash-chained ledger - is
  recorded at §6.5 as the intended structural direction and is NOT IMPLEMENTED.
  No ledger entry was created, HEAD remains at seq 0 state genesis, and no hash
  construction was invented. It requires a Class-C/A4 authority path, lawful
  ledger initialisation, an approved construction for L-0000001 and a migration
  plan for R-001..R-014.

  WHAT REMAINS OPEN

  The three-edit residual at §6.4 is a narrowing, not a closure, and is now
  pinned by a test that requires the two-edit prefix to fail. The §6.2 residual
  - backdating a successor's session to before the epoch - is unchanged and
  untouched by this pass. X-08, X-09 and X-10 still fail on declaration and cap
  decisions that belong to `project-manager`. F-007, task-grant enforcement and
  the A4 boundary were deliberately not touched.
```

## Notes

**Immutability.** One input and twelve deliverables are DC-1-pinned, and `R-013` is sealed
by a fourteenth digest taken over the whole record after its supersession fields were
written. `X-06` recomputes the thirteen pins on every run and compares the seal; any drift
makes this result STALE, and any subsequent edit to `R-013` raises
`REWRITTEN AFTER SUPERSESSION`.

**And the seal is now anchored to the link that carries it.** `R-013` declares
`superseded_by: R-014`; this record declares `supersedes: R-013` and seals it. Under
`graph.link_of` those are two readings of ONE relationship, so deleting the `supersedes`
line above does not orphan the seal — the seal alone still names `R-013`, the link stands,
and the digest is still compared. That is the whole of the `FIND-Q9-51` repair, stated on
the record it protects.

**Status.** `IMPLEMENTED PENDING INDEPENDENT VERIFICATION`. This record does not close a
finding and does not certify itself. `T-001`'s verifier is `qa-engineer`; `LAW-05` requires
the pass that disposes the findings above to be taken by a role that authored none of it.
