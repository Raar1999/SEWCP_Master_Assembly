# R-008 — Execution Layer, After Two Verification Passes

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-007`](R-007.md)**, which `VER-009` pass 2 disposed REJECTED.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-008
produced_by:
  task:      T-001
  role:      software.software-engineer
  session:   S-2026-08-09-01
status:      SUPERSEDED
superseded_by: R-009
supersedes:  R-007

inputs:
    - path: .ai/BOOT.md
      digest: 5a97d6ad763f8720b3e9e5c9cf9be6ad62f6ba953b5b70eb78a3624b580b82db
    - path: .ai/project/STATE.md
      digest: 1cde83cde8698ac758f043a04cede54e8e44258a16531c2a52312dfff56c175d
    - path: .ai/project/BINDING.md
      digest: 64a9ca216606e502ca186985dc4ef22d5f7fd0504ed281b392c84910ac81a15f
    - path: .ai/project/ROSTER.md
      digest: 3c657f6e866ab6f71719ca3939d04adf7c827d35aeb9f2ab0e44f32312b19694
    - path: .ai/core/CONTEXT_TIERS.md
      digest: dad8a47ad994cce45820b9b89f016872f259b7b44b10e8a87ed42f32e31ade4f
    - path: .ai/core/templates/TPL-task-package.md
      digest: a51c5d27fa44f85b3d6a291b28099161561e8cc367e2ce64dc8ae0553501e9b2
    - path: .ai/core/schemas/SCH-task.schema.json
      digest: e874fdd70b56f55b6205e26c7ef537e090e46b5f05ab2964f8b1ee4ad6cccc80
    - path: framework/framework.manifest.json
      digest: 920eb6eec217732152c452d51f01e471940df6f2e2ffe608c377fccc37814090

deliverables:
    - path: .ai/project/EXECUTION_ARCHITECTURE.md
      digest: 6afad9164dbd5050cb3bce3a38cc442df84b71cf3f57a86ac155b425fef87736
    - path: src/aief_exec/__init__.py
      digest: b8813ebc7fb18c820721517de771f9ed8ca224971824e2e9680f227ef5f77b69
    - path: src/aief_exec/__main__.py
      digest: 309bcfe906bf5b6714a9a1e2e4cdc0ed987a4f55b0af19900f00cd2b3e81a053
    - path: src/aief_exec/records.py
      digest: 98f571d5d4ff18bf54985ef606a137869983bc7b31bb0b488e02b1ca42467d91
    - path: src/aief_exec/scope.py
      digest: e7cf5b828712040579f51448a39fb960bade20d67e49ff1ee115968f26852119
    - path: src/aief_exec/graph.py
      digest: 3e739e44d51b1e738c5c11699ce4c07329666c98a80084e06330013df3926e58
    - path: src/aief_exec/checks.py
      digest: 865a1840f19cc1e743b98fca1b7e0c99b3fa6332988eeb2b98b516dae0d24249
    - path: tests/test_exec_records.py
      digest: ab3a474fcd9fe0bf757540af7a3c856ed49eff40fb3aa0b8ebd7ab50000b27f3
    - path: tests/test_exec_scope.py
      digest: 30963646c8d86bff0b27073792bf825312fec1cd24e959e0052e79b35425ec69
    - path: tests/test_exec_graph.py
      digest: 0c3fbbaccdbf703e3d7a9cf965660adb641bb1aca7ed9b4795141905ac39945e
    - path: tests/test_exec_checks.py
      digest: 28b5ebb0be7af4cff09459a2c9fbda4958085e688e592f8b3ebf379e7a5c8dcc

validation:
  - check: X-01 through X-08
    outcome: PASS on the live repository, with notices printed
  - check: V-01..V-09, V-23, V-25
    outcome: PASS
  - check: V-24
    outcome: FAIL - pre-existing, byte-identical to the pre-task baseline
  - check: pytest tests/
    outcome: 239 passed, 4 failed - exec 129 passed 0 failed; Stage 6 110 passed 4 failed at 114 total, unchanged

conclusion: |
  A bounded task-and-result execution layer is in place, operating on this
  repository's real backlog, and hardened through two independent verification
  passes that between them raised twenty-one findings.

  WHAT A DOWNSTREAM AGENT NEEDS TO KNOW

  1. Read `.ai/project/EXEC.md` after boot - a bounded index, one task id per
     line, naming the state of every task.
  2. Read the single `tasks/T-nnn.md` you are assigned. Read no other.
  3. Run `python -m aief_exec brief T-nnn` for the resolved minimum context:
     anchored excerpts plus the conclusions of consumed results.
  4. Classification is computed, never declared. `status` gives task states,
     derived blocking reasons and dispatch groups that are pairwise safe to run
     concurrently. `audit` attributes every working-tree change to a task, or
     reports it UNATTRIBUTED. `check` runs X-01..X-08 and prints both failures
     and notices.

  MEASURED EFFECT, exact under TF-1 and TF-2, never estimated

  Task T-002 mandatory scope resolves to TF-1 4,295 / TF-2 5,626 against
  TF-1 65,822 / TF-2 86,995 for the same files unanchored. Whole route to the
  first line of work: TF-1 9,727 declared, against 155,192 by citation chain and
  386,845 by unbounded sweep. Cold-session recovery to a task-ready agent:
  TF-1 7,565. The sweep and declared figures and the anchoring ratio are
  reproduced by `python -m aief_exec measure T-002`, which prints the envelope
  definition alongside the numbers - added after VER-009 FIND-Q9-26 found the
  figures came from a script outside the repository and an independent sweep
  landed elsewhere because the envelope was undefined. The citation-chain figure
  is a one-time historical measurement of the route that existed before this
  layer and is not reproducible by a command against the current tree.

  WHAT THE SECOND VERIFICATION PASS CHANGED

  Pass 1 found six MAJOR defects and R-007 claimed to close them. Pass 2
  disposed R-007 REJECTED and found that three of the closures were partial and
  that the fixes had introduced four new defects. All are now closed:

  - The `notices` channel was computed and never printed, so R-007's central
    claim - that a granted reach into the protected set is never silent - was
    false as delivered. `aief_exec check` prints notices.
  - `write_authority` and then `role_authority` were validated for shape only,
    so a junk citation of sufficient length granted `.ai/core/**` or released an
    UNASSIGNED role. A grant now declares `id` and `recorded_at`; the path must
    exist and must actually contain the id. This is the pattern both passes
    named: a control whose exemption is written by the party it constrains.
  - `BLOCKED` was read from the record when no blocker was derived, so half the
    X-02 cross-check was dead code and an over-declared block was invisible.
    BLOCKED is now always derived.
  - R-007 pinned `VER-009`, which is its own consumer's deliverable, so T-004
    filing its report staled the result T-004 depended on. It also pinned six
    mutable task records and EXEC.md, freezing the backlog. R-008 pins only the
    contract, the code and the tests - artifacts that are settled at
    publication.
  - The literal-prefix overlap test ignored that `*` does not cross a
    separator, so `.ai/*.md` falsely conflicted with `.ai/project/EXEC.md`. A
    witness test replaced it.
  - AC-4 named a task pair whose derived class changed when an unrelated fix
    moved one of them to BLOCKED. It now tests scope independence directly,
    because blocking is a state and scope independence is a property.

  A DEFECT FOUND IN THE REPOSITORY, NOT IN THIS WORK

  `manifest.validation[V-03].verifies` was amended by AIEF-AMD-014 AMD-49 to
  require the bounded-register mapping to hold in both directions, citing
  `mapping_state` and `index_grammar`. No implementation under `src/` references
  any of those tokens. Meanwhile `files[state-register]` declares
  `project/STATE_REGISTER.md`, which does not exist in the working tree. V-03
  therefore reports PASS while not implementing the clause that would fail it.
  A false negative in the validation layer, not merely a missing file.

  WHAT IS NOT CLAIMED

  Enforcement is by tooling and tests, not by a manifest-registered check.
  Read scope is advisory: VER-009 confirmed by instrumentation that `brief`
  itself opens 21 files, though the brief it emits is correctly bounded. A grant
  is inspectable, not unforgeable, and forging one needs less than an earlier
  draft of this record claimed. `id` is matched as a substring of any readable
  file: VER-009 FIND-Q9-22 landed five working forgeries of `.ai/core/**` from
  the task record alone, including `id: Never edit` cited to CLAUDE.md where that
  string occurs only inside a prohibition against the very write being granted.
  Three of the four cited files the forger does not write, so the earlier
  two-files framing was wrong in the direction that under-weights the risk. Every
  version of this control checks that a citation has a form, never that it has a
  referent; closing that is a chief-systems-engineer decision, recorded as F-007.
  X-01 cannot see session identity, half the (role, session) pair AMD-20 makes
  decisive. `attribute` is atemporal. Pinning secures bytes, never claims about
  them - pass 2 found R-007 stating a test count that was wrong when written
  while every pinned digest matched.

  AUTHORITY POSITION

  No file under `.ai/core/` was edited, no manifest field changed, no frozen
  artifact changed, no check was added to the V-01..V-25 namespace, no ledger
  entry was written and Stage 6 was not executed. Extension fields are lawful
  under `SCH-task.schema.json`'s `additionalProperties: true`. Exactly the seven
  tracked files modified before this work began remain the only tracked
  modifications.

findings:
  - id: F-001
    severity: MAJOR
    statement: V-03 reports PASS while implementing none of the AMD-49 bounded_register_split clause its own specification carries, and project/STATE_REGISTER.md is declared in manifest files[] but absent from the tree. A false negative in the validation layer.
    owner: chief-systems-engineer
  - id: F-002
    severity: MEDIUM
    statement: STATE.md lists CMP-BLOCK-006 under blockers and omits OQ-15, while OPEN_ITEMS.md - which STATE.md itself names authoritative - lists both under Closed. ENGINEERING.md section 7 also still describes CMP-BLOCK-006 as live. V-09 now passes, corroborating the register rather than STATE.
    owner: project-manager
  - id: F-003
    severity: LOW
    statement: Four pre-existing test failures, unrelated to this task and present before it began - two snapshot assertions superseded by AMD-42 and AMD-51, one V-24 registry assertion, one pipeline stub gated on V-24.
    owner: software.test-engineer
  - id: F-004
    severity: MEDIUM
    statement: qa-engineer, project-manager, chief-systems-engineer and all four mechanical roles are UNASSIGNED in ROSTER.md, so T-003 is blocked and the verifiers of T-004, T-005 and T-006 cannot be dispatched. This is OI-P-02 and assignment is a project-manager action.
    owner: project-manager
  - id: F-005
    severity: OBSERVATION
    statement: T-004 was dispatched under rank-1 live human instruction while its role is UNASSIGNED, recorded as an enumerated role_authority grant resolving against decision EXEC-D-001 in the T-001 checkpoint.
    owner: project-manager
  - id: F-007
    severity: MAJOR
    statement: Authority grants match `id` as a substring of any readable file, so a citation is checked for form and never for referent. VER-009 FIND-Q9-22 landed five working forgeries of .ai/core/** from the task record alone, one of them reading a prohibition in CLAUDE.md as authorization for the write it prohibits. Four engagements have each narrowed the hole without closing it; what makes a citation resolve to a referent is an architecture decision.
    owner: chief-systems-engineer
  - id: F-008
    severity: MINOR
    statement: The witness-based scope-overlap test under-reported six of eleven genuinely overlapping pattern pairs (VER-009 FIND-Q9-24). A conservative fallback now applies when both patterns are wildcarded and their literal prefixes nest, restoring error in the safe direction. Unverified by an independent pass.
    owner: software.software-engineer
  - id: F-006
    severity: OBSERVATION
    statement: VER-009 FIND-Q9-11 and FIND-Q9-12 remain open by design and are recorded as limits in EXECUTION_ARCHITECTURE section 14 rather than fixed - scope.attribute is atemporal, and parse_index enforces one-id-per-line only after a section's first identifier.
    owner: software.software-engineer
```

## Notes

**Immutability.** Eight inputs and eleven deliverables are DC-1-pinned. `X-06` recomputes all
nineteen on every run; any drift makes this result STALE and blocks `T-004`, which consumes
it.

**What is deliberately not pinned.** `VER-009` is T-004's deliverable and pinning it made
R-007 self-staling (FIND-Q9-13). The task records and `EXEC.md` are mutable registers and
pinning them froze the backlog (FIND-Q9-14). Neither appears above.
