# R-018 — LC-M04 Specification Coherence Package, After the C6 Instrument Ruling

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-015`](R-015.md)**, whose pinned `src/aief_gate/criteria.py` digest this task
> moved under `ECR-D-012`. `R-015` is retained unedited apart from the two supersession fields,
> and its DC-1 as it now stands is sealed below.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-018
produced_by:
  task:      T-009
  role:      chief-systems-engineer
  session:   S-2026-08-10-04
status:      CURRENT
supersedes:  R-015
supersedes_seal:
  path:   .ai/project/results/R-015.md
  digest: 98beff9050e33c139f36ef4b891008ce929e782d9b8ff58c46a343df35871d32

inputs:
    - path: .ai/project/verification/VER-016_Confirmatory_Round_On_VER-015_Repairs.md
      digest: 3134dbd88bf32985b522270a1c90d13cdbe52595304e797aa7bbc2036a7c72a2

deliverables:
    - path: spec/00_SEWCP-ENG-001_Architecture_and_Interface_Control.md
      digest: fa2a84ccf3a837176eade00c502916a7a1492c9c58785448a4c26ba0fbaab95d
    - path: spec/01_SEWCP-200_Cooling_Plate.md
      digest: 55b47ca30eeac99ca231d960a1066411b827bf6da139d5e7d178db6a182c3a39
    - path: spec/02_SEWCP-300_Heater_Plate.md
      digest: 0290580066829963de1c9bbbd059f5f088e442031d679cf4ddf5046693d66aef
    - path: spec/03_SEWCP-400_Chuck_Support_Ring.md
      digest: a2f951a1c749b688141d8245f8575ed6aabf28df03b55788a0d762f4e6c7dcbf
    - path: spec/06_SEWCP-700_Alignment_Pins.md
      digest: 75cda88184e5ae50acd05fb86dfb61ffc6238219462e8854120c05f14d04f396
    - path: spec/07_SEWCP-800_Vacuum_Port.md
      digest: 7558bc5b0f613ab9184d66dba6afd6674214d33302775eee2515bd25c8122afe
    - path: spec/README.md
      digest: 1d7720723ddd42028a9536ec20cfb50a9a8b803bc3172423b31f108a7f93416c
    - path: src/aief_approval/chain.py
      digest: be61c76c870166c634e2a43d56110c57f607b12ef927cc378b5973a2b62ac8b1
    - path: src/aief_gate/criteria.py
      digest: 2e13c1b7868f7457cdaaee4106057c5d9096129d5b58ee391ca236940622f85e
    - path: src/aief_clearance/check.py
      digest: 1b5c08cccb2344f0ba53e61718ccdf708cdc23eef01525f6b88ae447c919fb85
    - path: src/aief_params/__init__.py
      digest: df1edf0448052821bac5c0d6535c9f72c46c5bffbc3a20e6eb0100c1fcb1f690
    - path: src/aief_params/extract.py
      digest: e27295056c30b59f5b5a617b625fe617914ae9448033a2d83475219d20c7b636
    - path: src/aief_params/__main__.py
      digest: 711a4965f38d790e6322afe17a1f8b545097dfbd7ce541f2d9a81f18deb4680e
    - path: tests/test_approval_chain.py
      digest: bef2b2c8c0f3f3d1371544d8c660011c7e58ab06eeab6ef0159604c99205328d
    - path: tests/test_clearance.py
      digest: 3f411f4c149adb6d547d52fbcbbabe03a47420f9fabf07af8c8a8679b3e6e4ae
    - path: tests/test_verification_chain.py
      digest: 5ba1ad803a371371cabfb891c1b4e22d72d33d5c3bc8610e6f7f9c78b798bd0f
    - path: tests/test_params_export.py
      digest: 492abe55a12e535e32905c0a5d45da29ba508a3c8ed0d6d2bbf1ef605e9b6cc1
    - path: params/generated/SEWCP-200.csv
      digest: 5434421dd781169fa7e47d4ec78bad310943d0f58a3999cb1619006587beb27f
    - path: implementation/01_SEWCP-200_Cooling_Plate/SEWCP-200_CAD_Implementation_Package.md
      digest: 2f58beec9b8cba5cbf19e5f9ff96ffad64b49203cc20993420ada12fc0da4955

conclusion: |
  LC-M04-EXIT C1..C7 all PASS on computed evidence; python -m aief_gate exits 0 and prints
  LC-M04 CAD-READY: YES. C6 is satisfied for all four gated ECRs by VER-017, which supersedes
  VER-014, VER-015 and VER-016 under the sealed relation ruled at ECR-D-012 and approved at
  APR-028. No spec/** artifact changed in this task - the frozen set is byte-identical and its
  DC-2 aggregate is unchanged. CAD is authorised to begin.
```

---

## 1 · What was inspected

`VER-016`'s fourteen findings; the `C6` predicate and the `GATES.md` text that is supposed to
declare it; every record under `.ai/project/ecr/` and `.ai/project/approvals/`; `FROZEN.md`; the
SEWCP-200 CAD implementation package §3, §6 and §9; and the exec-layer tests `OI-C-12` names.

## 2 · What was changed

**The `C6` instrument, under `ECR-D-012` / `APR-028`.** The supersession relation it had been
computing since `5e7ac74` was **declared nowhere and bound to nothing**; a bare `supersedes:` line
in any report retired any audit, of any subject, with no proof the superseding verifier had read
what it displaced. It is now ruled in `GATES.md`, **sealed** to the predecessor's DC-1, scoped per
gated ECR, fork- and self-reference-guarded, and fails closed with no fallback. `status` became a
closed vocabulary in the same ruling.

**`VER-016` F-07, F-09, F-10, F-13** repaired at the record. **F-06** repaired by deriving
`params/generated/SEWCP-200.csv` from the package §3 master under a standing check. **F-08**
recorded as `OI-C-13` rather than repaired — see §3. **F-11** repaired in `C7`'s evidence lines.
**F-14** is under `.ai/core/**` and is not this task's to touch.

**Three modelling steps that would have built nothing.** Steps 6.08, 6.27 and 6.32 still
instructed the modeller to insert *suppressed placeholder groups* in place of the coolant circuit,
the sixteen choke counterbores and the six kinematic locators — all three HOLDs having been
discharged in `S-2026-08-10-01`. `VER-016` W10 checked whether §6 built the **wrong** geometry and
found four defects; it did not check whether §6 built **no** geometry. Corrected, with the stale
`HOLD` tags on `CP-IF-1`, `CP-IF-4`, `CP-IF-10`, §3.2 and `ang_coolant_*`.

## 3 · What was NOT changed

**No `spec/**` artifact.** `git diff --name-only 5e7ac74 HEAD -- spec/` and the working-tree
equivalent are both empty; all eleven registered `spec/**` digests reproduce and the DC-2 aggregate
is unchanged at `73911786…`. The gate's *engineering* criteria were satisfied before this session
began; what was not satisfied was the evidence criterion.

**No verification record's verdict.** `VER-014`, `VER-015` and `VER-016` are on disk unedited,
verdicts intact. They are superseded, which is not the same as contradicted, and the seal makes any
later edit to them **fail the gate**.

**`APR-001`…`APR-013`.** `VER-016` F-08 wants an `ecr:` back-reference on thirteen human-approval
records that predate the convention. Adding it means rewriting a record of a human decision to
carry metadata its approver never wrote. Recorded as `OI-C-13`; not done silently.

Nothing under `.ai/core/`. No Stage 6 execution, no `MANIFEST.lock`, no ledger entry, no tag, no
push.

## 4 · Root cause, and where it was repaired

**One shape, and it is the inverse of the one `R-015` §4 recorded.** `R-015` found three *declared
properties that nothing computed*. This task found the mirror image: **a computed property that
nothing declared.** `criteria.py` enforced a supersession relation and a verdict rule that
`GATES.md` — the authority the framework delegates the criteria to — did not state. A gate that
passes on a rule no authority ruled has not been passed, whichever direction the gap runs.

**Two defects fell out of closing it, neither of them in the raised finding:**

1. **`C6` had been passing `ECR-D-001` on a report that says it is not closed.** `VER-014` declares
   `status: ECR-D-001 NOT CLOSED`. The predicate scanned for `FAIL`, `NOT CLEARED` and `NOT
   VERIFIED`; that string contains none of them. `VER-014` §6 reads *"`LC-M04-EXIT` `C6` is not
   satisfied for it"* and `OI-V-11` said the same — **the index was more truthful than the
   instrument.** `VER-016` F-12 had examined this exact predicate and called it *"fail-safe"*.
2. **The reports were being read on a line ending.** `VER-001`…`VER-014` are CRLF and
   `VER-015`/`-016` are LF; parsing the raw decode rather than the DC-1-normalised text dropped
   every CRLF report silently. Held by `test_crlf_reports_parse`.

## 5 · Evidence

```
PYTHONPATH=src python -m aief_gate            C1..C7 PASS; LC-M04 CAD-READY: YES; exit 0
PYTHONPATH=src python -m aief_clearance       CLEARANCE OK; exit 0
PYTHONPATH=src python -m aief_params check    PARAMETERS OK, 105 derived from section 3; exit 0
PYTHONPATH=src python -m aief_exec check      X-06 PASS
PYTHONPATH=src python -m pytest tests/ -q     621 passed, 2 failed
```

The two failures are pre-existing and are both `ECR-D-006` — `V-24`'s one unverifiable registry
row and the pipeline stub downstream of it. Session baseline was 565 passed / 7 failed. No test
was weakened, skipped or deleted; 56 were added.

**Two false claims in `ENGINEERING.md` were corrected**, both of the stale-label class and both
found by running what they published rather than reading them. §1 offered
`git describe --tags --exact-match HEAD` as the verification of release `v0.10.0`; that command
**fails** — `HEAD` is `v0.10.0-16-g5e7ac74`, and it has not passed since the first commit after
the tag. §7 read *"`HEAD == origin/main`"*; `HEAD` is **14 commits ahead**, and push is deferred
to 2026-09-01. Both now name the command that decides them instead of asserting a result.

**`.gitattributes` was created by this task**, pinning `params/generated/*.csv` to LF. The
repository carried none, `core.autocrlf=true` is set, and the generated CSV is the one artifact
here compared byte-for-byte — a stash round-trip taken to measure the exec-layer baseline rewrote
it and broke `aief_params check`. Repaired at both ends: the attribute pins the artifact Fusion
imports, and `check` now compares DC-1-normalised content, as every other comparison in this
repository does. `VER-017` N-5.

`python -m aief_approval verify` exits **1** on two pre-existing defects (`APR-003` carries no
`subject_path`; the manifest is at a DC-1 no approval binds). **Every `spec/**` chain resolves**,
which is what `C5` reads. Recorded at `VER-017` N-4 because `ENGINEERING.md` publishes that
command without saying so.

## 6 · Decisions required, and taken

**Three, all by the human owner in one interactive pause**: the supersession relation (declare and
seal, over declaring it as-implemented or removing it); the `status` vocabulary (closed, over a
documented keyword slot); and the parameter CSV (derive from §3 under a check, over correcting the
prose). Every option presented and not approved is recorded in `ECR-D-012` and `APR-028`.

## 7 · Remaining dependencies

**None for CAD.** `ECR-D-006`, `CMP-BLOCK-004`/`-005` and `C-4` remain open and are excluded from
this gate by name. `VER-017` N-1 records that this round is role-separated but not
session-separated, and that a cold-context ratification is the residual.

## 8 · Exact next action

**OPEN FUSION 360.** Start at
`implementation/01_SEWCP-200_Cooling_Plate/SEWCP-200_CAD_Implementation_Package.md`, §6.
