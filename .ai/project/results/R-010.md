# R-010 — Execution Layer, After the Publication-Reachability Repair

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-009`](R-009.md)**, whose eleven pinned deliverable digests the repair
> pass moved. `R-009` is retained unedited apart from the two supersession fields, and its
> DC-1 as it now stands is sealed below.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-010
produced_by:
  task:      T-001
  role:      software.software-engineer
  session:   S-2026-08-09-07
status:      SUPERSEDED
superseded_by: R-011
supersedes:  R-009
supersedes_seal:
  path:   .ai/project/results/R-009.md
  digest: 6cc1c3efcd0d14e0a8ceac2288ddc6e4ffe4f5d76c9a1d9fa0adec252bd548aa

inputs:
    - path: .ai/BOOT.md
      digest: 5a97d6ad763f8720b3e9e5c9cf9be6ad62f6ba953b5b70eb78a3624b580b82db
    - path: .ai/core/CONTEXT_TIERS.md
      digest: dad8a47ad994cce45820b9b89f016872f259b7b44b10e8a87ed42f32e31ade4f
    - path: .ai/core/templates/TPL-task-package.md
      digest: a51c5d27fa44f85b3d6a291b28099161561e8cc367e2ce64dc8ae0553501e9b2
    - path: .ai/core/schemas/SCH-task.schema.json
      digest: e874fdd70b56f55b6205e26c7ef537e090e46b5f05ab2964f8b1ee4ad6cccc80
    - path: framework/framework.manifest.json
      digest: 920eb6eec217732152c452d51f01e471940df6f2e2ffe608c377fccc37814090
    - path: src/aief_stage6/digests.py
      digest: 311247d6fdb11ecffa160adad4a884371ee9d6a779dcfc5308ca8e15a358b0a4
    - path: src/aief_stage6/tokenizers.py
      digest: 26a6c5bd424aef5459b0049181158163793e38402733adf2ef3d74536caef95f

deliverables:
    - path: .ai/project/EXECUTION_ARCHITECTURE.md
      digest: 953a09e472e26dd17e8528d39d69b723b80d51d1c0eed8f2f984f4ecca9c9c11
    - path: src/aief_exec/__init__.py
      digest: b8813ebc7fb18c820721517de771f9ed8ca224971824e2e9680f227ef5f77b69
    - path: src/aief_exec/__main__.py
      digest: bc5bb5f1258963641692a3b9af630774caf46b13ce9e88301ad0a86dc5f75b3a
    - path: src/aief_exec/records.py
      digest: 022af653f936efd46189af078c35fe1b43fbdf21cbbc2da15fde5085b7a8e1eb
    - path: src/aief_exec/scope.py
      digest: 32f30988413d53ea3cc1f16ee10a62cf374f7d4badca595f406441892b86825c
    - path: src/aief_exec/graph.py
      digest: 403d85b6e10ee1b4fe07a586fe17e36d3e91104fd5cf53a6aeaeec8b3da4af0e
    - path: src/aief_exec/checks.py
      digest: 66dd66896af71a4d64bedafb3581d80d10e78127f48c059f5c81c75aa76dc1f7
    - path: tests/test_exec_records.py
      digest: f49a7d8440a7aae42e18eeea91ad2ea0b29f0db2885f1e16d88c309954219269
    - path: tests/test_exec_scope.py
      digest: fcd15ea501091ab89c8468c89b5e72d54c3e3102a2d961d03e94518e6be7337f
    - path: tests/test_exec_graph.py
      digest: b120fe60a985e4d88e00ca46ccd99f2b1ee2aa3ff63878bdd5c0e076ee1049f6
    - path: tests/test_exec_checks.py
      digest: 8a34793e30e14b6b4ed719704c2a9b1ff4efbb2eb375f4f7d73b2619fefd7735

acceptance:
  - id: AC-1
    outcome: FAIL
    evidence: |
      `PYTHONPATH=src python -m aief_exec check` reports 7 of 9 PASS and exits 1.
      X-01 through X-07 PASS. X-08 and X-09 FAIL, and neither fails for a defect
      in the layer: X-08 because the corrected measurement exceeds four tasks'
      declared caps, which are a project-manager decision and were deliberately
      not tuned to fit it; X-09 because five tasks are contracted to publish a
      record they have no lawful write path to, which is the open A4 decision.
      Both are enumerated under `unresolved` with the repair and its owner.

      The criterion itself is also now stale: it reads `X-01 through X-08`,
      written before X-09 existed. It is left as written rather than restated to
      fit the result - see U-5.
  - id: AC-2
    outcome: PASS
    evidence: |
      `python -m aief_exec brief T-002` emits the five entries T-002 declares
      mandatory and nothing else, and its reported total equals the resolved
      mandatory scope, TF-1 4910 / TF-2 6411.
  - id: AC-3
    outcome: PASS
    evidence: |
      `python -m aief_exec scope T-002`: mandatory resolved TF-1 4910 / TF-2
      6411 against the same files unanchored at TF-1 66437 / TF-2 87780. Factor
      13.5x TF-1, 13.7x TF-2, both above the required five.
  - id: AC-4
    outcome: PASS
    evidence: |
      All four clauses hold, first time across five records. `classify T-002
      T-004` is PARALLEL and prints the five comparisons the verdict rests on -
      two write/write and write/read pairs and both write/observe directions -
      each with its two operand sets and an EMPTY intersection, so the verdict
      is readable as the absence of three named hazards rather than asserted.
      `classify T-002 T-005` is CONFLICT on three reasons: one `[write/write]`
      and both `[write/observe]` directions, nine intersecting paths.
      `conflict_reasons` is empty for T-002 x T-003 and T-002 x T-004 and
      non-empty for T-002 x T-005. T-002 x T-003 remains BLOCKED on the
      unassigned qa-engineer role, which the output labels
      `blocked/precondition - not a scope hazard`; the criterion tests the scope
      property and the property holds, per the FIND-Q9-15 restatement.

      The T-002 x T-004 clause failed in R-009 because publishing R-009
      superseded R-008 while T-004 still consumed R-008. Re-pointing T-004 to
      R-010 in the same act that published it is what clears it here.
  - id: AC-5
    outcome: PASS
    evidence: |
      `git status --porcelain` lists exactly the same seven tracked
      modifications as before this pass - BINDING.md, FROZEN.md, OPEN_ITEMS.md,
      ROSTER.md, STATE.md, ENGINEERING.md, framework/framework.manifest.json -
      none of them written by it. Every path this pass wrote is inside
      `.ai/project/` and already untracked. No path under `.ai/core/`,
      `framework/` or `spec/` was written.
  - id: AC-6
    outcome: NOT MEASURED
    evidence: |
      Reported, not disposed. `pytest tests/` gives 372 passed, 5 failed. Two
      are in the Stage 6 partition - `test_stage6_coverage_and_build.py::
      test_v24_live_registry` and `test_stage6_pipeline_stub.py::
      test_full_pipeline_with_stub_families`, both on V-24 registry drift in
      `framework/framework.manifest.json` and the unregistered AIEF-AMD-014,
      neither touched by this pass. Three are the exec live-state tests this
      closure turned red by design - see U-3.

      The criterion cannot be disposed as written: its baseline is "the same 4
      pre-existing failures" and the Stage 6 partition shows 2, a count that
      moved under OI-C-09 and T-002 rather than under this pass. Restating that
      baseline is T-002's, not this record's. What is asserted here is the
      narrower fact this pass can establish: it wrote nothing under
      `src/aief_stage6/` or `tests/test_stage6_*.py` and introduced no Stage 6
      failure.

files_changed:
  - path: .ai/project/EXECUTION_ARCHITECTURE.md
    change: |
      §5 record schema gains the optional `observes` field. New §5.3 documents
      the observation surface: what it is, the three derived terms, and that
      `observes` is only the residue the derivation cannot reach. §11 replaced
      by the three-quantity split - `acquisition` gated, `revision` reported and
      non-monotonic inside the task's own write scope, `telemetry` unmeasurable
      and excluded. §12 gains the X-09 row and restates X-07 and X-08 onto what
      they now test. §14 gains item 9, the open A4 decision.
  - path: src/aief_exec/checks.py
    change: |
      X-09 added - publication reachability. X-08 gates `acquisition` alone and
      reports `revision` and `telemetry` separately. X-07 compares observation
      surfaces and prints the undeclared-observation notices.
  - path: src/aief_exec/graph.py
    change: |
      observation_surface and undeclared_observation added; conflict_reasons
      gains the `write/observe` limb and prefixes every reason with its hazard
      class; seal_epoch added as the historical/present-tense discriminator for
      an absent supersession seal.
  - path: src/aief_exec/scope.py
    change: |
      ChargedContext split into acquisition / revision / telemetry, with the
      non-monotonic members of `revision` listed by path.
  - path: src/aief_exec/records.py
    change: TaskRecord.observes, TaskRecord.result_paths and TaskRecord.effective_write_scope added.
  - path: src/aief_exec/__main__.py
    change: |
      `scope` prints the declared, derived-effective and observed surfaces and
      the three budget quantities separately; `plan` prints the observation
      notices.
  - path: tests/test_exec_scope.py, test_exec_graph.py, test_exec_checks.py, test_exec_records.py
    change: |
      Coverage for the budget split, the observation surface, the `write/observe`
      hazard limb, seal_epoch and X-09, plus the live-repository open-failure
      tests that record each remaining failure with the repair that clears it.
  - path: .ai/project/results/R-010.md
    change: This record. Published by the closure pass, superseding R-009.
  - path: .ai/project/results/R-009.md
    change: status CURRENT -> SUPERSEDED, superseded_by R-010. No other field touched.
  - path: .ai/project/tasks/T-004.md
    change: |
      consumes and read_scope.dependency re-pointed R-009 -> R-010 together, and
      the checkpoint prose corrected from R-008, two supersessions stale.
  - path: .ai/project/tasks/T-001.md
    change: R-010 added to `produces`.

validation:
  - check: X-01 through X-07
    outcome: |
      PASS on the live repository, with notices printed. Reproduce with
      `PYTHONPATH=src python -m aief_exec check`, which reports 7 of 9 and
      exits 1.
  - check: X-08, X-09
    outcome: |
      FAIL - deliberately. X-08 on T-002 (TF-1, TF-2), T-003 (TF-1), T-004
      (TF-1, TF-2) and T-006 (TF-1); X-09 on T-002 through T-006. No cap was
      changed and no `write_scope` was widened to clear either. See U-1, U-2.
  - check: supersession seal over R-009
    outcome: |
      Verified, and verified as present rather than by the alarm's silence.
      `graph.supersession_seal` resolves R-009's sealer to R-010 and its sealed
      digest to 6cc1c3efcd0d14e0a8ceac2288ddc6e4ffe4f5d76c9a1d9fa0adec252bd548aa,
      equal to the recomputed DC-1 of the file as it stands after its two
      supersession fields were written. `seal_epoch` is S-2026-08-09-06 and this
      record's session is S-2026-08-09-07, so an omitted seal here would have
      been an X-06 failure, not a notice. Perturbation on a scratch copy: a
      one-comment edit to R-009 moves its DC-1 to 2f67e2b3... and X-06 turns FAIL
      with `R-009: REWRITTEN AFTER SUPERSESSION`. The control is silent because
      it matched, and it is demonstrably not disabled.
  - check: pytest tests/test_exec_*.py
    outcome: |
      225 passed, 3 failed. Reproduce with
      `PYTHONPATH=src python -m pytest tests/test_exec_*.py -q`. The three are
      the live-state tests the repair pass wrote to fail exactly when this
      closure lands; they are recorded in U-3 and no test was weakened, skipped
      or deleted to hide one. The figure is stated with the command and the
      partition it covers, because FIND-Q9-19, Q9-25 and Q9-30 were three
      consecutive records carrying a suite figure that did not reproduce.
  - check: pytest tests/
    outcome: |
      372 passed, 5 failed - the three above plus two pre-existing Stage 6
      failures on V-24 registry drift. See AC-6.
  - check: AC-4 hazard comparisons
    outcome: |
      `classify T-002 T-004` PARALLEL, printing all five comparisons with EMPTY
      intersections; `classify T-002 T-005` CONFLICT on `[write/write]` and both
      `[write/observe]` directions over nine paths. The `write/observe` limb is
      what makes T-002 x T-005 a three-reason conflict rather than a one-reason
      one: the same nine paths are both a lost-update hazard and an evidence
      contamination hazard, and before this limb existed only the first was
      visible.

consumed:  [R-009]

notices:
  - id: N-1
    note: |
      IMPLEMENTED PENDING INDEPENDENT VERIFICATION. This record closes nothing
      and certifies nothing. Every item below is delivered and demonstrated by a
      named command; none is disposed. T-001's verifier is `qa-engineer` and
      LAW-05 requires the closing pass to be taken by a role that authored none
      of this.
  - id: N-2
    note: |
      What is deliberately not pinned, unchanged in reasoning from R-009 N-2.
      VER-009 is T-004's deliverable and pinning it made R-007 self-staling
      (FIND-Q9-13). Task records and EXEC.md are mutable registers and pinning
      them froze the backlog (FIND-Q9-14). STATE.md, BINDING.md and ROSTER.md
      stay dropped for the same reason R-009 dropped them. This narrows what
      X-06 can detect and is recorded as a decision, not left to be discovered.
  - id: N-3
    note: |
      src/aief_stage6/digests.py and tokenizers.py are pinned as inputs although
      they lie in another task's write scope. DC-1 and the two tokenizer
      families are load-bearing for every digest and every cost figure above. If
      this record goes STALE on either, that is the pin working.
  - id: N-4
    note: |
      The seal over R-009 is taken over R-009 **after** its two supersession
      fields were written, which is the transaction R-009 N-4 said had to be
      taken as one act and was not taken then. Verified after the fact: X-06
      raises no REWRITTEN AFTER SUPERSESSION row, and it is silent because the
      sealed digest equals the recomputed DC-1 of the file, not because the
      field is absent - an absent seal in a record published at or after the
      seal epoch is itself an X-06 failure, by `graph.seal_epoch`.
  - id: N-5
    note: |
      Identifier collision, reported rather than resolved. `A4` in X-09's notice
      and in §14 item 9 is the open **architecture** decision on whether
      `produces` carries a write grant. `A4` in STATE.md and AIEF-AMD-013 AMD-41
      is a role identifier in the CMP-BLOCK-006 determination. Two unrelated
      referents share a token; the layer already emits its own, so it is left as
      it stands and flagged for whoever disposes either.

unresolved:
  - id: U-1
    statement: |
      X-09 fails on T-002, T-003, T-004, T-005 and T-006: each declares
      `produces: R-nnn` and none has a write_scope pattern reaching
      `.ai/project/results/R-nnn.md`, so no one of them can lawfully publish the
      record it is contracted to publish. This is the reproduced publication
      defect and it is left failing deliberately. Neither repair may be taken
      from here - widening write_scope presumes the implicit grant, dropping
      `produces` discards the contract - because which is correct is open
      architecture decision A4, recorded at EXECUTION_ARCHITECTURE.md §14 item
      9. `effective_write_scope` computes and displays the grant that would
      close it and applies nothing.
    owner: project-manager
    blocks: X-09, AC-1
  - id: U-2
    statement: |
      X-08 fails on T-002 (TF-1 and TF-2), T-003 (TF-1), T-004 (TF-1 and TF-2)
      and T-006 (TF-1). These are the honest output of a corrected measurement,
      not a defect in it: `acquisition` charges the task record, the resolved
      mandatory and optional read scope and each consumed result whole. The caps
      live in task records and were deliberately not tuned to fit the
      measurement. T-001's own breach is reported and not gated because T-001 is
      COMPLETE and will not be dispatched again.

      Six numbers from one run, not six constants - T-005 crossed and recrossed
      its cap during R-009's pass without being touched, when a concurrent
      session created the deliverable it declares. Read the command, not the
      numbers.
    owner: project-manager
    blocks: X-08, AC-1
  - id: U-3
    statement: |
      Three exec tests are red, and publishing this record is what turned them
      red:

        tests/test_exec_checks.py::TestLiveRepositoryOpenFailures::
          test_x02_open_on_the_consumer_of_a_staled_result
        tests/test_exec_checks.py::TestLiveRepositoryOpenFailures::
          test_x06_open_on_the_result_that_pins_the_layer_it_describes
        tests/test_exec_graph.py::TestLivePlan::
          test_the_live_dependency_state_is_derived_end_to_end

      Each asserts the pre-closure live state unconditionally - X-02 FAIL, X-06
      FAIL, R-009 STALE, T-004 BLOCKED - and each carries a comment saying it is
      asserted that way on purpose, so that it "goes red the moment R-009 is
      republished, which forces the next state to be written down rather than
      inferred". They are not defects and they are not repaired here: the repair
      is a `tests/**` write, and this record pins all four exec test files by
      DC-1, so making it would stale R-010 the moment it landed. Whoever takes
      it must restate the three onto the post-closure state and then republish
      or supersede this record with the recomputed digests, in one act - the
      transaction R-009 N-4 described and did not take.

      There is no ordering that avoids this. A record that pins the layer is
      invalidated by any change to the layer, and a test that asserts the live
      state is invalidated by any change to the state. The two constraints meet
      here, and the honest end state is three named red tests rather than a
      green suite bought by editing an assertion.
    owner: software.software-engineer
  - id: U-4
    statement: |
      R-001 and R-007 are unsealed and cannot be retro-sealed: their successors
      were published before the field existed. `graph.seal_epoch` makes that a
      notice rather than a failure by taking the epoch from the records
      themselves - the earliest session in which a seal was actually published -
      so the rule is not applied backwards. R-007's own rewrite after
      supersession remains undetectable by the mechanism and is known only from
      its own confession.
    owner: software.software-engineer
  - id: U-5
    statement: |
      T-001's deliverable list still names `.ai/project/tasks/T-001.md through
      T-006.md` and `tests/test_exec_records.py, test_exec_scope.py,
      test_exec_graph.py, test_exec_checks.py` as single prose entries. Neither
      resolves to a path, so X-08 charges them nothing and says so on every run.
      T-001's AC-1 also still reads `X-01 through X-08`, written before X-09
      existed. T-004 carries the same class of drift in the halves this pass was
      not scoped to touch: `inputs` names R-001, AC-3 reads "R-008 is confirmed
      CURRENT at the time of audit", AC-4 names the R-007 finding closures, and
      the escalation condition names R-001. Only `consumes`, the coupled
      `read_scope.dependency` and the checkpoint prose were re-pointed, because
      those three are what the derivation reads; the rest is the verifier's
      contract and restating it is a project-manager act, not a side effect of a
      closure pass.
    owner: project-manager
  - id: U-7
    statement: |
      A verifier of this record will read R-010's account of `src/aief_exec/**`
      and `tests/test_exec_*.py` written by a pass that did not write them. This
      closure pass wrote only `.ai/project/EXECUTION_ARCHITECTURE.md` and the
      four records; the layer itself was delivered by the preceding repair pass
      under the same task. The digests are recomputed and pinned here, so what
      is published is the bytes; the narrative of items 1-5 of the conclusion is
      read off the code and its tests and should be re-derived, not trusted.
    owner: project-manager
  - id: U-6
    statement: |
      F-007 remains open with chief-systems-engineer - an authority citation is
      checked for form and never for referent. Unchanged by this pass.
    owner: chief-systems-engineer

conclusion: |
  The execution layer as delivered by the repair pass under T-001, published and
  sealed. IMPLEMENTED PENDING INDEPENDENT VERIFICATION - nothing here is closed
  and nothing here is self-certified.

  WHAT THE REPAIR PASS DELIVERED

  1. X-09, publication reachability. X-05 checks that a `produces` or `consumes`
     target exists as a graph node; nothing checked whether the channel between
     them could carry anything. X-09 does: every `produces: R` must have a
     lawful write path to its record, every `consumes: R` must name a result some
     task produces, and no two tasks may produce the same R. It fails on five of
     six tasks on the live tree, which is the finding - see U-1.

  2. The observation surface, derived rather than declared. A task's evidence can
     be contaminated by a writer it neither reads from nor writes to: the surface
     is `write_scope` ∪ resolved deliverables ∪ the deliverable pins of every
     consumed result, and the declared `observes` list is only the residue those
     three cannot reach. `conflict_reasons` gains a third hazard limb,
     `write/observe`, alongside `write/write` and `write/read`, and every reason
     is now prefixed with its class so a reader need not parse prose. What stays
     undeclared stays undetectable, and the heuristic notice says so in those
     words.

  3. PARALLEL verdicts are explainable. A pair no longer reports a bare class;
     the reasons carry the hazard that fired and the paths that witness it, so a
     PARALLEL verdict can be read as the absence of three named hazards rather
     than as an assertion.

  4. The context budget split three ways. One number cannot gate a dispatch when
     part of it exists only after the dispatch - T-005 charged TF-1 1,411 against
     a cap of 1,500 before it ran and 7,161 after, on its own new deliverable.
     `acquisition` (record + mandatory + optional + dependency) is stable across
     the task's own execution and is the only quantity gated. `revision` is real
     cost, reported, and flagged non-monotonic wherever the path lies inside the
     task's own write scope. `telemetry` is unmeasurable from a repository that
     holds no execution trace, and is excluded rather than estimated - it is not
     small, and saying "cannot see" instead of "zero" is the whole content of the
     field.

  5. `seal_epoch`, the discriminator that keeps the supersession seal from being
     applied backwards. R-001 and R-007 were superseded before the field existed;
     failing them for omitting it would be a rule run in reverse. The epoch is
     derived from the records alone - the earliest session in which a seal was
     actually published - so an omission before it is history and reported, and
     an omission at or after it is a present-tense failure. Two residuals are
     stated in the function rather than hidden: the rule is vacuous before any
     seal exists, and a falsified `produced_by.session` evades it, which is a
     different defect from an absent seal.

  WHAT A DOWNSTREAM AGENT NEEDS TO KNOW

  1. Read `.ai/project/EXEC.md` after boot - a bounded index, one task id per
     line, naming the state of every task.
  2. Read the single `tasks/T-nnn.md` you are assigned. Read no other.
  3. Run `python -m aief_exec brief T-nnn` for the resolved minimum context, and
     `python -m aief_exec scope T-nnn` to see the three budget quantities, which
     of your charge moves while you work, and what you are observing.
  4. Classification is computed, never declared. So is supersession: a record is
     superseded when a successor says so, whatever it says of itself.
  5. `check` reports 7 of 9 and exits 1. X-08 and X-09 are open, enumerated
     under `unresolved` with the record and the role that owns each repair.
     Neither is cleared by widening a scope or raising a cap, and neither was.
     Do not read a green `check` as this layer's acceptance criterion until U-1
     and U-2 are disposed - and do not read a green suite as one until U-3 is.

  MEASURED EFFECT

  T-002's mandatory scope resolves to TF-1 4,910 / TF-2 6,411 against TF-1 66,437
  / TF-2 87,780 for the same files unanchored - 13.5x and 13.7x. Reproduce with
  `python -m aief_exec measure T-002`, which prints the envelope definition
  alongside the numbers.

  No sweep figure is stated here. FIND-Q9-31 established that the envelope
  contains the result and verification registers, so it moves whenever an audit
  or a result is filed - including by the filing of this record. A number that
  changes when it is written down is a measurement, not a constant. Run the
  command.
```

## Notes

**Immutability.** Seven inputs and eleven deliverables are DC-1-pinned, and `R-009` is
sealed by a nineteenth digest taken over the whole record after its supersession fields were
written. `X-06` recomputes the eighteen pins on every run and compares the seal; any drift
makes this result STALE, and any subsequent edit to `R-009` raises
`REWRITTEN AFTER SUPERSESSION`.

**Status.** `IMPLEMENTED PENDING INDEPENDENT VERIFICATION`. This record does not close a
finding and does not certify itself. `T-001`'s verifier is `qa-engineer`; `LAW-05` requires
the pass that disposes the open items above to be taken by a role that authored none of it.
