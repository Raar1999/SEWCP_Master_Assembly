# R-012 — Execution Layer, After the Pass-5 Findings Repair

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-011`](R-011.md)**, whose pinned architecture, layer and test digests this
> pass moved while correcting the five MAJOR and two MINOR findings `VER-009` pass 5 raised.
> `R-011` is retained unedited apart from the two supersession fields, and its DC-1 as it now
> stands is sealed below.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-012
produced_by:
  task:      T-001
  role:      software.software-engineer
  session:   S-2026-08-09-10
status:      SUPERSEDED
superseded_by: R-013
supersedes:  R-011
supersedes_seal:
  path:   .ai/project/results/R-011.md
  digest: a74a507e1daca9d79aa71a9bf9a0b97320eff9d87323205afba39c1d22b8b03a

inputs:
    - path: .ai/BOOT.md
      digest: 5a97d6ad763f8720b3e9e5c9cf9be6ad62f6ba953b5b70eb78a3624b580b82db
    - path: .ai/core/CONTEXT_TIERS.md
      digest: dad8a47ad994cce45820b9b89f016872f259b7b44b10e8a87ed42f32e31ade4f
    - path: .ai/core/templates/TPL-task-package.md
      digest: a51c5d27fa44f85b3d6a291b28099161561e8cc367e2ce64dc8ae0553501e9b2
    - path: .ai/core/schemas/SCH-task.schema.json
      digest: e874fdd70b56f55b6205e26c7ef537e090e46b5f05ab2964f8b1ee4ad6cccc80
    - path: framework/framework.manifest.json
      digest: 920eb6eec217732152c452d51f01e471940df6f2e2ffe608c377fccc37814090
    - path: src/aief_stage6/digests.py
      digest: 311247d6fdb11ecffa160adad4a884371ee9d6a779dcfc5308ca8e15a358b0a4
    - path: src/aief_stage6/tokenizers.py
      digest: 26a6c5bd424aef5459b0049181158163793e38402733adf2ef3d74536caef95f

deliverables:
    - path: .ai/project/EXECUTION_ARCHITECTURE.md
      digest: daabdd5aa4cc74d8913ed46643f52749da2c98c248466bd8ee4af0ce93204783
    - path: src/aief_exec/__init__.py
      digest: b8813ebc7fb18c820721517de771f9ed8ca224971824e2e9680f227ef5f77b69
    - path: src/aief_exec/__main__.py
      digest: 3df115976b558eb0d789204887481225594c4888fea9b6bbf402b498bcb0d138
    - path: src/aief_exec/records.py
      digest: 022af653f936efd46189af078c35fe1b43fbdf21cbbc2da15fde5085b7a8e1eb
    - path: src/aief_exec/scope.py
      digest: 10846e1517e43ef3c6fe0c8a958604d188a42f05dd311c9d2ea47a6dc8e436e3
    - path: src/aief_exec/graph.py
      digest: e3328f97b43d1ebd0b795b044afe1f17a9c1a34d979d983c48252e94bb1eb870
    - path: src/aief_exec/checks.py
      digest: 06ac66e34e0a63f28abe07f086606d4529bfa6c806ccf2dd5dd7d0db1dd7c9cb
    - path: tests/test_exec_records.py
      digest: f49a7d8440a7aae42e18eeea91ad2ea0b29f0db2885f1e16d88c309954219269
    - path: tests/test_exec_scope.py
      digest: 2a8e653666ddb8acb581d6d3731031f733d73a36da318ea99958fb1d048ef089
    - path: tests/test_exec_graph.py
      digest: e00e36cae3144789b51beea886bddfe9624652143d30ef7467a9f587595e4df4
    - path: tests/test_exec_checks.py
      digest: 189a80bf55cf100d3299cef0216720d006366fd0578c8f853d1cfd0fa59b1347

acceptance:
  - id: AC-1
    outcome: FAIL
    evidence: |
      `PYTHONPATH=src python -m aief_exec check` reports 7 of 9 PASS and exits 1.
      X-01 through X-07 PASS. X-08 and X-09 FAIL, and neither fails for a defect
      in the layer. Both now carry MORE rows than they did under R-011, which is
      the repair working rather than a regression:

        * X-08 gains four rows. Two verdicts are reported against the one
          declared cap - the `acquisition` gate as before, and the new
          `total_measurable` bound that makes the excluded `revision` visible
          (FIND-Q9-37). T-004 and T-005 breach the bound; T-005 passes the gate.
        * X-09 gains five rows, all on T-001, all from the new converse mode
          (FIND-Q9-42): T-001's write scope reaches R-002 through R-006, which
          it does not produce.

      No cap was changed and no write_scope was widened by this pass. The six
      X-08 gate rows and the five X-09 mode-1 rows are the same rows over the
      same tasks for the same reasons as under R-011.

      The criterion itself is still stale: it reads `X-01 through X-08`, written
      before X-09 existed. Left as written rather than restated to fit the
      result - see U-5.
  - id: AC-2
    outcome: PASS
    evidence: |
      `python -m aief_exec brief T-002` emits the five entries T-002 declares
      mandatory and nothing else, and its reported total equals the resolved
      mandatory scope, TF-1 4910 / TF-2 6411. Unchanged by this pass.
  - id: AC-3
    outcome: PASS
    evidence: |
      `python -m aief_exec scope T-002`: mandatory resolved TF-1 4910 / TF-2
      6411 against the same files unanchored at TF-1 66437 / TF-2 87780. Factor
      13.5x TF-1, 13.7x TF-2, both above the required five. Unchanged by this
      pass.
  - id: AC-4
    outcome: PASS
    evidence: |
      All four clauses hold, and one of them holds for a different reason than
      it did under R-011, which is worth stating rather than leaving to be
      discovered.

      `conflict_reasons` is empty for T-002 x T-003 and for T-002 x T-004, and
      non-empty for T-002 x T-005 - the scope property AC-4 tests, unchanged.
      `classify T-002 T-005` is CONFLICT on `[write/write]` and both
      `[write/observe]` directions. `classify T-002 T-004` is PARALLEL and
      prints the five comparisons the verdict rests on, each with its two
      operand sets and an EMPTY intersection.

      WHAT MOVED. The write/read comparison in that explanation is now taken
      over the whole read surface - record, mandatory, optional and the consumed
      result record - rather than mandatory alone. T-002 x T-004 stays PARALLEL
      because none of T-004's four acquisition components lies in
      `src/aief_stage6/**`. That verdict is now a narrower and better-evidenced
      claim than it was, and it is still not a complete one: see U-8.

      T-002 x T-003 remains BLOCKED on the unassigned qa-engineer role, labelled
      `blocked/precondition - not a scope hazard`.
  - id: AC-5
    outcome: PASS
    evidence: |
      `git status --porcelain` lists exactly the same seven tracked
      modifications as before this pass - BINDING.md, FROZEN.md, OPEN_ITEMS.md,
      ROSTER.md, STATE.md, ENGINEERING.md, framework/framework.manifest.json -
      none of them written by it. Every path this pass wrote is either inside
      `.ai/project/` or under `src/aief_exec/` and `tests/test_exec_*.py`, and
      every one of those was already untracked. No path under `.ai/core/`,
      `framework/` or `spec/` was written.
  - id: AC-6
    outcome: NOT MEASURED
    evidence: |
      Reported, not disposed, and still not disposable as written - its baseline
      reads "the same 4 pre-existing failures" against a Stage 6 partition that
      shows 2, a count that moved under OI-C-09 and T-002. Restating that
      baseline is T-002's act.

      What this pass can establish is narrower and is asserted: it wrote nothing
      under `src/aief_stage6/` or `tests/test_stage6_*.py`, and
      `pytest tests/test_exec_*.py` is 257 passed, 0 failed. The Stage 6 suite
      was not run by this pass, by contract.

files_changed:
  - path: src/aief_exec/scope.py
    change: |
      `acquisition_units` added - the single enumeration of the pre-dispatch
      surface, read by both the cost model and `graph.read_surface`, so the
      FIND-Q9-38 asymmetry cannot return by editing one of them.
      `charged_context` now builds its unit list from it and drops only what it
      cannot measure. `ChargedContext` gains `write_scope`,
      `is_self_referential`, `moving_by_component`, `acquisition_stable`,
      `acquisition_self_referential` and `total_measurable`. The class
      docstring's invariance claim and the self-referential notice were both
      false and are rewritten.
  - path: src/aief_exec/graph.py
    change: |
      `read_surface` added and used by the `write/read` limb of both
      `conflict_reasons` and `safety_explanation`; `_reached` attributes the
      hazard per entry so every reached component is named. `SEAL_EPOCH`
      declared as a constant, `seal_epoch` returns it, `derived_seal_epoch`
      retains the old derivation as evidence. The false monotonicity claim in
      `result_currency` and the false "Maximal" claim in `parallel_sets` are
      corrected. `build_plan` evaluates BLOCKED before CONFLICT and keeps the
      scope reasons.
  - path: src/aief_exec/checks.py
    change: |
      X-06 reports any disagreement between the declared epoch and the records.
      X-08 emits the `total_measurable` verdict as a failure and reports
      `acquisition = stable + self-referential` with the moving paths attributed
      by component. X-09 gains mode 5, the converse publication guard.
  - path: src/aief_exec/__main__.py
    change: |
      `scope` prints the stable/self-referential split, the moving paths in
      their own block with the component that holds each, and
      `total_measurable` labelled as bounded. The moving-path count used to
      decorate the `REVISION` line, which was FIND-Q9-36b repeated in the CLI:
      for T-002 it printed "revision TF-1 0 ... NON-MONOTONIC in 2 path(s)"
      about two paths in `mandatory`.
  - path: tests/test_exec_scope.py
    change: |
      `TestAcquisitionIsNotInvariant` added - seven tests, including the
      auditor's +145% checkpoint case and the live T-002 shape.
      `test_a_self_referential_budget_is_reported` restated off the notice
      wording FIND-Q9-36b condemned and onto component attribution.
  - path: tests/test_exec_graph.py
    change: |
      `TestReadSurface`, `TestPairClassOrdering` and
      `TestParallelSetsAreNotMaximal` added - fourteen tests, including the E1
      matched pair, the live T-001 pairs, and an assertion that E4 remains open.
      `test_the_live_dependency_state_is_derived_end_to_end` restated to walk
      the supersession chain instead of naming R-011.
  - path: tests/test_exec_checks.py
    change: |
      Mode 5, the seal-strip tamper, the total_measurable failure and the
      component-attributed notice are covered by eight added tests. Seven of the
      nine restatements are here: the two X-02/X-06 node ids R-010 U-3 names,
      the X-08 honesty test, the X-09 offender test, the deliverable-gating test
      and the two seal-epoch tests. N-6 lists all nine with the reason for each.
  - path: .ai/project/EXECUTION_ARCHITECTURE.md
    change: |
      §6.1 extended and §6.2 added - the declared seal epoch and what the seal
      does and does not guarantee. §7 restated onto the read surface, the pair
      class ordering and the first-fit grouping. §11 corrected: the false
      "Stable across the task's own execution" is replaced by the dispatch-time
      statement, and `total_measurable` is added to the quantity table. §12 rows
      for X-06, X-07, X-08 and X-09 restated. §14 items 10, 11 and 12 added.
  - path: .ai/project/results/R-012.md
    change: This record.
  - path: .ai/project/results/R-011.md
    change: status CURRENT -> SUPERSEDED, superseded_by R-012. No other field touched.
  - path: .ai/project/tasks/T-004.md
    change: consumes and read_scope.dependency re-pointed R-011 -> R-012, with the two checkpoint lines naming R-011.
  - path: .ai/project/tasks/T-001.md
    change: R-012 added to `produces`.

validation:
  - check: X-01 through X-07
    outcome: |
      PASS on the live repository, with notices printed. Reproduce with
      `PYTHONPATH=src python -m aief_exec check`, which reports 7 of 9 and exits
      1. X-02 and X-06 went FAIL mid-pass, exactly as R-011 N-7 predicts they
      must - this pass rewrote four files R-011 pinned - and are cleared by this
      record, not by any change to a check.
  - check: X-08
    outcome: |
      FAIL, on ten rows rather than six. Six `acquisition` gate rows unchanged
      from R-011: T-002 (TF-1, TF-2), T-003 (TF-1), T-004 (TF-1, TF-2), T-006
      (TF-1). Four new `total_measurable` bound rows: T-004 (TF-1, TF-2) and
      T-005 (TF-1, TF-2). T-005 is the case FIND-Q9-37 was raised for - it
      passes the gate at TF-1 1,411 against 1,500 and breaches the bound at
      7,161, 80% of which is the deliverable it must read to rewrite. T-001's
      breaches of both verdicts are reported and not gated because T-001 is
      COMPLETE. No cap was changed.
  - check: X-09
    outcome: |
      FAIL, on ten rows rather than five. Five mode-1 rows unchanged: T-002
      through T-006 each produce a record no write_scope pattern reaches. Five
      new mode-5 rows, all T-001: its write scope reaches R-002 through R-006,
      five records it does not declare. Neither is repaired here - both turn on
      open decision A4. No write_scope was widened.
  - check: supersession seal over R-011
    outcome: |
      Verified as present, not by the alarm's silence.
      `graph.supersession_seal` resolves R-011's sealer to R-012 and its sealed
      digest to a74a507e1daca9d79aa71a9bf9a0b97320eff9d87323205afba39c1d22b8b03a,
      equal to the recomputed DC-1 of R-011 as it stands after its two
      supersession fields were written - which is when the digest was taken.
      `seal_epoch` is the declared constant S-2026-08-09-06 and this record's
      session is S-2026-08-09-10, so an omitted seal here would have been an
      X-06 failure and not a notice.
  - check: the seal chain behind R-011
    outcome: |
      Unbroken and re-verified rather than assumed. R-009's seal over R-008,
      R-010's seal over R-009 and R-011's seal over R-010 all still equal the
      recomputed DC-1 of their sealed files. R-001 and R-007 remain unsealed and
      unsealable - see U-4.
  - check: the seal control under the FIND-Q9-39 tamper
    outcome: |
      Reproduced on a synthetic chain and now guarded by a test. Stripping every
      `supersedes_seal` from a fully sealed chain returned X-06 to PASS with
      zero details under the derived epoch. Under the declared constant it
      returns FAIL, naming each successor that dropped its seal, and X-06
      additionally prints the disagreement between the constant and the now
      empty derivation. `tests/test_exec_checks.py::TestX06SealEpoch::
      test_stripping_every_seal_does_not_disarm_the_rule`.
  - check: pytest tests/test_exec_*.py
    outcome: |
      257 passed, 0 failed - 228 before this pass, 29 added (7 in
      test_exec_scope, 14 in test_exec_graph, 8 in test_exec_checks), none
      removed, none skipped, xfailed or weakened. Reproduce with
      `PYTHONPATH=src python -m pytest tests/test_exec_*.py -q`. Nine tests were
      restated, each carrying in its own body what changed and why; N-6 lists
      them.
  - check: new tests fail against the unrepaired behaviour
    outcome: |
      Established by mutation rather than asserted. Seven mutants, one per
      finding, each reverting exactly the mechanism the fix introduced, applied
      to a scratch copy of `src/` and reverted by digest-verified restore. Every
      one is killed by the tests added for that finding. N-8 carries the table.
  - check: AC-4 hazard comparisons
    outcome: |
      `classify T-002 T-004` PARALLEL, printing all five comparisons with EMPTY
      intersections and the read-surface operands now shown in full;
      `classify T-002 T-005` CONFLICT. Re-run because T-004's read surface is
      derived partly from the result record it consumes, and this pass changed
      which record that is.

consumed:  [R-011]

notices:
  - id: N-1
    note: |
      IMPLEMENTED PENDING INDEPENDENT VERIFICATION. This record closes nothing
      and certifies nothing. Every item below is delivered and demonstrated by a
      named command; none is disposed. `VER-009` pass 5 raised FIND-Q9-36, 36b,
      37, 38, 39, 40, 41 and 42; this pass repairs seven of the eight and
      discloses the eighth as open. Whether each repair holds is `qa-engineer`'s
      to decide, and LAW-05 requires the closing pass to be taken by a role that
      authored none of this.
  - id: N-2
    note: |
      What is deliberately not pinned, unchanged in reasoning from R-011 N-2.
      VER-009 is T-004's deliverable and pinning it made R-007 self-staling
      (FIND-Q9-13). Task records and EXEC.md are mutable registers and pinning
      them froze the backlog (FIND-Q9-14). STATE.md, BINDING.md and ROSTER.md
      stay dropped for the same reason. This narrows what X-06 can detect and is
      recorded as a decision, not left to be discovered.
  - id: N-3
    note: |
      src/aief_stage6/digests.py and tokenizers.py are pinned as inputs although
      they lie in another task's write scope. DC-1 and the two tokenizer
      families are load-bearing for every digest and every cost figure above. If
      this record goes STALE on either, that is the pin working - and it is the
      same coupling U-8 records as an undetected hazard, met from the one side
      this layer can see.
  - id: N-4
    note: |
      The seal over R-011 is taken over R-011 **after** its two supersession
      fields were written. A seal taken before the closing edit seals a version
      that no longer exists.
  - id: N-5
    note: |
      Identifier collision, carried forward from R-011 N-5. `A4` in X-09's
      notices and in EXECUTION_ARCHITECTURE.md §14 item 9 is the open
      architecture decision on whether `produces` carries a write grant. `A4` in
      STATE.md and AIEF-AMD-013 AMD-41 is a role identifier. Two unrelated
      referents share a token; left as it stands and flagged.
  - id: N-6
    note: |
      The nine restated tests, named so that no reader has to infer from a
      green suite which assertions moved. None was skipped, xfailed, deleted or
      reduced to a bare status comparison, and each carries in its own body what
      changed and why.

        test_exec_scope.py::TestChargedContext::
          test_a_self_referential_budget_is_reported
        test_exec_checks.py::TestLiveRepositoryOpenFailures::
          test_x02_open_on_the_consumer_of_a_staled_result
          test_x06_open_on_the_result_that_pins_the_layer_it_describes
          test_x08_open_because_the_measurement_is_now_honest
          test_x09_open_on_five_tasks_that_cannot_publish_what_they_produce
        test_exec_checks.py::TestVer009Regressions::
          test_x08_measures_an_existing_deliverable_without_gating_on_it
        test_exec_checks.py::TestX06SealEpoch::
          test_the_epoch_is_declared_and_the_derivation_is_only_evidence
          test_with_no_seal_anywhere_the_rule_is_no_longer_vacuous
        test_exec_graph.py::TestLivePlan::
          test_the_live_dependency_state_is_derived_end_to_end

      Four of them were restated because the property they asserted was one a
      finding removed, and that is worth separating from the five restated
      because the live state moved.

        * `test_a_self_referential_budget_is_reported` required the notice to
          end "this is why `revision` ... is reported and `acquisition` ... is
          the gated quantity". FIND-Q9-36b is that this sentence is a
          non-sequitur, so requiring it in a test was requiring the falsehood.
          It now requires component attribution and the gate's own split, which
          is strictly more.
        * `test_x08_open_because_the_measurement_is_now_honest` asserted that
          every X-08 detail is an acquisition breach. FIND-Q9-37 makes that
          false by design. It now partitions the details between the two
          verdicts and asserts properties of each, which is strictly more.
        * The two seal-epoch tests carried the derived epoch's semantics in
          their node ids - "the epoch is the earliest sealing session", "with no
          seal anywhere the rule is vacuous". Both are properties FIND-Q9-39
          removed. Their names changed with their content, which is the only
          honest thing to do when a node id states a property that is no longer
          claimed; the old names are recorded here so the mapping is not lost.

      `test_x08_measures_an_existing_deliverable_without_gating_on_it` is the
      one restatement that asserts less about a row status and more about the
      thing the row is made of. It asserted the whole X-08 row is PASS before
      and after the deliverable exists. That can only stay true while the
      excluded cost is invisible, which is the finding. It now asserts that no
      `acquisition` detail appears either way, that the gated figure is
      byte-identical either way, and that the new detail is the separately
      labelled bound and nothing else. The FIND-Q9-35 property is intact and is
      asserted with more precision than before.
  - id: N-7
    note: |
      Ordering, unchanged in mechanism from R-011 N-7 and load-bearing again.
      A record that pins the layer is staled by any change to the layer. The
      code and test work therefore lands first, in full, and the digests are
      taken after it; the record published from those digests is current the
      moment it lands. Two earlier attempts at this repair failed by publishing
      before the last edit. Nothing after step one touches a pinned path.
  - id: N-8
    note: |
      Why the added tests are believed to bite, established by mutation on a
      throwaway copy of `src/` and not by inspection. Each mutant reverts
      exactly the mechanism one fix introduced; the copy was restored by
      digest-verified overwrite afterwards, so no pinned byte moved.

      Every mutant was killed. Counts are green-before / red-under-mutant over
      the tests named for that finding.

      | Mutant | Reverts | Finding | Before | Under mutant |
      |---|---|---|---|---|
      | V1 | `read_surface` back to mandatory-only | Q9-38 | 8 passed | **6 failed**, 2 passed |
      | V2 | `seal_epoch` back to the `min()` derivation | Q9-39 | 7 passed | **3 failed**, 4 passed |
      | V3 | `is_self_referential` always False - no gate split | Q9-36/36b | 9 passed | **6 failed**, 3 passed |
      | V4 | X-08 emits no `total_measurable` verdict | Q9-37 | 3 passed | **3 failed** |
      | V5 | X-09 mode 5 never fires | Q9-42 | 12 passed | **3 failed**, 9 passed |
      | V6 | `build_plan` evaluates CONFLICT before BLOCKED | Q9-41 | 3 passed | **1 failed**, 2 passed |
      | V7 | `parallel_sets` summary line back to "Maximal groups" | Q9-40 | 3 passed | **1 failed**, 2 passed |

      The surviving passes in each row are the controls - a mutant that killed
      its whole class would be a class testing one thing. V1's two survivors are
      the structural guard, which the mutant satisfies vacuously, and the E4
      residual test, which asserts something the mutant does not touch.

      One mutant survived its first run and the test was strengthened rather
      than the result reported: V7's first form replaced only the docstring's
      opening lines, and the assertion looked at the whole docstring, which
      still carried the corrected wording further down. The assertion now reads
      the summary line - the line a reader and every doc tool actually sees -
      and V7 replaces the docstring whole.
  - id: N-9
    note: |
      Reproduce the mutation campaign: for each row of N-8, apply the single
      textual revert to the named function on a copy of `src/`, run
      `PYTHONPATH=<copy> python -m pytest <the named tests>`, and restore. No
      mutant is left in the tree and none was applied to a pinned file without
      being reverted and re-digested.

unresolved:
  - id: U-1
    statement: |
      X-09 fails on ten rows and neither mode may be repaired from here.

      Mode 1, carried forward from R-011 U-1 unchanged: T-002 through T-006 each
      declare `produces: R-nnn` with no write_scope pattern reaching
      `.ai/project/results/R-nnn.md`.

      Mode 5 is new with FIND-Q9-42 and fires five times on T-001, whose
      write_scope `.ai/project/results/**` reaches R-002 through R-006 - five
      records declared by five other tasks. It was raised because X-09 guarded
      the publication channel in one direction only: it verified that a declared
      producer *can* write, and never that a non-producer *cannot*. Compounded
      by FIND-Q9-38 H1 that was concrete - T-001 x T-002 was PARALLEL, so T-001
      could have published R-002 beside its declared producer with all nine
      checks clean. Both halves of that are now visible: the pair is CONFLICT
      and the reach is a failure.

      Both modes turn on open architecture decision A4, recorded at
      EXECUTION_ARCHITECTURE.md §14 item 9. Widening a write_scope presumes the
      implicit grant; narrowing T-001's would remove the only lawful publisher
      this project has. `effective_write_scope` still computes the grant that
      would close mode 1 and applies nothing.
    owner: project-manager
    blocks: X-09, AC-1
  - id: U-2
    statement: |
      X-08 fails on ten rows and the caps were not touched.

      Six gate rows, carried forward from R-011 U-2: T-002 (TF-1, TF-2), T-003
      (TF-1), T-004 (TF-1, TF-2), T-006 (TF-1). Four bound rows, new with
      FIND-Q9-37: T-004 and T-005, both families.

      The bound rows are the finding, not a defect in it. `revision` is real,
      mandatory, pre-dispatch cost - an artifact being rewritten must be read.
      FIND-Q9-37 measured it at 53% of T-004's measurable input, 85% of T-001's
      and 80% of T-005's; recomputed on the tree as this record is published it
      is 58%, 87% and 80%. Those figures move because two of the three
      artifacts in question are audit files that grow as they are written, which
      is why the command is cited and not the number. For T-004 the excluded
      item is VER-009 itself, which T-004's own AC-4 makes mandatory reading.
      Reproduce with `python -m aief_exec scope T-004`. It is bounded against
      the same declared cap
      because that cap is the only bound the repository declares and inventing a
      second here would be resolving by assumption.

      What this leaves for project-manager is sharper than before: T-005 passes
      the gate and fails the bound, so the question "what should a cap bound"
      can no longer be answered by looking only at rows that were already red.
    owner: project-manager
    blocks: X-08, AC-1
  - id: U-4
    statement: |
      R-001 and R-007 are unsealed and cannot be retro-sealed: their successors
      were published before the field existed. The seal epoch makes that a
      notice rather than a failure. Carried forward from R-011 U-4 with one
      change of substance: the epoch is no longer derived from the records.
      FIND-Q9-39 showed a derived epoch can be emptied by deleting the seals it
      is derived from, which returned X-06 to PASS after a three-record tamper.
      It is now the declared constant S-2026-08-09-06, whose authority is
      R-009's published session and which is recorded in
      EXECUTION_ARCHITECTURE.md §6.2 as well as in code.

      The residual that remains: a successor declaring a `produced_by.session`
      earlier than the epoch still evades the rule. That evasion is a false
      session identifier, a different defect from an absent seal, and nothing
      here detects it.
    owner: software.software-engineer
  - id: U-5
    statement: |
      Register drift in the task records, carried forward from R-011 U-5 and
      still not this pass's to restate. T-001's deliverable list still names two
      prose entries that resolve to no path; its AC-1 still reads `X-01 through
      X-08`; its AC-6 baseline still reads "the same 4 pre-existing failures".
      T-004's `inputs` still names R-001, its AC-3 still reads "R-008 is
      confirmed CURRENT", its AC-4 still names the R-007 finding closures and
      its escalation condition still names R-001. Only `consumes`, the coupled
      `read_scope.dependency` and the two checkpoint lines were re-pointed,
      because those are what the derivation reads.
    owner: project-manager
  - id: U-6
    statement: |
      F-007 remains open with chief-systems-engineer - an authority citation is
      checked for form and never for referent. Unchanged by this pass.
    owner: chief-systems-engineer
  - id: U-8
    statement: |
      FIND-Q9-38 E4 is NOT repaired and is disclosed rather than closed. A task's
      runtime dependencies - the code it imports to generate its evidence - are
      on no declared surface, so `classify T-002 T-004` is PARALLEL while T-002
      writes `src/aief_stage6/**`, which holds `tokenizers.py`, the backend every
      token figure T-004 reports flows through. T-002 may replace the instrument
      T-004 measures with, mid-measurement, under a safe verdict.

      This is the one pair of the three the pass-5 finding names that did not
      flip, and it did not flip because closing it is a different act from the
      one this pass took. E1, E2 and E3 were surfaces the repository already
      declares and the cost model already charged; E4 is an import graph, which
      is not a declared scope and would have to be derived from code. Deriving
      it is a design decision with its own failure modes - a stale or
      over-broad import closure would make every task conflict with the
      toolchain - and taking it silently inside a defect-repair pass would be
      the defect class this audit has tracked throughout.

      It is disclosed in three places so that closing it later must come through
      them: `graph.read_surface`'s docstring, EXECUTION_ARCHITECTURE.md §14 item
      10, and `tests/test_exec_graph.py::TestReadSurface::
      test_runtime_tooling_stays_undetected_and_is_disclosed`, which asserts the
      residual as a residual and fails against a layer that claims to have
      closed it.
    owner: chief-systems-engineer
  - id: U-9
    statement: |
      FIND-Q9-38 E5, undeclared observation, is unchanged and remains a limit of
      the repository rather than of this layer: there is no execution trace, so
      what `pytest tests/` actually read is not recoverable.
      `graph.undeclared_observation` emits a heuristic notice on five T-001
      acceptance criteria and says plainly that it is a heuristic.
    owner: software.software-engineer
  - id: U-10
    statement: |
      The gate is honest about not being invariant, and that is a disclosure
      rather than a repair. FIND-Q9-36 has two lawful fixes and the repository
      chooses neither: exclude self-written paths from `acquisition`, or forbid
      a task's own record from lying inside its write scope. The first removes
      real cost an agent must load - the defect the audit found in the previous
      attempt - and the second collides with the checkpoint-update obligation in
      TPL-task-package. Both are chief-systems-engineer questions.

      What this pass did instead is measure and report the split: 34% of T-001's
      gated figure and 24% of T-002's are self-written, both figures printed on
      every run and attributed to the component that holds them. T-002 remains a
      live X-08 gate failure on a number it moves itself, and that fact is now
      stated by the check rather than found by an auditor.
    owner: chief-systems-engineer

conclusion: |
  The execution layer with the seven repairable findings of `VER-009` pass 5
  corrected and the eighth disclosed. IMPLEMENTED PENDING INDEPENDENT
  VERIFICATION - nothing here is closed and nothing here is self-certified.

  WHAT THIS PASS DID

  1. FIND-Q9-38, the surface asymmetry, repaired at the root. The cost model
     enumerated five surfaces and the hazard model compared three, and every
     escape but one was a surface the same package already computed and threw
     away before comparing. There is now one enumeration,
     `scope.acquisition_units`, and both models read it, so a component added to
     the charge is compared in the same edit. Three escapes close: optional read
     scope, the consumed result record itself, and the task's own contract. Two
     live pairs change verdict as a result - `T-001 x T-002` and
     `T-001 x T-005`, PARALLEL to CONFLICT, on T-001's reach over the contracts
     those tasks are executing. The third pair the finding names,
     `T-002 x T-004`, does not flip: see U-8, which says so rather than forcing
     it.

  2. FIND-Q9-39, the anti-monotonic epoch, repaired by removing the derivation
     from the rule. `seal_epoch` was `min()` over the records it polices, so
     deleting every seal emptied it and returned X-06 to PASS with zero details.
     It is a declared constant now, recorded in the architecture document as
     well as in code, and the old derivation is retained as evidence with X-06
     printing any disagreement. The docstring that claimed "tampering can only
     add the alarm, never remove it" now states what is actually guaranteed:
     per link, once a seal exists - not across the chain.

  3. FIND-Q9-36 and 36b, the moving gate, measured and attributed rather than
     hidden. Every charged unit is classified self-referential or stable,
     `acquisition = stable + self-referential` is printed for every task in
     every state, and each moving path is attributed to the component that holds
     it. The notice that told a reader T-002's problem was solved by the very
     number that carries it is gone. `record` was not dropped from the gate:
     an agent must read its contract, and removing real cost to make a number
     behave is the defect, not the repair.

  4. FIND-Q9-37, the excluded cost, made a failure. `total_measurable` carries
     its own verdict against the same declared cap, its breach is a detail and
     not a notice, and every such row is labelled `NON-MONOTONIC BOUND, not the
     acquisition gate`. `revision` was not folded into the gate - that would
     restore the non-monotonicity the split removed - and where `revision` is
     zero the bound row is suppressed because it would be the gate row printed
     twice under two labels.

  5. FIND-Q9-42, the one-directional guard, closed in the other direction.
     X-09 mode 5 fails a task whose write_scope reaches a result it does not
     declare, which is five live rows on T-001. It reports the reach and does
     not decide A4.

  6. The two MINORs. `parallel_sets` is first-fit greedy and now says so - the
     docstring was corrected rather than the algorithm, because maximal grouping
     is NP-hard and buys a larger batch rather than a safer one, and X-07
     re-verifies every group pairwise. `build_plan` evaluates BLOCKED before
     CONFLICT so the reported class is the one that decides dispatchability, and
     carries the scope reasons inside it so nothing is hidden by the reorder.
     COMPLETE is deliberately not folded into that test: it never clears, so it
     would bury `T-001 x T-002` permanently.

  7. Nothing else. No cap was changed, no write_scope was widened, no check was
     removed, no test was skipped, xfailed, deleted or weakened, and EXEC.md was
     not written. X-08 and X-09 fail on more rows than before, and every added
     row is a finding the layer previously could not see.

  WHAT A DOWNSTREAM AGENT NEEDS TO KNOW

  1. Read `.ai/project/EXEC.md` after boot - a bounded index, one task id per
     line, naming the state of every task.
  2. Read the single `tasks/T-nnn.md` you are assigned. Read no other.
  3. Run `python -m aief_exec brief T-nnn` for the resolved minimum context, and
     `python -m aief_exec scope T-nnn` to see the budget quantities: what is
     gated, how much of the gate your own work moves, what is bounded but not
     gated, and what cannot be measured at all.
  4. Classification is computed, never declared. So is supersession.
  5. `check` reports 7 of 9 and exits 1. X-08 and X-09 are open, enumerated
     under `unresolved` with the record and the role that owns each repair.
     Neither is cleared by widening a scope or raising a cap, and neither was.
  6. **A PARALLEL verdict is a claim about declared surfaces only.** It compares
     write scope, the four acquisition components and the observation surface.
     It does not see the code your task imports (U-8) and it does not see what a
     command you run reads (U-9). If your evidence depends on a tool another
     task owns, say so in `observes`; the layer cannot derive it for you.

  MEASURED EFFECT

  T-002's mandatory scope resolves to TF-1 4,910 / TF-2 6,411 against TF-1
  66,437 / TF-2 87,780 for the same files unanchored - 13.5x and 13.7x.
  Reproduce with `python -m aief_exec measure T-002`.

  No sweep figure is stated here. FIND-Q9-31 established that the envelope
  contains the result and verification registers, so it moves whenever an audit
  or a result is filed - including by the filing of this record. Run the command.
```

## Notes

**Immutability.** Seven inputs and eleven deliverables are DC-1-pinned, and `R-011` is
sealed by a nineteenth digest taken over the whole record after its supersession fields were
written. `X-06` recomputes the eighteen pins on every run and compares the seal; any drift
makes this result STALE, and any subsequent edit to `R-011` raises
`REWRITTEN AFTER SUPERSESSION`.

**Status.** `IMPLEMENTED PENDING INDEPENDENT VERIFICATION`. This record does not close a
finding and does not certify itself. `T-001`'s verifier is `qa-engineer`; `LAW-05` requires
the pass that disposes the findings above to be taken by a role that authored none of it.
