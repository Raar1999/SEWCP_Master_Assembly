# R-011 — Execution Layer, After the Live-State Test Closure

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-010`](R-010.md)**, two of whose pinned test-file digests this pass moved
> when it restated the three live-state tests `R-010` U-3 left red. `R-010` is retained
> unedited apart from the two supersession fields, and its DC-1 as it now stands is sealed
> below.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-011
produced_by:
  task:      T-001
  role:      software.software-engineer
  session:   S-2026-08-09-08
status:      SUPERSEDED
superseded_by: R-012
supersedes:  R-010
supersedes_seal:
  path:   .ai/project/results/R-010.md
  digest: 45516365790fd220ccd16be0b4a7dd25294cb3cb19f0301622a2d1d8bfcd1f12

inputs:
    - path: .ai/BOOT.md
      digest: 5a97d6ad763f8720b3e9e5c9cf9be6ad62f6ba953b5b70eb78a3624b580b82db
    - path: .ai/core/CONTEXT_TIERS.md
      digest: dad8a47ad994cce45820b9b89f016872f259b7b44b10e8a87ed42f32e31ade4f
    - path: .ai/core/templates/TPL-task-package.md
      digest: a51c5d27fa44f85b3d6a291b28099161561e8cc367e2ce64dc8ae0553501e9b2
    - path: .ai/core/schemas/SCH-task.schema.json
      digest: e874fdd70b56f55b6205e26c7ef537e090e46b5f05ab2964f8b1ee4ad6cccc80
    - path: framework/framework.manifest.json
      digest: 920eb6eec217732152c452d51f01e471940df6f2e2ffe608c377fccc37814090
    - path: src/aief_stage6/digests.py
      digest: 311247d6fdb11ecffa160adad4a884371ee9d6a779dcfc5308ca8e15a358b0a4
    - path: src/aief_stage6/tokenizers.py
      digest: 26a6c5bd424aef5459b0049181158163793e38402733adf2ef3d74536caef95f

deliverables:
    - path: .ai/project/EXECUTION_ARCHITECTURE.md
      digest: 953a09e472e26dd17e8528d39d69b723b80d51d1c0eed8f2f984f4ecca9c9c11
    - path: src/aief_exec/__init__.py
      digest: b8813ebc7fb18c820721517de771f9ed8ca224971824e2e9680f227ef5f77b69
    - path: src/aief_exec/__main__.py
      digest: bc5bb5f1258963641692a3b9af630774caf46b13ce9e88301ad0a86dc5f75b3a
    - path: src/aief_exec/records.py
      digest: 022af653f936efd46189af078c35fe1b43fbdf21cbbc2da15fde5085b7a8e1eb
    - path: src/aief_exec/scope.py
      digest: 32f30988413d53ea3cc1f16ee10a62cf374f7d4badca595f406441892b86825c
    - path: src/aief_exec/graph.py
      digest: 403d85b6e10ee1b4fe07a586fe17e36d3e91104fd5cf53a6aeaeec8b3da4af0e
    - path: src/aief_exec/checks.py
      digest: 66dd66896af71a4d64bedafb3581d80d10e78127f48c059f5c81c75aa76dc1f7
    - path: tests/test_exec_records.py
      digest: f49a7d8440a7aae42e18eeea91ad2ea0b29f0db2885f1e16d88c309954219269
    - path: tests/test_exec_scope.py
      digest: fcd15ea501091ab89c8468c89b5e72d54c3e3102a2d961d03e94518e6be7337f
    - path: tests/test_exec_graph.py
      digest: d2844d65925bd873477a8d0a6c91ccc524cd8d29c0876599ff1d6263334b0564
    - path: tests/test_exec_checks.py
      digest: 1f13168daafe2ee6900c8a99d86b46b25da5753522ee012ed5b5db1b7dd05458

acceptance:
  - id: AC-1
    outcome: FAIL
    evidence: |
      `PYTHONPATH=src python -m aief_exec check` reports 7 of 9 PASS and exits 1.
      X-01 through X-07 PASS. X-08 and X-09 FAIL, and neither fails for a defect
      in the layer: X-08 because the corrected measurement exceeds four tasks'
      declared caps, which are a project-manager decision and were deliberately
      not tuned to fit it; X-09 because five tasks are contracted to publish a
      record they have no lawful write path to, which is the open A4 decision.
      Both are enumerated under `unresolved` with the repair and its owner.

      Unchanged in substance from R-010. No cap was raised and no `write_scope`
      was widened by this pass; the six X-08 rows and the five X-09 rows are the
      same rows, over the same tasks, for the same reasons.

      The criterion itself is also still stale: it reads `X-01 through X-08`,
      written before X-09 existed. It is left as written rather than restated to
      fit the result - see U-5.
  - id: AC-2
    outcome: PASS
    evidence: |
      `python -m aief_exec brief T-002` emits the five entries T-002 declares
      mandatory and nothing else, and its reported total equals the resolved
      mandatory scope, TF-1 4910 / TF-2 6411.
  - id: AC-3
    outcome: PASS
    evidence: |
      `python -m aief_exec scope T-002`: mandatory resolved TF-1 4910 / TF-2
      6411 against the same files unanchored at TF-1 66437 / TF-2 87780. Factor
      13.5x TF-1, 13.7x TF-2, both above the required five.
  - id: AC-4
    outcome: PASS
    evidence: |
      All four clauses hold. `classify T-002 T-004` is PARALLEL and prints the
      five comparisons the verdict rests on - two write/write and write/read
      pairs and both write/observe directions - each with its two operand sets
      and an EMPTY intersection, so the verdict is readable as the absence of
      three named hazards rather than asserted. `classify T-002 T-005` is
      CONFLICT on three reasons: one `[write/write]` and both `[write/observe]`
      directions, nine intersecting paths. `conflict_reasons` is empty for
      T-002 x T-003 and T-002 x T-004 and non-empty for T-002 x T-005.
      T-002 x T-003 remains BLOCKED on the unassigned qa-engineer role, which
      the output labels `blocked/precondition - not a scope hazard`; the
      criterion tests the scope property and the property holds, per the
      FIND-Q9-15 restatement.

      The T-002 x T-004 clause is transaction-sensitive and has now failed twice
      for the same structural reason: mid-pass, with T-004 re-pointed to R-011
      and R-011 not yet written, `classify T-002 T-004` read BLOCKED on
      `consumes R-011 which has not been published`. Publishing R-011 in the
      same act that re-points T-004 is what clears it, exactly as re-pointing
      T-004 to R-010 alongside publishing R-010 cleared it in the previous pass.
  - id: AC-5
    outcome: PASS
    evidence: |
      `git status --porcelain` lists exactly the same seven tracked
      modifications as before this pass - BINDING.md, FROZEN.md, OPEN_ITEMS.md,
      ROSTER.md, STATE.md, ENGINEERING.md, framework/framework.manifest.json -
      none of them written by it. Every path this pass wrote is either inside
      `.ai/project/` or under `tests/test_exec_*.py`, and every one of those was
      already untracked. No path under `.ai/core/`, `framework/` or `spec/` was
      written.
  - id: AC-6
    outcome: NOT MEASURED
    evidence: |
      Reported, not disposed, and the criterion is still not disposable as
      written - see below. `pytest tests/` gives 375 passed, 2 failed. Both
      failures are in the Stage 6 partition - `test_stage6_coverage_and_build.py
      ::test_v24_live_registry` and `test_stage6_pipeline_stub.py::
      test_full_pipeline_with_stub_families`, both on V-24 registry drift in
      `framework/framework.manifest.json` and the unregistered AIEF-AMD-014,
      neither touched by this pass.

      The three exec live-state failures R-010 reported under U-3 are gone -
      discharged by this pass, not suppressed. See N-6 and the conclusion.

      The criterion cannot be disposed as written: its baseline is "the same 4
      pre-existing failures" and the Stage 6 partition shows 2, a count that
      moved under OI-C-09 and T-002 rather than under this pass or the two
      before it. Restating that baseline is T-002's act, not this record's. What
      is asserted here is the narrower fact this pass can establish: it wrote
      nothing under `src/aief_stage6/` or `tests/test_stage6_*.py` and
      introduced no Stage 6 failure.

files_changed:
  - path: tests/test_exec_checks.py
    change: |
      `TestLiveRepositoryOpenFailures` restated. The class docstring now says
      X-08 and X-09 are the open failures and records that X-02 and X-06 were
      closed by this record. `test_x02_open_on_the_consumer_of_a_staled_result`
      and `test_x06_open_on_the_result_that_pins_the_layer_it_describes` assert
      the post-closure state. Both function names are retained deliberately -
      they are the node ids R-010 U-3 names, so they stay addressable - and each
      docstring says so and says what moved. `graph` added to the module imports.
  - path: tests/test_exec_graph.py
    change: |
      `TestLivePlan::test_the_live_dependency_state_is_derived_end_to_end`
      restated onto the post-closure derivation. Also
      `test_independent_backlog_tasks_are_scope_independent`: its prose named
      R-009 as the current blocker and its guarded branch asserted a hard-coded
      `R-009`, both stale and the second latently wrong. The branch now names
      whatever T-004 actually consumes, so it cannot go stale the same way.
  - path: .ai/project/results/R-011.md
    change: This record. Published by the closure pass, superseding R-010.
  - path: .ai/project/results/R-010.md
    change: status CURRENT -> SUPERSEDED, superseded_by R-011. No other field touched.
  - path: .ai/project/tasks/T-004.md
    change: |
      consumes and read_scope.dependency re-pointed R-010 -> R-011 together, and
      the two checkpoint lines naming R-010 corrected with them.
  - path: .ai/project/tasks/T-001.md
    change: R-011 added to `produces`.

validation:
  - check: X-01 through X-07
    outcome: |
      PASS on the live repository, with notices printed. Reproduce with
      `PYTHONPATH=src python -m aief_exec check`, which reports 7 of 9 and
      exits 1. X-02 and X-06 were the two this pass moved from FAIL to PASS;
      the other five were already passing and are unchanged.
  - check: X-08, X-09
    outcome: |
      FAIL - deliberately, and identically to R-010. X-08 on T-002 (TF-1, TF-2),
      T-003 (TF-1), T-004 (TF-1, TF-2) and T-006 (TF-1); X-09 on T-002 through
      T-006. No cap was changed and no `write_scope` was widened to clear
      either. See U-1, U-2.
  - check: supersession seal over R-010
    outcome: |
      Verified, and verified as present rather than by the alarm's silence.
      `graph.supersession_seal` resolves R-010's sealer to R-011 and its sealed
      digest to 45516365790fd220ccd16be0b4a7dd25294cb3cb19f0301622a2d1d8bfcd1f12,
      equal to the recomputed DC-1 of the file as it stands after its two
      supersession fields were written - which is when the digest was taken, not
      before. `seal_epoch` is S-2026-08-09-06 and this record's session is
      S-2026-08-09-08, so an omitted seal here would have been an X-06 failure,
      not a notice. Perturbation on a scratch copy of the repository: appending
      one comment line to R-010 moves its DC-1 and X-06 turns FAIL with
      `R-010: REWRITTEN AFTER SUPERSESSION: DC-1 ... does not match the digest
      ... that R-011 sealed at supersession`. The control is silent because it
      matched, and it is demonstrably not disabled. The scratch copy was
      discarded.
  - check: the seal chain behind R-010
    outcome: |
      Unbroken and re-verified, not assumed. R-009's seal over R-008 and R-010's
      seal over R-009 both still equal the recomputed DC-1 of their sealed
      files, so no ancestor was edited after supersession. R-001 and R-007
      remain unsealed and unsealable - see U-4.
  - check: pytest tests/test_exec_*.py
    outcome: |
      228 passed, 0 failed. Reproduce with
      `PYTHONPATH=src python -m pytest tests/test_exec_*.py -q`. The three tests
      R-010 U-3 recorded as red are green, restated rather than weakened: none
      was skipped, xfailed, deleted or reduced to a bare status comparison, and
      each still fails if the state it records regresses. See N-6.
  - check: pytest tests/
    outcome: |
      375 passed, 2 failed - the two pre-existing Stage 6 failures on V-24
      registry drift, and nothing else. See AC-6.
  - check: AC-4 hazard comparisons
    outcome: |
      `classify T-002 T-004` PARALLEL, printing all five comparisons with EMPTY
      intersections; `classify T-002 T-005` CONFLICT on `[write/write]` and both
      `[write/observe]` directions over nine paths. Unchanged by this pass in
      substance; re-run because T-004's observation surface is derived partly
      from the deliverable pins of the result it consumes, and this pass changed
      which result that is.

consumed:  [R-010]

notices:
  - id: N-1
    note: |
      IMPLEMENTED PENDING INDEPENDENT VERIFICATION. This record closes nothing
      and certifies nothing. Every item below is delivered and demonstrated by a
      named command; none is disposed. T-001's verifier is `qa-engineer` and
      LAW-05 requires the closing pass to be taken by a role that authored none
      of this.
  - id: N-2
    note: |
      What is deliberately not pinned, unchanged in reasoning from R-010 N-2.
      VER-009 is T-004's deliverable and pinning it made R-007 self-staling
      (FIND-Q9-13). Task records and EXEC.md are mutable registers and pinning
      them froze the backlog (FIND-Q9-14). STATE.md, BINDING.md and ROSTER.md
      stay dropped for the same reason R-009 and R-010 dropped them. This
      narrows what X-06 can detect and is recorded as a decision, not left to be
      discovered.
  - id: N-3
    note: |
      src/aief_stage6/digests.py and tokenizers.py are pinned as inputs although
      they lie in another task's write scope. DC-1 and the two tokenizer
      families are load-bearing for every digest and every cost figure above. If
      this record goes STALE on either, that is the pin working.
  - id: N-4
    note: |
      The seal over R-010 is taken over R-010 **after** its two supersession
      fields were written. The digest was recomputed at that point and not
      carried over from any earlier reading of the file, because a seal taken
      before the closing edit seals a version that no longer exists.
  - id: N-5
    note: |
      Identifier collision, carried forward from R-010 N-5, reported rather than
      resolved. `A4` in X-09's notice and in EXECUTION_ARCHITECTURE.md §14 item 9
      is the open **architecture** decision on whether `produces` carries a write
      grant. `A4` in STATE.md and AIEF-AMD-013 AMD-41 is a role identifier in the
      CMP-BLOCK-006 determination. Two unrelated referents share a token; the
      layer already emits its own, so it is left as it stands and flagged for
      whoever disposes either.
  - id: N-6
    note: |
      R-010 U-3 is discharged by this record, and this is what discharging it
      consisted of, so that no reader has to infer it from a green suite.

      The three tests are

        tests/test_exec_checks.py::TestLiveRepositoryOpenFailures::
          test_x02_open_on_the_consumer_of_a_staled_result
        tests/test_exec_checks.py::TestLiveRepositoryOpenFailures::
          test_x06_open_on_the_result_that_pins_the_layer_it_describes
        tests/test_exec_graph.py::TestLivePlan::
          test_the_live_dependency_state_is_derived_end_to_end

      Each asserted the pre-closure live state unconditionally - X-02 FAIL, X-06
      FAIL, R-009 STALE, T-004 BLOCKED - on purpose, so that it would go red the
      moment the record was republished and force the next state to be written
      down rather than inferred. It did. This is the next state written down.

      The first two keep their function names although the names now describe
      history rather than content: they are the node ids R-010 U-3 names as red,
      and renaming them would break the only handle a verifier has on the item
      being discharged. Each docstring opens by saying the name is retained
      deliberately and what the test now holds.

      None was weakened. A bare `assert status == "PASS"` would have been
      worthless - X-06's entire function is to notice drift, so a test that
      merely records it passing tests nothing. What each asserts instead is the
      derivation that produces the pass: that the current record still pins the
      layer it describes and matches it, that every superseded ancestor still
      matches the digest its successor sealed, and that T-004's derived state
      falls out of both. Move a file under `src/aief_exec/` or
      `tests/test_exec_*.py` without republishing and all three go red again;
      edit R-009 or R-010 after supersession and two of them do.
  - id: N-7
    note: |
      Why this pass terminates and the two before it did not. A record that pins
      the layer is staled by any change to the layer, and a test that asserts the
      live state is invalidated by any change to that state. R-010 U-3 stated the
      deadlock correctly: restating the tests stales the record, and republishing
      the record without restating them leaves them red.

      What breaks it is ordering inside a single transaction, not a new
      mechanism. The tests are restated first and are the last thing to change;
      the digests are then taken over the restated files; the record published
      from those digests is current the moment it lands. Nothing after step one
      touches a pinned path, so there is no second staling to chase. The two
      halves had to be in one write scope for this to be possible at all, and
      that - not any change to the layer - is what this pass had that its
      predecessors did not.

unresolved:
  - id: U-1
    statement: |
      X-09 fails on T-002, T-003, T-004, T-005 and T-006: each declares
      `produces: R-nnn` and none has a write_scope pattern reaching
      `.ai/project/results/R-nnn.md`, so no one of them can lawfully publish the
      record it is contracted to publish. This is the reproduced publication
      defect and it is left failing deliberately. Neither repair may be taken
      from here - widening write_scope presumes the implicit grant, dropping
      `produces` discards the contract - because which is correct is open
      architecture decision A4, recorded at EXECUTION_ARCHITECTURE.md §14 item
      9. `effective_write_scope` computes and displays the grant that would
      close it and applies nothing.

      Carried forward from R-010 U-1 unchanged. This pass added R-011 to
      T-001.produces; T-001's write_scope already covers
      `.ai/project/results/**`, so T-001 stays off the X-09 list and the count
      of offenders is unmoved at five.
    owner: project-manager
    blocks: X-09, AC-1
  - id: U-2
    statement: |
      X-08 fails on T-002 (TF-1 and TF-2), T-003 (TF-1), T-004 (TF-1 and TF-2)
      and T-006 (TF-1). These are the honest output of a corrected measurement,
      not a defect in it: `acquisition` charges the task record, the resolved
      mandatory and optional read scope and each consumed result whole. The caps
      live in task records and were deliberately not tuned to fit the
      measurement. T-001's own breach is reported and not gated because T-001 is
      COMPLETE and will not be dispatched again.

      Six numbers from one run, not six constants - T-005 crossed and recrossed
      its cap during R-009's pass without being touched, when a concurrent
      session created the deliverable it declares. Read the command, not the
      numbers. T-004's own figure moves with this pass for the same reason: its
      `dependency` charge is the consumed result read whole, and the consumed
      result is now R-011 rather than R-010.

      Carried forward from R-010 U-2. No cap was changed by this pass.
    owner: project-manager
    blocks: X-08, AC-1
  - id: U-4
    statement: |
      R-001 and R-007 are unsealed and cannot be retro-sealed: their successors
      were published before the field existed. `graph.seal_epoch` makes that a
      notice rather than a failure by taking the epoch from the records
      themselves - the earliest session in which a seal was actually published -
      so the rule is not applied backwards. R-007's own rewrite after
      supersession remains undetectable by the mechanism and is known only from
      its own confession. Carried forward from R-010 U-4, unchanged.
    owner: software.software-engineer
  - id: U-5
    statement: |
      Register drift in the task records, carried forward from R-010 U-5 and
      still not this pass's to restate.

      T-001's deliverable list still names `.ai/project/tasks/T-001.md through
      T-006.md` and `tests/test_exec_records.py, test_exec_scope.py,
      test_exec_graph.py, test_exec_checks.py` as single prose entries. Neither
      resolves to a path, so X-08 charges them nothing and says so on every run.
      T-001's AC-1 also still reads `X-01 through X-08`, written before X-09
      existed, and its AC-6 baseline still reads "the same 4 pre-existing
      failures" against a Stage 6 partition that now shows 2 - which is why AC-6
      above is reported and not disposed. T-004 carries the same class of drift
      in the halves this pass was not scoped to touch: `inputs` names R-001,
      AC-3 reads "R-008 is confirmed CURRENT at the time of audit", AC-4 names
      the R-007 finding closures, and the escalation condition names R-001. Only
      `consumes`, the coupled `read_scope.dependency` and the two checkpoint
      lines were re-pointed, because those are what the derivation reads; the
      rest is the verifier's contract and restating it is a project-manager act,
      not a side effect of a closure pass.
    owner: project-manager
  - id: U-7
    statement: |
      A verifier of this record will read an account of `src/aief_exec/**`
      written by a pass that did not write it. This pass wrote only the three
      restated tests, one stale reference in a fourth, and the four records; the
      layer itself was delivered by the repair pass under the same task and is
      byte-identical to what R-010 pinned - of the eleven deliverable digests,
      only two moved, both under `tests/`. The digests are recomputed and
      pinned here, so what is published is the bytes; the narrative carried
      forward from R-010 is read off the code and its tests and should be
      re-derived, not trusted. Carried forward from R-010 U-7.
    owner: project-manager
  - id: U-6
    statement: |
      F-007 remains open with chief-systems-engineer - an authority citation is
      checked for form and never for referent. Unchanged by this pass.
    owner: chief-systems-engineer

conclusion: |
  The execution layer as delivered by the repair pass under T-001, republished
  with the three live-state tests restated onto the state that publication
  produced. IMPLEMENTED PENDING INDEPENDENT VERIFICATION - nothing here is closed
  and nothing here is self-certified.

  WHAT THIS PASS DID

  1. Discharged R-010 U-3. Three tests asserted the pre-closure live state
     unconditionally, each carrying an author comment saying it was written that
     way so that republishing the record would turn it red and force the next
     state to be written down. Two previous passes could not clear them, because
     restating them stales the record that pins them and republishing without
     restating them leaves them red. Both acts land here, in one transaction, in
     the only order that terminates: tests first, digests taken after, record
     published from those digests. See N-6 for what was and was not changed in
     each test, and N-7 for why the ordering closes.

  2. Moved X-02 and X-06 from FAIL to PASS - not by editing a check or an
     assertion, but by removing what they were correctly reporting. X-06 failed
     because a CURRENT record pinned digests of a layer that had moved; the
     record is superseded and the successor pins the layer as it stands. X-02
     failed because T-004 consumed that staled record and so derived BLOCKED
     against a READY index entry; T-004 consumes the current record and derives
     READY. `check` is 7 of 9 rather than 5 of 9. Nothing else moved.

  3. Nothing else. The layer is untouched: `src/aief_exec/**` is byte-identical
     to what R-010 pinned, as its six unchanged deliverable digests show. No cap
     was raised, no `write_scope` was widened, no check was added or altered, and
     EXEC.md was not written. X-08 and X-09 fail exactly as they did.

  WHAT A DOWNSTREAM AGENT NEEDS TO KNOW

  1. Read `.ai/project/EXEC.md` after boot - a bounded index, one task id per
     line, naming the state of every task.
  2. Read the single `tasks/T-nnn.md` you are assigned. Read no other.
  3. Run `python -m aief_exec brief T-nnn` for the resolved minimum context, and
     `python -m aief_exec scope T-nnn` to see the three budget quantities, which
     of your charge moves while you work, and what you are observing.
  4. Classification is computed, never declared. So is supersession: a record is
     superseded when a successor says so, whatever it says of itself.
  5. `check` reports 7 of 9 and exits 1. X-08 and X-09 are open, enumerated
     under `unresolved` with the record and the role that owns each repair.
     Neither is cleared by widening a scope or raising a cap, and neither was.
     Do not read a green `check` as this layer's acceptance criterion until U-1
     and U-2 are disposed.
  6. `pytest tests/test_exec_*.py` is green, and a green exec suite is now a
     usable signal again - it was not while U-3 stood. It is still not an
     acceptance criterion: AC-1 fails, and `pytest tests/` carries two Stage 6
     failures that belong to T-002.

  MEASURED EFFECT

  T-002's mandatory scope resolves to TF-1 4,910 / TF-2 6,411 against TF-1 66,437
  / TF-2 87,780 for the same files unanchored - 13.5x and 13.7x. Reproduce with
  `python -m aief_exec measure T-002`, which prints the envelope definition
  alongside the numbers.

  No sweep figure is stated here. FIND-Q9-31 established that the envelope
  contains the result and verification registers, so it moves whenever an audit
  or a result is filed - including by the filing of this record. A number that
  changes when it is written down is a measurement, not a constant. Run the
  command.
```

## Notes

**Immutability.** Seven inputs and eleven deliverables are DC-1-pinned, and `R-010` is
sealed by a nineteenth digest taken over the whole record after its supersession fields were
written. `X-06` recomputes the eighteen pins on every run and compares the seal; any drift
makes this result STALE, and any subsequent edit to `R-010` raises
`REWRITTEN AFTER SUPERSESSION`.

**Status.** `IMPLEMENTED PENDING INDEPENDENT VERIFICATION`. This record does not close a
finding and does not certify itself. `T-001`'s verifier is `qa-engineer`; `LAW-05` requires
the pass that disposes the open items above to be taken by a role that authored none of it.
