# R-009 — Execution Layer, After the Fourth Verification Pass

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Result record per [`../EXECUTION_ARCHITECTURE.md`](../EXECUTION_ARCHITECTURE.md) §6.
> **Supersedes [`R-008`](R-008.md)**, whose eleven pinned deliverable digests this
> correction pass moved. `R-008` was certified CURRENT by `VER-009` pass 4 with 19 of 19
> digests recomputed and is retained unedited; its DC-1 at supersession is sealed below.
> Consume the `conclusion`. Do not read the producing task's inputs to re-derive it.

```yaml
result_id:   R-009
produced_by:
  task:      T-001
  role:      software.software-engineer
  session:   S-2026-08-09-06
status:      SUPERSEDED
superseded_by: R-010
supersedes:  R-008
supersedes_seal:
  path:   .ai/project/results/R-008.md
  digest: e081eb7b9370afe98cb57d8f98b15fdd5ea088c1ecc417477755205959d15a6b

inputs:
    - path: .ai/BOOT.md
      digest: 5a97d6ad763f8720b3e9e5c9cf9be6ad62f6ba953b5b70eb78a3624b580b82db
    - path: .ai/core/CONTEXT_TIERS.md
      digest: dad8a47ad994cce45820b9b89f016872f259b7b44b10e8a87ed42f32e31ade4f
    - path: .ai/core/templates/TPL-task-package.md
      digest: a51c5d27fa44f85b3d6a291b28099161561e8cc367e2ce64dc8ae0553501e9b2
    - path: .ai/core/schemas/SCH-task.schema.json
      digest: e874fdd70b56f55b6205e26c7ef537e090e46b5f05ab2964f8b1ee4ad6cccc80
    - path: framework/framework.manifest.json
      digest: 920eb6eec217732152c452d51f01e471940df6f2e2ffe608c377fccc37814090
    - path: src/aief_stage6/digests.py
      digest: 311247d6fdb11ecffa160adad4a884371ee9d6a779dcfc5308ca8e15a358b0a4
    - path: src/aief_stage6/tokenizers.py
      digest: 26a6c5bd424aef5459b0049181158163793e38402733adf2ef3d74536caef95f

deliverables:
    - path: .ai/project/EXECUTION_ARCHITECTURE.md
      digest: 128c68501b7600e1343270862eba20bab65f33b481882f5d69b19e55fbaf8cc3
    - path: src/aief_exec/__init__.py
      digest: b8813ebc7fb18c820721517de771f9ed8ca224971824e2e9680f227ef5f77b69
    - path: src/aief_exec/__main__.py
      digest: 6bc13b29691d4176485918b018350a773683c974f54c662247548166cb5bcc4e
    - path: src/aief_exec/records.py
      digest: 4eeb1050fe5a867351bc990bab99fc78e392056a2f0018f928b6595137470a5e
    - path: src/aief_exec/scope.py
      digest: fe6620126fd6154f75d7e5b58e4c2370dd5b3ce6dfda3859fb030e49a9dde844
    - path: src/aief_exec/graph.py
      digest: e4ca2b1304d456f31926bd95d248b500af762de33c058f72c34ff5053415ec22
    - path: src/aief_exec/checks.py
      digest: a34c24edb8dad9e3d809c8bfad46cccf1f332af717c6900dd8b999ddb18035eb
    - path: tests/test_exec_records.py
      digest: ab3a474fcd9fe0bf757540af7a3c856ed49eff40fb3aa0b8ebd7ab50000b27f3
    - path: tests/test_exec_scope.py
      digest: 5d72da297cb9eb606eed21efcd6c9c337feff322c02d28578e15276be5191f7b
    - path: tests/test_exec_graph.py
      digest: 0aa50152f4d41f19b6a806e675a6cdcff15c9f6627e6827d682bca203c86e037
    - path: tests/test_exec_checks.py
      digest: 6530a6b8b89734ffa10cad60547e5c463101f4c3e07c44f34447f0f2e6b085d7

acceptance:
  - id: AC-1
    outcome: FAIL
    evidence: |
      `PYTHONPATH=src python -m aief_exec check` reports 5 of 8 PASS and exits 1.
      X-01, X-03, X-04, X-05, X-07 PASS. X-02, X-06 and X-08 FAIL, and all three
      fail for writes this pass was not granted, not for a defect in the layer:
      X-06 because R-008 still declares `status: CURRENT` under a successor;
      X-02 because T-004 still declares `consumes: [R-008]` and therefore derives
      BLOCKED while the index files it Ready; X-08 because the honest charge
      exceeds five of the six declared caps. Each is enumerated under
      `unresolved` with the exact repair and the record that owns it.
  - id: AC-2
    outcome: PASS
    evidence: |
      `python -m aief_exec brief T-002` emits the five entries T-002 declares
      mandatory and nothing else, and its reported total equals the resolved
      mandatory scope, TF-1 4910 / TF-2 6411.
  - id: AC-3
    outcome: PASS
    evidence: |
      `python -m aief_exec scope T-002`: resolved TF-1 4910 / TF-2 6411 against
      unanchored TF-1 66437 / TF-2 87780. Factor 13.5x TF-1, 13.7x TF-2, both
      above the required five.
  - id: AC-4
    outcome: PARTIAL
    evidence: |
      Scope independence holds and is what the criterion tests after FIND-Q9-15:
      `conflict_reasons` is empty for T-002 x T-003 and for T-002 x T-004, and
      non-empty for T-002 x T-005; `classify T-002 T-005` is CONFLICT. The
      criterion's remaining clause, `classify T-002 T-004 is PARALLEL`, does not
      hold: publishing this record superseded R-008, T-004 consumes R-008, and a
      consumer of a superseded result derives BLOCKED. Re-pointing T-004 is a
      task-record write withheld from this pass.
  - id: AC-5
    outcome: PASS
    evidence: |
      `git status --porcelain` lists the same seven tracked modifications as
      before this pass. No path under .ai/core/, framework/ or spec/ was written.
  - id: AC-6
    outcome: NOT MEASURED
    evidence: |
      tests/test_stage6_*.py is owned by a concurrent session and this pass was
      instructed not to run or repair it. The Stage 6 partition is deliberately
      unmeasured here rather than asserted from R-008's figure, which FIND-Q9-30
      showed was wrong when written.

files_changed:
  - path: src/aief_exec/scope.py
    change: |
      patterns_overlap replaced by an exact decision procedure - each glob
      compiles to a Thompson NFA over three character classes and the two are
      intersected by product reachability. Excerpt now carries its resolved
      text; resolve_scope takes `whole` so a caller that prints no unanchored
      comparison does not read every file twice. charged_context and
      deliverable_paths added.
  - path: src/aief_exec/graph.py
    change: |
      The supersession guard replaced by a seal comparison; successor_of,
      derived_status and supersession_seal added; a consumer blocked on a
      superseded result is now told which record supersedes it.
  - path: src/aief_exec/checks.py
    change: |
      X-06 fails on a record rewritten after supersession, notices an unsealed
      one, and fails on a record still declaring CURRENT under a successor. X-08
      compares the charged context, not the resolved read scope, and prints the
      breakdown with every figure.
  - path: src/aief_exec/records.py
    change: ResultRecord.supersedes and ResultRecord.supersedes_seal added.
  - path: src/aief_exec/__main__.py
    change: |
      cmd_measure no longer formats a possibly-None cost, and states that the
      sweep is a run-time measurement with the size of its moving component.
      cmd_scope prints the charged breakdown. cmd_brief prints the excerpt it
      already has instead of re-resolving it.
  - path: .ai/project/EXECUTION_ARCHITECTURE.md
    change: §5 line 163 corrected to agree with §14 item 5 on grant forgery.
  - path: tests/test_exec_scope.py
    change: |
      TestChargedContext added; the six FIND-Q9-29 pairs pinned with a concrete
      witness each; six disjoint pairs pinned against over-reporting; a
      brute-force cross-check of the decision procedure; a single-open assertion.
  - path: tests/test_exec_graph.py
    change: |
      TestSupersessionSeal added - eleven assertions including a monotonicity
      property and the seal's DC-1 blind spot. Two live-plan tests restated onto
      the post-supersession state.
  - path: tests/test_exec_checks.py
    change: |
      TestLiveRepositoryOpenFailures added, recording each open failure with the
      repair that clears it. Six regression tests added for Q9-28, Q9-31, Q9-34
      and Q9-35.

validation:
  - check: X-01, X-03, X-04, X-05, X-07
    outcome: PASS on the live repository, with notices printed
  - check: X-02, X-06, X-08
    outcome: FAIL - each traced to a named write outside this pass's scope
  - check: pytest tests/test_exec_*.py
    outcome: |
      169 passed, 0 failed. Reproduce with
      `PYTHONPATH=src python -m pytest tests/test_exec_*.py -q`. The figure is
      stated with the command that produces it and with the partition it covers
      (the four exec files only) because FIND-Q9-19, FIND-Q9-25 and FIND-Q9-30
      were three consecutive records carrying a suite figure that did not
      reproduce. The Stage 6 partition is not measured here - see AC-6.
  - check: FIND-Q9-29 decision procedure
    outcome: |
      All six pairs pass 4 named as false negatives now report overlap, each
      with a concrete witness path asserted against glob_to_regex; six disjoint
      pairs still report disjoint; and an exhaustive search over every path of
      length <= 4 in {a,b,/} finds no pattern pair the procedure under-reports.
  - check: FIND-Q9-33 instrumentation
    outcome: |
      builtins.open, Path.read_text and Path.read_bytes wrapped around
      `brief T-002`: 52 open events over 37 distinct repository files, of which
      6 are the tool's own imported module source, while the emitted brief is 5
      entries. Before this pass the same instrumentation counted 62 events, 15
      of them three-per-entry re-reads of the five mandatory paths; those are
      now five. Both halves of FIND-Q9-33 were true: the tool did over-read, and
      R-008's figure of 21 was wrong in the direction that understates the
      disclosure. The removable part is removed; the rest is structural - the
      plan derivation reads every task record and recomputes every result's
      pinned digests, which is what lets `brief` say BLOCKED at all. What is
      bounded is the brief, not the tool, exactly as AC-2 was restated to say.

consumed:  []

notices:
  - id: N-1
    note: |
      FIND-Q9-28 through FIND-Q9-35 are CORRECTED PENDING RE-VERIFICATION, not
      closed. Nothing in this record is a closure. VER-009 pass 4's judgement
      was that four of six closures taken after pass 3 were closed in form and
      defective in substance, each detectable by running the test the finding
      named; every correction here names the command that demonstrates it and
      none is asserted on inspection.
  - id: N-2
    note: |
      What is deliberately not pinned, and why. VER-009 is T-004's deliverable
      and pinning it made R-007 self-staling (FIND-Q9-13). The task records and
      EXEC.md are mutable registers and pinning them froze the backlog
      (FIND-Q9-14). This record extends that reasoning one step: STATE.md,
      BINDING.md and ROSTER.md were pinned by R-008 and are dropped here,
      because a concurrent session rewrites them at close and this record's
      conclusion does not depend on their content. Dropping them narrows what
      X-06 can detect and is recorded as a decision, not left to be discovered.
  - id: N-3
    note: |
      Two pinned inputs, src/aief_stage6/digests.py and tokenizers.py, are
      inside a concurrent session's write scope. They are pinned because DC-1
      and the two tokenizer families are load-bearing for the seal and for every
      cost figure above. If this record goes STALE on either, that is the pin
      working.
  - id: N-4
    note: |
      The seal over R-008 is taken over R-008 as it stands now, which still
      declares `status: CURRENT`. Closing R-008 - the two status fields, which
      is what supersession consists of - will change its DC-1 and fire the
      rewrite alarm. That edit and a re-seal here are one transaction and must
      be taken together. This is stated in advance rather than discovered.

unresolved:
  - id: U-1
    statement: |
      R-008 still declares `status: CURRENT` with no `superseded_by`. X-06 fails
      on the declared/derived disagreement. Repair: R-008 status -> SUPERSEDED,
      superseded_by: R-009, and re-seal above in the same act.
    owner: project-manager
    blocks: X-06
  - id: U-2
    statement: |
      T-004 declares `consumes: [R-008]` and must be re-pointed to R-009 after
      its conclusion is read; the index files T-004 Ready while the derivation
      blocks it. Repair: T-004.md consumes, and the EXEC.md section.
    owner: project-manager
    blocks: X-02, AC-4
  - id: U-3
    statement: |
      X-08 now charges the record, the resolved read scope, consumed results
      whole and every existing deliverable. All six caps are exceeded. Measured
      by `PYTHONPATH=src python -m aief_exec check` at the close of this pass:
      T-001 34956/6000, T-002 12880/12000, T-003 8193 TF-1/8000, T-004
      31328/8000, T-005 7161/1500, T-006 12154 TF-1/12000. T-002, T-003 and
      T-006 exceed by less than the cost of their own task record; T-001 and
      T-004 exceed by multiples, both on the deliverable term. The caps live in
      task records and were deliberately not tuned to fit the measurement.

      These are six numbers from one run, not six constants. T-005 was within
      its cap earlier in this same pass and crossed it when a concurrent session
      created `tests/test_stage6_crash_trials.py`, the deliverable T-005
      declares - the charge moved by 5,750 TF-1 without T-005 being touched.
      Read the command, not the numbers.
    owner: project-manager
    blocks: X-08, AC-1
  - id: U-4
    statement: |
      T-001 `produces` lists R-001, R-007 and R-008 and does not list R-009. Its
      deliverable list names `tests/test_exec_records.py, test_exec_scope.py,
      test_exec_graph.py, test_exec_checks.py` and `.ai/project/tasks/T-001.md
      through T-006.md` as single prose entries, which resolve to no path, so
      X-08 charges them nothing and says so on every run.
    owner: project-manager
  - id: U-5
    statement: |
      R-001 and R-007 are unsealed and cannot be retro-sealed: their successors
      were published before the field existed, and R-007 records in its own
      header that it was rewritten after supersession. X-06 reports both as
      unsealed rather than guessing. R-007's rewrite is not detectable by the
      mechanism, only by its own confession.
    owner: software.software-engineer
  - id: U-6
    statement: |
      F-007 remains open with chief-systems-engineer - an authority citation is
      checked for form and never for referent. Unchanged by this pass.
    owner: chief-systems-engineer

conclusion: |
  Eight findings from VER-009 pass 4 are corrected. None is closed: closure
  requires an independent pass, and the pattern that pass found - four of six
  closures asserted rather than demonstrated - is the reason this record states a
  command for every correction.

  WHAT CHANGED, AND WHAT DEMONSTRATES IT

  1. The supersession guard was inverted. It fired on R-001, whose pins are six
     inputs T-001 was forbidden to touch, and stayed silent on R-007, the one
     record here that was rewritten - because what it measured was whether the
     tree had moved, not whether the record had. A superseded record is now
     compared against a digest of itself that its successor sealed at
     supersession. The control is monotonic by construction: the evidence lives
     in a different file from the one it protects, so tampering can only add the
     alarm. Where no seal exists the answer is "undecidable", said out loud,
     which is what R-001 and R-007 now get. Demonstrated by
     TestSupersessionSeal, including the auditor's own perturbation, which
     silenced the old guard and raises this one.

  2. Glob overlap is no longer a heuristic. Both previous forms were heuristics
     and both were wrong - one over-reported, one under-reported five of six
     named pairs in the unsafe direction. Each glob now compiles to an automaton
     and the two are intersected by product reachability, which decides the
     question rather than approximating it. Demonstrated by the six pairs with a
     concrete witness each, six disjoint pairs, and an exhaustive cross-check.

  3. The context budget was fiction for any task that revises something that
     already exists. X-08 charged what a task reads to start and omitted what it
     must rewrite, certifying T-004 at TF-1 2,519 against a cap of 8,000 while
     the real cost was about 8.7x that, dominated by T-004's own deliverable.
     X-08 now charges the record, the read scope, consumed results whole and
     every deliverable that exists, prints the breakdown with every figure, and
     names the paths that make the figure self-referential. The measurement was
     corrected and the caps were not: five of six now fail, and that is the
     finding rather than a defect in it.

  4. Three smaller defects: `measure` no longer dies formatting an UNMEASURED
     cost and no longer presents its sweep as a constant; `brief` opens each
     mandatory entry once instead of three times; and §5 of the architecture no
     longer carries the grant-forgery framing that §14 item 5 corrects.

  WHAT A DOWNSTREAM AGENT NEEDS TO KNOW

  1. Read `.ai/project/EXEC.md` after boot - a bounded index, one task id per
     line, naming the state of every task.
  2. Read the single `tasks/T-nnn.md` you are assigned. Read no other.
  3. Run `python -m aief_exec brief T-nnn` for the resolved minimum context.
     Run `python -m aief_exec scope T-nnn` to see what your budget is charged
     for and which of it moves while you work.
  4. Classification is computed, never declared - and supersession now is too:
     a record is superseded when a successor says so, whatever it says of
     itself.
  5. `check` reports 5 of 8. The three failures are enumerated under
     `unresolved` with the record that owns each repair. Do not read a green
     `check` as the layer's acceptance criterion until U-1, U-2 and U-3 are
     disposed.

  MEASURED EFFECT

  T-002 mandatory scope resolves to TF-1 4,910 / TF-2 6,411 against TF-1 66,437
  / TF-2 87,780 for the same files unanchored - 13.5x and 13.7x. Reproduce with
  `python -m aief_exec measure T-002`, which prints the envelope definition
  alongside the numbers.

  No sweep figure is stated here. FIND-Q9-31 established that the envelope
  contains the result and verification registers, so the sweep moves whenever an
  audit is filed - it moved 9,396 TF-1 by the filing of the audit that measured
  it, and `measure` now prints how much of the envelope is that moving
  component. A number that changes when it is written down is a measurement, not
  a constant, and citing it as one is the defect. Run the command.
```

## Notes

**Immutability.** Seven inputs and eleven deliverables are DC-1-pinned, and `R-008` is
sealed by a nineteenth digest taken over the whole record. `X-06` recomputes the eighteen
pins on every run and compares the seal; any drift makes this result STALE, and any edit to
`R-008` raises `REWRITTEN AFTER SUPERSESSION`.

**Status.** `CORRECTED PENDING RE-VERIFICATION`. This record does not close a finding and
does not certify itself. `T-001`'s verifier is `qa-engineer`; `LAW-05` requires the pass
that closes `FIND-Q9-28` … `FIND-Q9-35` to be taken by a role that authored none of it.
