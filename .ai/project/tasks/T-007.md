# T-007 — LC-M04 Specification Coherence Package

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Template `core/templates/TPL-task-package.md`; schema `core/schemas/SCH-task.schema.json`.

```yaml
task_id:    T-007
role:       chief-systems-engineer
objective:  Disposition every ECR blocking LC-M04-EXIT, apply each to the frozen specification, re-register, and put the gate criteria and the invariants they rest on under standing checks.
status:     COMPLETE

inputs:
  - .ai/project/GATES.md
  - .ai/project/ecr/
  - .ai/project/OPEN_ITEMS_REGISTER.md
  - spec/00, spec/01, spec/02, spec/03, spec/06, spec/07, spec/README.md

deliverable:
  - Dispositions on ECR-D-001, -002, -003, -004, -007, -008, -009, -010, -011
  - ECR records for ECR-D-003, -004, -006, -010, -011 (none existed)
  - approvals APR-020 through APR-026
  - src/aief_approval/, src/aief_gate/, src/aief_clearance/
  - tests/test_approval_chain.py, tests/test_clearance.py

acceptance_criteria:
  - id: AC-1
    criterion: Every ECR-D against spec/** is dispositioned, approved, applied and re-registered
    test: python -m aief_gate reports C1..C5 and C7 PASS
  - id: AC-2
    criterion: Every ECR record conforms to SCH-ecr
    test: All eight required fields are present in every record under ecr/
  - id: AC-3
    criterion: The approval-supersession relation is computed, and an unapproved edit collapses the chain
    test: tests/test_approval_chain.py, in particular test_future_edit_without_approval_collapses_the_whole_chain
  - id: AC-4
    criterion: The spec/00 section 3.2 clearance claim is computed rather than asserted
    test: tests/test_clearance.py::test_ecr_d_010_defect_is_detected restores the old clocking and requires failure
  - id: AC-5
    criterion: No unrelated byte of any frozen artifact changed
    test: Every hunk in git diff -- spec/ maps to a disposition enumerated in APR-020..APR-026

forbidden_actions:
  - Executing Compiler Stage 6 or emitting core/MANIFEST.lock
  - Editing any file under .ai/core/
  - Writing a ledger entry, creating a tag, or pushing to any remote
  - Deciding a Design Authority question without a recorded human ruling
  - Self-certifying - LAW-05 reserves C6 to a distinct verifying identity

escalation:
  - condition: A disposition requires Design Authority authority
    to: human-owner, by interactive decision pause
  - condition: A newly discovered defect blocks LC-M04
    to: human-owner, raised as an ECR first

depends_on:  []
consumes:    []
produces:    [R-015]
blocked_by:  []

read_scope:
  mandatory:
    - path: .ai/project/GATES.md
    - path: .ai/project/OPEN_ITEMS.md
  optional:
    - path: .ai/project/OPEN_ITEMS_REGISTER.md
  dependency: []
  forbidden:
    - .ai/core/**

write_scope:
  - spec/**
  - .ai/project/ecr/**
  - .ai/project/approvals/**
  - .ai/project/FROZEN.md
  - .ai/project/STATE.md
  - .ai/project/GATES.md
  - .ai/project/OPEN_ITEMS.md
  - .ai/project/OPEN_ITEMS_REGISTER.md
  - .ai/project/EXEC.md
  - .ai/project/tasks/T-007.md
  - .ai/project/tasks/T-008.md
  - .ai/project/results/R-015.md
  - src/aief_approval/**
  - src/aief_gate/**
  - src/aief_clearance/**
  - tests/test_approval_chain.py
  - tests/test_clearance.py
  - implementation/**
  - ENGINEERING.md

write_authority:
  paths:
    - spec/**
    - .ai/project/approvals/**
    - .ai/project/FROZEN.md
  id: APR-020
  recorded_at: .ai/project/approvals/APR-020_Cooling_Plate_coherence_package.md
  citation: LAW-01 permits a frozen artifact to change under an approved ECR and a recorded human approval. The human owner ruled nine dispositions in session S-2026-08-10-01, bound to content by APR-020 through APR-026. APR-020 is the first of that set. The reach into approvals and FROZEN.md is the registration those same rulings require.

qa:
  verifier_role: qa-engineer
  report:        .ai/project/verification/VER-015_Independent_Verification_ECR-D-002_003_004_and_the_Coherence_Package.md

context_budget:
  tf1: 60000
  tf2: 75000

checkpoint:
  phase: complete
  completed:
    - Nine dispositions ruled by the human owner across three decision pauses
    - Applied to seven spec artifacts, re-registered, DC-2 aggregate recomputed
    - Three standing checks built with adversarial tests
  pending: []
  next_action: None. C6 is reserved to T-008 under LAW-05.
  decision: null
```

## Notes

**Three root causes were repaired at their owners, not at their symptoms.** Each was a
*declared property that nothing computed*, which is the shape `OI-V-02` already recorded:

| Declared | Checked by nothing | Now |
|---|---|---|
| `LAW-10` approval binding across sequential ECRs on one file | — | `aief_approval`, and `GATES.md` carries the ruled relation |
| `GATES.md` C1–C7, *"binary and evidence-based"* | — | `aief_gate`, computed from repository bytes |
| `spec/00` §3.2, *"binding on every component … No conflicts"* | — | `aief_clearance`, which then found `ECR-D-010` |

**Two defects were found by the checks rather than by reading.** `aief_approval` independently
re-derived `ECR-D-006` without being told it existed. `aief_clearance` found the locator/choke
collision that a frozen prose claim had asserted away.

**Scope held.** No Stage 6 work, no `MANIFEST.lock`, no ledger entry, no tag, no push.
