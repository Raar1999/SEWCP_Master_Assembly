# T-006 — Compiler Stage 6 Execution

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Template `core/templates/TPL-task-package.md`; schema `core/schemas/SCH-task.schema.json`.

```yaml
task_id:    T-006
role:       software.platform-engineer
objective:  Execute Compiler Stage 6 to emit core/MANIFEST.lock and write BINDING.core_digest_pin, satisfying boot step B2a.
status:     BLOCKED

inputs:
  - framework/AIEF-AMD-010 section AMD-31 preconditions
  - framework/AIEF-AMD-013 sections AMD-43 to AMD-48
  - src/aief_stage6/

deliverable:
  - .ai/core/MANIFEST.lock
  - .ai/project/BINDING.md core_digest_pin
  - The reproducible distributable and its DC-5 sidecar

acceptance_criteria:
  - id: AC-1
    criterion: Two executions produce a byte-identical distributable
    test: DC-5 of both archives agree
  - id: AC-2
    criterion: Boot step B2a executes and passes against the emitted lock
    test: A cold boot verifies the core aggregate against MANIFEST.lock
  - id: AC-3
    criterion: Every AMD-31 precondition is PASS at execution time
    test: The precondition report records V-01..V-08 and V-23..V-25 PASS

forbidden_actions:
  - Executing before OQ-14 explicit human authorization is recorded
  - Executing before OI-C-09 is delivered and re-certified
  - Proceeding on any failing AMD-31 precondition

escalation:
  - condition: A precondition fails at execution time
    to: chief-systems-engineer - halt, do not emit
  - condition: Authorization is absent
    to: human owner

depends_on:  [T-002, T-003]
consumes:    [R-002, R-003]
produces:    [R-006]
blocked_by:  [OQ-14]

read_scope:
  mandatory:
    - path: framework/AIEF-AMD-013_Boot_Budget_Determination_and_Stage_6_Build_Constructions.md
      anchor: AMD-43
    - path: .ai/project/BINDING.md
  optional:
    - path: framework/AIEF-AMD-010_Stage_6_Constructions_and_Preflight_Dispositions.md
  dependency:
    - result: R-002
    - result: R-003
  forbidden:
    - spec/**
    - cad/**
    - implementation/**

write_scope:
  - .ai/core/MANIFEST.lock
  - build/stage6/**
write_authority:
  paths:
    - .ai/core/MANIFEST.lock
  id: L7
  recorded_at: .ai/FRAMEWORK.md
  citation: Compiler Stage 6 is the emitting authority for layer L7, recorded in the Layers table of FRAMEWORK.md. The grant covers this one path and no other; it is gated by OQ-14 and reported as a notice by X-04 on every run.

qa:
  verifier_role: qa-engineer
  report:        .ai/project/verification/VER-011_Independent_Verification_Stage_6_Execution.md

context_budget:
  tf1: 12000
  tf2: 15000

checkpoint:
  phase: not started
  completed: []
  pending:
    - Await OQ-14 authorization
    - Await T-002 and T-003
  next_action: None. This task is blocked and must not be started.
  decision: null
```

## Notes

**This task is recorded, not executed.** It is BLOCKED on three independent grounds, each
derived rather than asserted: `depends_on` T-002 and T-003 are not COMPLETE; `consumes`
R-002 and R-003 do not exist; and `blocked_by` OQ-14 is open in `OPEN_ITEMS.md`.

`write_scope` names `.ai/core/MANIFEST.lock`, which lies inside the framework-protected set.
That is lawful **only** for Compiler Stage 6, which is the emitting authority for layer L7 —
and it is exactly why check X-04 flags this task's write scope for explicit review rather
than allowing it silently. Recording the task does not authorize the write; OQ-14 does.
