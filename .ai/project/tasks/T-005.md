# T-005 — OI-V-06 V-14 Trial Count and Crash Harness

> **Instance artifact.** Partition `project`. Owner `project-manager`. Mutability mutable.
> Template `core/templates/TPL-task-package.md`; schema `core/schemas/SCH-task.schema.json`.

```yaml
task_id:    T-005
role:       software.test-engineer
objective:  Declare the V-14 trial count and implement the crash-detection harness that boot-step B4 divergence detection requires.
status:     READY

inputs:
  - .ai/core/validation/CHECKS.md check V-14
  - .ai/project/OPEN_ITEMS_REGISTER.md row OI-V-06
  - src/aief_stage6/preconditions.py

deliverable:
  - tests/test_stage6_crash_trials.py
  - The declared trial count, recorded with its derivation

acceptance_criteria:
  - id: AC-1
    criterion: The trial count is declared with a stated derivation, not asserted
    test: The record cites how the number was chosen
  - id: AC-2
    criterion: Termination between entry write and HEAD update is detected in every trial
    test: The harness induces the fault and detects it at the next simulated boot

forbidden_actions:
  - Writing a real ledger entry or advancing project/ledger/HEAD
  - Editing any file under .ai/core/
  - Executing Compiler Stage 6

escalation:
  - condition: The trial count is an engineering decision beyond test authority
    to: chief-systems-engineer via ECR-Q
  - condition: Any other stop condition
    to: project-manager

depends_on:  []
consumes:    []
produces:    [R-005]
blocked_by:  []

read_scope:
  mandatory:
    - path: .ai/core/validation/CHECKS.md
      anchor: V-14
    - path: .ai/project/ledger/HEAD
  optional:
    - path: .ai/core/laws/LAW-09_session.md
  dependency: []
  forbidden:
    - spec/**
    - cad/**

write_scope:
  - tests/test_stage6_*.py

qa:
  verifier_role: qa-engineer
  report:        .ai/project/verification/VER-010_Independent_Verification_V-14_Harness.md

context_budget:
  tf1: 1500
  tf2: 2000

checkpoint:
  phase: not started
  completed: []
  pending:
    - Derive and declare the trial count
    - Implement the induced-fault harness
  next_action: Derive the trial count from the V-14 wording before writing any harness code.
  decision: null
```

## Notes

**This task exists in the backlog and conflicts with T-002 by construction.** Both declare
`tests/test_stage6_*.py` in `write_scope`. `graph.conflict_reasons` derives CONFLICT from
the overlap; neither task records that classification and neither can be dispatched with the
other. Resolving it is a `project-manager` sequencing action, not an agent decision.
